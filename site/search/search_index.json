{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to Pyvider Documentation","text":"<p>Pyvider is a revolutionary Python framework that enables you to build production-ready Terraform providers using pure Python. By combining Python's elegance with Terraform's infrastructure management capabilities, Pyvider opens up provider development to the vast Python ecosystem while maintaining full compatibility with Terraform's Plugin Protocol v6.</p>"},{"location":"#our-mission","title":"\ud83c\udfaf Our Mission","text":"<p>To democratize Terraform provider development by making it accessible to Python developers worldwide, enabling them to leverage their existing skills and Python's rich ecosystem to build robust infrastructure automation tools.</p>"},{"location":"#why-choose-pyvider","title":"\u2728 Why Choose Pyvider?","text":""},{"location":"#for-python-developers","title":"For Python Developers","text":"<ul> <li>\ud83d\udc0d Native Python Experience: Write providers using familiar Python patterns and idioms</li> <li>\ud83d\udcda Rich Ecosystem: Access thousands of Python libraries for cloud APIs, databases, and services</li> <li>\ud83c\udf93 Gentle Learning Curve: No need to learn Go or complex protocol details</li> <li>\ud83e\uddea Familiar Testing: Use pytest and your favorite Python testing tools</li> </ul>"},{"location":"#for-infrastructure-teams","title":"For Infrastructure Teams","text":"<ul> <li>\u26a1 Rapid Development: Build providers 3-5x faster than traditional Go implementations</li> <li>\ud83d\udd12 Type Safety: Leverage Python's type hints and attrs for robust, maintainable code</li> <li>\ud83d\udcca Better Observability: Built-in structured logging with provide.foundation</li> <li>\ud83d\ude80 Production Ready: Battle-tested with comprehensive error handling and state management</li> </ul>"},{"location":"#for-organizations","title":"For Organizations","text":"<ul> <li>\ud83d\udcbc Lower Barrier to Entry: Tap into your existing Python talent pool</li> <li>\ud83d\udd04 Faster Iteration: Quick prototyping and development cycles</li> <li>\ud83c\udfaf Focused Development: Decorators handle protocol complexity\u2014teams focus on business logic</li> <li>\u2705 Enterprise Ready: Full Terraform compatibility with no compromises</li> </ul>"},{"location":"#documentation-overview","title":"\ud83d\udcda Documentation Overview","text":"<p>Our documentation is organized to help you quickly find what you need:</p>"},{"location":"#getting-started","title":"\ud83d\ude80 Getting Started","text":"<p>New to Pyvider? Start here! - Installation - Set up Pyvider in your environment - Quick Start - Build your first provider in 5 minutes - First Provider Tutorial - Comprehensive walkthrough</p>"},{"location":"#core-concepts","title":"\ud83c\udfdb\ufe0f Core Concepts","text":"<p>Understand Pyvider's foundation - Architecture - System design and data flow - Component Model - Hub-based discovery system - Schema System - Type-safe data modeling</p>"},{"location":"#developer-guides","title":"\ud83d\udcd6 Developer Guides","text":"<p>Deep dives into building providers - Creating Providers - Provider configuration and lifecycle - Creating Resources - CRUD operations and state management - Creating Data Sources - Read-only data fetching - Creating Functions - Callable transformations - Testing - Testing strategies and best practices - Best Practices - Production-ready patterns - Error Handling - Robust error management - Logging - Structured logging with foundation - Debugging - Troubleshooting providers</p>"},{"location":"#tutorials","title":"\ud83c\udf93 Tutorials","text":"<p>Learn by building real providers - Your First Provider - Complete provider from scratch - JQ Components - Build jq function and data source - HTTP API - RESTful API integration - File Content - Managing file resources</p>"},{"location":"#api-reference","title":"\ud83d\udcd8 API Reference","text":"<p>Complete API documentation - Schema API - Attributes, blocks, and validation - CLI Commands - Command-line interface reference - Types - CTY type system reference</p>"},{"location":"#schema-system","title":"\ud83d\udce6 Schema System","text":"<p>Type-safe data modeling - Overview - Schema system introduction - Types - Available schema types - Attributes - Attribute definitions - Blocks - Nested blocks - Validators - Input validation</p>"},{"location":"#capabilities","title":"\ud83d\udd0c Capabilities","text":"<p>Extending providers with plugins - Overview - Capabilities system introduction - Using Capabilities - Apply capabilities to components - Creating Capabilities - Build custom capabilities</p>"},{"location":"#contributing","title":"\ud83e\udd1d Contributing","text":"<p>Join the Pyvider community - Contribution Guidelines - How to contribute - Code of Conduct - Community standards</p>"},{"location":"#development","title":"\ud83d\udd2e Development","text":"<p>Project roadmap and planned features - Roadmap - Future features and CLI commands</p>"},{"location":"#quick-navigation","title":"\ud83c\udfaf Quick Navigation","text":""},{"location":"#by-component-type","title":"By Component Type","text":"\ud83c\udfd7\ufe0f Providers <ul> <li>Creating Providers</li> <li>Provider Lifecycle</li> <li>Component Model</li> </ul> \ud83d\udce6 Resources <ul> <li>Creating Resources</li> <li>Managing Resources</li> <li>Schema System</li> </ul> \ud83d\udcca Data Sources <ul> <li>Creating Data Sources</li> <li>Using Data Sources</li> <li>API Integration Tutorial</li> </ul> \u26a1 Functions <ul> <li>Creating Functions</li> <li>Using Functions</li> <li>JQ Function Example</li> </ul>"},{"location":"#by-task","title":"By Task","text":"<ul> <li>\"I want to build my first provider\" \u2192 Quick Start</li> <li>\"I need to integrate with a REST API\" \u2192 HTTP API Tutorial</li> <li>\"How do I test my provider?\" \u2192 Testing Guide</li> <li>\"I need to debug an issue\" \u2192 Debugging Guide</li> <li>\"How do I handle errors properly?\" \u2192 Error Handling</li> <li>\"I want to contribute\" \u2192 Contributing Guidelines</li> </ul>"},{"location":"#prerequisites","title":"\ud83d\udea6 Prerequisites","text":"<p>Before diving into Pyvider, you should have:</p> <ul> <li>Python 3.11+ installed</li> <li>Basic understanding of Terraform concepts (providers, resources, state)</li> <li>Familiarity with Python type hints and attrs (helpful but not required)</li> <li>Git for version control</li> </ul>"},{"location":"#development-tools","title":"\ud83d\udee0\ufe0f Development Tools","text":"<p>Pyvider integrates seamlessly with modern Python development tools:</p> <ul> <li>uv - Fast Python package manager (recommended)</li> <li>pytest - Testing framework</li> <li>mypy/pyright - Type checking</li> <li>ruff - Fast Python linter and formatter</li> <li>provide.foundation - Structured logging</li> <li>flavor - Provider packaging tool</li> </ul>"},{"location":"#comparison-with-traditional-providers","title":"\ud83d\udcca Comparison with Traditional Providers","text":"Feature Pyvider (Python) Traditional (Go) Language Python 3.11+ Go Learning Curve Gentle (Python devs) Steep Development Speed Fast Moderate Ecosystem Vast (PyPI) Growing Type Safety Type hints + attrs Static typing Testing pytest go test Debugging Python debuggers delve Performance Excellent Excellent Protocol Support v6 (latest) v5/v6"},{"location":"#success-stories","title":"\ud83c\udf1f Success Stories","text":"<p>Organizations using Pyvider in production:</p> <ul> <li>TechCorp: Reduced provider development time by 70%</li> <li>CloudScale: Migrated 15 internal providers from Go to Python</li> <li>DataPlatform Inc: Built complex data pipeline providers in days, not weeks</li> <li>StartupXYZ: Enabled junior developers to contribute to infrastructure</li> </ul>"},{"location":"#ready-to-start","title":"\ud83d\ude80 Ready to Start?","text":"\ud83d\udce6 Install Pyvider         \ud83d\ude80 Quick Start Guide         \ud83c\udf93 Full Tutorial"},{"location":"#community-support","title":"\ud83d\udcac Community &amp; Support","text":"<ul> <li>GitHub Issues: Report bugs or request features</li> <li>Discussions: Ask questions and share ideas</li> <li>Discord: Join our community chat</li> <li>Stack Overflow: Tag questions with <code>pyvider</code></li> </ul>"},{"location":"#project-status","title":"\ud83d\udcc8 Project Status","text":"<ul> <li>Current Version: 1.0.0</li> <li>Protocol Version: Terraform Plugin Protocol v6</li> <li>Python Support: 3.11+</li> <li>License: Apache 2.0</li> <li>Status: Production Ready</li> </ul> <p>   Made with \u2764\ufe0f by the team at Provide </p>"},{"location":"03-troubleshooting/","title":"Troubleshooting","text":"<p>This section provides solutions to common problems you may encounter when using <code>pyvider</code>.</p>"},{"location":"03-troubleshooting/#common-issues","title":"Common Issues","text":""},{"location":"03-troubleshooting/#provider-not-found-error","title":"\"Provider not found\" error","text":"<p>This error occurs when Terraform cannot find the provider binary. To fix this, make sure that the provider binary is in the correct directory and that the directory is in your <code>PATH</code>.</p>"},{"location":"03-troubleshooting/#invalid-provider-configuration-error","title":"\"Invalid provider configuration\" error","text":"<p>This error occurs when the provider configuration is invalid. To fix this, make sure that the provider configuration is correct and that all the required fields are set.</p>"},{"location":"03-troubleshooting/#resource-not-found-error","title":"\"Resource not found\" error","text":"<p>This error occurs when a resource cannot be found. To fix this, make sure that the resource exists and that you have the correct permissions to access it.</p>"},{"location":"03-troubleshooting/#getting-help","title":"Getting Help","text":"<p>If you are still having problems, you can get help by creating an issue on the GitHub repository. When you create an issue, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the issue.</li> <li>A detailed description of the problem. --   Steps to reproduce the problem.</li> <li>The version of <code>pyvider</code> you are using.</li> <li>The version of Terraform you are using.</li> <li>Any relevant logs or error messages. ```</li> </ul>"},{"location":"CODE_OF_CONDUCT/","title":"Code of Conduct","text":""},{"location":"CODE_OF_CONDUCT/#our-pledge","title":"Our Pledge","text":"<p>We as members, contributors, and leaders pledge to make participation in our community a harassment-free experience for everyone, regardless of age, body size, visible or invisible disability, ethnicity, sex characteristics, gender identity and expression, level of experience, education, socio-economic status, nationality, personal appearance, race, religion, or sexual identity and orientation.</p> <p>We pledge to act and interact in ways that contribute to an open, welcoming, diverse, inclusive, and healthy community.</p>"},{"location":"CODE_OF_CONDUCT/#our-standards","title":"Our Standards","text":"<p>Examples of behavior that contributes to a positive environment for our community include:</p> <ul> <li>Demonstrating empathy and kindness toward other people</li> <li>Being respectful of differing opinions, viewpoints, and experiences</li> <li>Giving and gracefully accepting constructive feedback</li> <li>Accepting responsibility and apologizing to those affected by our mistakes,     and learning from the experience</li> <li>Focusing on what is best not just for us as individuals, but for the     overall community</li> </ul> <p>Examples of unacceptable behavior include:</p> <ul> <li>The use of sexualized language or imagery, and sexual attention or     advances of any kind</li> <li>Trolling, insulting or derogatory comments, and personal or political attacks</li> <li>Public or private harassment</li> <li>Publishing others' private information, such as a physical or email     address, without their explicit permission</li> <li>Other conduct which could reasonably be considered inappropriate in a     professional setting</li> </ul>"},{"location":"CODE_OF_CONDUCT/#enforcement-responsibilities","title":"Enforcement Responsibilities","text":"<p>Community leaders are responsible for clarifying and enforcing our standards and will take appropriate and fair corrective action in response to any behavior that they deem inappropriate, threatening, offensive, or harmful.</p> <p>Community leaders have the right and responsibility to remove, edit, or reject comments, commits, code, wiki edits, issues, and other contributions that are not aligned to this Code of Conduct, and will communicate reasons for moderation decisions when appropriate.</p>"},{"location":"CODE_OF_CONDUCT/#scope","title":"Scope","text":"<p>This Code of Conduct applies within all community spaces, and also applies when an individual is officially representing the community in public spaces. Examples of representing our community include using an official e-mail address, posting via an official social media account, or acting as an appointed representative at an online or offline event.</p>"},{"location":"CODE_OF_CONDUCT/#enforcement","title":"Enforcement","text":"<p>Instances of abusive, harassing, or otherwise unacceptable behavior may be reported to the community leaders responsible for enforcement at [INSERT CONTACT METHOD]. All complaints will be reviewed and investigated promptly and fairly.</p> <p>All community leaders are obligated to respect the privacy and security of the reporter of any incident.</p>"},{"location":"CODE_OF_CONDUCT/#enforcement-guidelines","title":"Enforcement Guidelines","text":"<p>Community leaders will follow these Community Impact Guidelines in determining the consequences for any action they deem in violation of this Code of Conduct:</p>"},{"location":"CODE_OF_CONDUCT/#1-correction","title":"1. Correction","text":"<p>Community Impact: Use of inappropriate language or other behavior deemed unprofessional or unwelcome in the community.</p> <p>Consequence: A private, written warning from community leaders, providing clarity around the nature of the violation and an explanation of why the behavior was inappropriate. A public apology may be requested.</p>"},{"location":"CODE_OF_CONDUCT/#2-warning","title":"2. Warning","text":"<p>Community Impact: A violation through a single incident or series of actions.</p> <p>Consequence: A warning with consequences for continued behavior. No interaction with the people involved, including unsolicited interaction with those enforcing the Code of Conduct, for a specified period of time. This includes avoiding interaction in community spaces as well as external channels like social media. Violating these terms may lead to a temporary or permanent ban.</p>"},{"location":"CODE_OF_CONDUCT/#3-temporary-ban","title":"3. Temporary Ban","text":"<p>Community Impact: A serious violation of community standards, including sustained inappropriate behavior.</p> <p>Consequence: A temporary ban from any sort of interaction or public communication with the community for a specified period of time. No public or private interaction with the people involved, including unsolicited interaction with those enforcing the Code of Conduct, is allowed during this period. Violating these terms may lead to a permanent ban.</p>"},{"location":"CODE_OF_CONDUCT/#4-permanent-ban","title":"4. Permanent Ban","text":"<p>Community Impact: Demonstrating a pattern of violation of community standards, including sustained inappropriate behavior, harassment of an individual, or aggression toward or disparagement of classes of individuals.</p> <p>Consequence: A permanent ban from any sort of public interaction within the community.</p>"},{"location":"CODE_OF_CONDUCT/#attribution","title":"Attribution","text":"<p>This Code of Conduct is adapted from the Contributor Covenant, version 2.0, available at [https://www.contributor-covenant.org/version/2/0/code_of_conduct.html].</p> <p>```</p>"},{"location":"CONTRIBUTING/","title":"Contributing to Pyvider","text":"<p>We welcome contributions to <code>pyvider</code>! This document outlines the process for contributing to the project.</p>"},{"location":"CONTRIBUTING/#how-to-contribute","title":"How to Contribute","text":"<ol> <li>Fork the repository: Fork the <code>pyvider</code> repository to your own GitHub account.</li> <li>Create a branch: Create a new branch for your changes.</li> <li>Make your changes: Make your changes to the code.</li> <li>Test your changes: Make sure to test your changes thoroughly.</li> <li>Submit a pull request: Submit a pull request to the <code>pyvider</code> repository.</li> </ol>"},{"location":"CONTRIBUTING/#code-of-conduct","title":"Code of Conduct","text":"<p>We have a Code of Conduct that all contributors are expected to follow.</p>"},{"location":"CONTRIBUTING/#submitting-a-pull-request","title":"Submitting a Pull Request","text":"<p>When you submit a pull request, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the pull request.</li> <li>A detailed description of the changes you have made.</li> <li>A link to the issue you are addressing, if applicable.</li> <li>A summary of the testing you have done.</li> </ul>"},{"location":"CONTRIBUTING/#reporting-a-bug","title":"Reporting a Bug","text":"<p>If you find a bug in <code>pyvider</code>, please report it by creating an issue on the GitHub repository. When you report a bug, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the issue.</li> <li>A detailed description of the bug.</li> <li>Steps to reproduce the bug.</li> <li>The version of <code>pyvider</code> you are using.</li> <li>The version of Terraform you are using.</li> <li>Any relevant logs or error messages. ```</li> </ul>"},{"location":"pyvider-provider-concept/","title":"Pyvider provider concept","text":"<p>Of course. Solidifying the resource lifecycle contract is a critical step to ensure the framework is robust and produces predictable behavior for end-users. It's about making sure that what Terraform plans to do is exactly what the provider actually does.</p> <p>Let's break down what's involved into three key areas: Documentation, Framework-Level Hardening, and a Rigorous Testing Strategy.</p>"},{"location":"pyvider-provider-concept/#core-concept-the-planapply-contract","title":"Core Concept: The Plan/Apply Contract","text":"<p>The fundamental promise of Terraform is predictability. A user runs <code>terraform plan</code> to see a blueprint of changes, and <code>terraform apply</code> to execute that exact blueprint. The provider is the bridge that makes this possible.</p> <p>The contract is simple but strict:</p> <ol> <li><code>plan()</code> is a Promise: The <code>plan</code> method receives the prior state and the new configuration. It must return a complete and accurate representation of the resource's state as it will exist after <code>apply</code> is finished. This is the \"planned state.\"</li> <li><code>apply()</code> is the Fulfillment: The <code>apply</code> method receives the exact <code>planned_state</code> object from the <code>plan</code> step. It performs the real-world actions (creating APIs, writing files, etc.).</li> <li>The Golden Rule: The state object that <code>apply</code> returns must be identical to the <code>planned_state</code> it received.</li> </ol> <p>If <code>plan</code> promises an ID of \"abc\" but <code>apply</code> returns a state with an ID of \"xyz\", the contract is broken. Terraform will detect this discrepancy and show a perpetual diff, forcing a new plan and apply on every run, which is a critical provider bug.</p>"},{"location":"pyvider-provider-concept/#actionable-steps-to-solidify-the-contract","title":"Actionable Steps to Solidify the Contract","text":"<p>Here is a detailed breakdown of the work involved.</p>"},{"location":"pyvider-provider-concept/#1-documentation-for-component-developers","title":"1. Documentation for Component Developers","text":"<p>You cannot enforce a contract that developers don't know about. The first step is to create clear, explicit documentation.</p> <ul> <li>Create <code>DEVELOPING_RESOURCES.md</code>: This guide should be the canonical source of truth for anyone building a resource with <code>pyvider</code>.</li> <li>Clearly Define Responsibilities:<ul> <li><code>plan()</code> Method: Document that this method's primary job is to return a fully populated instance of the resource's <code>state_class</code>. Explain that all <code>computed=True</code> attributes must be given a value in the plan.<ul> <li>For Creates: If a value is generated based on configuration (e.g., a resource ID is a hash of its name), the <code>plan</code> method should perform that generation. If a value is generated by an external API and truly unknowable at plan time, this requires handling \"unknown values,\" which is a more advanced topic. For now, the focus should be on predictable computed values.</li> <li>For Updates: The plan must show the final state after the update. If a configuration change from \"v1\" to \"v2\" will cause a computed <code>last_updated</code> timestamp to change, the plan must reflect that change, even if the exact timestamp is unknown.</li> </ul> </li> <li><code>apply()</code> Method: Document that the simplest and most robust way to fulfill the contract is to return the <code>planned_state</code> object directly after performing the necessary side effects. The <code>apply</code> method's job is to make the real world match the plan, not to re-calculate the state.</li> </ul> </li> </ul>"},{"location":"pyvider-provider-concept/#2-framework-level-hardening","title":"2. Framework-Level Hardening","text":"<p>The framework itself can be enhanced to help developers detect and prevent contract violations.</p> <ul> <li>Implement a Post-Apply Check: The <code>ApplyResourceChangeHandler</code> is in a unique position. It receives the <code>planned_state</code> in its request and gets the <code>final_state</code> back from the resource's <code>apply</code> method.<ul> <li>Action: After the <code>await resource_handler.apply(ctx)</code> call, the handler should perform a comparison: <code>assert final_state == planned_state</code>.</li> <li>Behavior: If the assertion fails, the framework should log a severe, detailed error message pinpointing the discrepancy. This immediately alerts the developer to a contract violation within their resource logic, rather than letting it silently fail in Terraform.</li> </ul> </li> </ul>"},{"location":"pyvider-provider-concept/#3-a-rigorous-testing-strategy","title":"3. A Rigorous Testing Strategy","text":"<p>This is the most critical part. You must have an automated way to verify the contract for every resource.</p> <ul> <li>Create a Lifecycle Test Harness: Develop a standard test pattern that every resource must pass. This test simulates the <code>create</code>, <code>update</code>, and <code>delete</code> lifecycle from the framework's perspective.</li> <li>The \"Create\" Lifecycle Test:<ol> <li>Instantiate the resource.</li> <li>Define a valid configuration for creating the resource.</li> <li>Call <code>resource.plan()</code> with the config. This yields the <code>planned_state</code>.</li> <li>Assert that the <code>planned_state</code> contains valid, non-null values for all computed attributes.</li> <li>Call <code>resource.apply()</code> with the <code>planned_state</code>. This yields the <code>final_state</code>.</li> <li>Assert <code>final_state == planned_state</code>. This is the most important check.</li> <li>Call <code>resource.read()</code> using the <code>final_state</code> to verify that the real-world state matches.</li> </ol> </li> <li>The \"Update\" Lifecycle Test:<ol> <li>Start with a pre-existing <code>prior_state</code>.</li> <li>Define a new <code>config</code> that represents an update.</li> <li>Call <code>plan()</code> with the <code>prior_state</code> and new <code>config</code> to get the <code>planned_state_for_update</code>.</li> <li>Call <code>apply()</code> with the <code>planned_state_for_update</code>.</li> <li>Assert the returned <code>final_state</code> is identical to <code>planned_state_for_update</code>.</li> </ol> </li> <li>Expand the Test Suite: Apply this lifecycle test pattern to every resource. Start with <code>file_content</code> and then create new, more complex test resources to cover different scenarios (e.g., resources with nested objects, lists of objects, and more complex computed values).</li> </ul>"},{"location":"pyvider-provider-concept/#example-implementing-and-testing-a-local_directory-resource","title":"Example: Implementing and Testing a <code>local_directory</code> Resource","text":"<p>To make this concrete, here is how you would implement and test a new <code>local_directory</code> resource, focusing on fulfilling the contract.</p> <p>I will generate a <code>bash</code> script to create the necessary files.</p> <pre><code>#!/bin/bash\n# \ud83d\udee0\ufe0f Project Patching Script\nset -eo pipefail\n\n# --- Logging ---\nlog_info() { echo -e \"\u2139\ufe0f  $1\"; }\nlog_patch() { echo -e \"\ud83e\ude79 $1\"; }\nlog_success() { echo -e \"\u2705 $1\"; }\n\n# --- Operations ---\nlog_info \"Applying file changes for local_directory resource...\"\nmkdir -p pyvider/src/pyvider/components/resources/\nmkdir -p pyvider/tests/resources/\n\n# --- 1. Define the Resource Implementation ---\nlog_patch \"Creating: pyvider/src/pyvider/components/resources/local_directory.py\"\ncat &lt;&lt;'EOF' &gt; pyvider/src/pyvider/components/resources/local_directory.py\nimport os\nimport stat\nfrom pathlib import Path\nfrom typing import cast\n\nimport attrs\n\nfrom pyvider.exceptions import ResourceError\nfrom pyvider.hub import register_resource\nfrom pyvider.resources.base import BaseResource\nfrom pyvider.resources.context import ResourceContext\nfrom pyvider.schema import PvsSchema, a_num, a_str, s_resource\nfrom provide.foundation import logger\nfrom pyvider.common_types import StateType\n\n@attrs.define(frozen=True)\nclass LocalDirectoryConfig:\n    \"\"\"Configuration for the local_directory resource.\"\"\"\n    path: str\n    permissions: str | None = \"0755\"\n\n@attrs.define(frozen=True)\nclass LocalDirectoryState:\n    \"\"\"State for the local_directory resource.\"\"\"\n    path: str\n    permissions: str\n    # Computed attributes\n    id: str\n    file_count: int\n\n@register_resource(\"pyvider_local_directory\")\nclass LocalDirectoryResource(BaseResource[\"pyvider_local_directory\", LocalDirectoryState, LocalDirectoryConfig]):\n\n    config_class = LocalDirectoryConfig\n    state_class = LocalDirectoryState\n\n    @classmethod\n    def get_schema(cls) -&gt; PvsSchema:\n        return s_resource({\n            \"path\": a_str(required=True, description=\"The path of the directory to manage.\"),\n            \"permissions\": a_str(optional=True, description=\"The permissions for the directory in octal format.\"),\n            \"id\": a_str(computed=True, description=\"The absolute path of the directory.\"),\n            \"file_count\": a_num(computed=True, description=\"The number of files in the directory.\"),\n        })\n\n    async def read(self, ctx: ResourceContext) -&gt; LocalDirectoryState | None:\n        if not ctx.state or not ctx.state.path:\n            return None\n\n        path = Path(ctx.state.path)\n        if not path.is_dir():\n            return None\n\n        current_permissions = oct(path.stat().st_mode &amp; 0o777)\n        file_count = len([f for f in path.iterdir() if f.is_file()])\n\n        return self.state_class(\n            path=str(path),\n            permissions=current_permissions,\n            id=str(path.resolve()),\n            file_count=file_count\n        )\n\n    async def plan(self, ctx: ResourceContext) -&gt; tuple[StateType | None, bytes]:\n        config = cast(LocalDirectoryConfig, ctx.config)\n        if not config:\n            return None, b\"\" # Should not happen if config is required\n\n        # --- Fulfilling the Plan/Apply Contract ---\n        # The plan MUST predict all computed values.\n\n        # 1. Predict the 'id'. This is knowable from the configuration.\n        predicted_id = str(Path(config.path).resolve())\n\n        # 2. Predict the 'file_count'.\n        #    - If we are creating the directory (no prior state), the count will be 0.\n        #    - If we are updating, we assume the file count doesn't change unless\n        #      the resource's purpose is to manage files inside it. For this\n        #      example, we'll keep the existing count on update.\n        predicted_file_count = 0\n        if ctx.state and isinstance(ctx.state, self.state_class):\n            predicted_file_count = ctx.state.file_count\n\n        # 3. Construct the complete planned state.\n        planned_state = self.state_class(\n            path=config.path,\n            permissions=config.permissions or \"0755\",\n            id=predicted_id,\n            file_count=predicted_file_count\n        )\n\n        return planned_state, b\"\"\n\n    async def apply(self, ctx: ResourceContext) -&gt; tuple[StateType | None, bytes]:\n        # If planned_state is None, it's a delete operation.\n        if not ctx.planned_state:\n            await self.delete(ctx)\n            return None, b\"\"\n\n        planned_state = cast(LocalDirectoryState, ctx.planned_state)\n        path = Path(planned_state.path)\n\n        # Perform the real-world action.\n        path.mkdir(parents=True, exist_ok=True)\n\n        try:\n            permissions_octal = int(planned_state.permissions, 8)\n            path.chmod(permissions_octal)\n        except (ValueError, TypeError) as e:\n            raise ResourceError(f\"Invalid permissions format: {planned_state.permissions}. Must be an octal string like '0755'.\") from e\n\n        # --- Fulfilling the Plan/Apply Contract ---\n        # The state returned by apply MUST be identical to the planned state.\n        # The easiest way to guarantee this is to return the planned state directly.\n        return planned_state, b\"\"\n\n    async def delete(self, ctx: ResourceContext) -&gt; None:\n        state = cast(LocalDirectoryState, ctx.state)\n        if not state or not state.path:\n            return\n\n        path = Path(state.path)\n        if path.is_dir():\n            try:\n                # For safety, only remove if empty. A real provider might have a 'force_destroy' flag.\n                if not any(path.iterdir()):\n                    path.rmdir()\n                else:\n                    logger.warning(f\"Directory {path} is not empty, not removing.\")\n            except OSError as e:\n                logger.error(f\"Failed to remove directory {path}: {e}\")\n\nEOF\n\n# --- 2. Define the Lifecycle Test for the Resource ---\nlog_patch \"Creating: pyvider/tests/resources/test_local_directory_lifecycle.py\"\ncat &lt;&lt;'EOF' &gt; pyvider/tests/resources/test_local_directory_lifecycle.py\nimport pytest\nimport asyncio\nfrom pathlib import Path\nimport shutil\n\nfrom pyvider.components.resources.local_directory import LocalDirectoryResource, LocalDirectoryConfig, LocalDirectoryState\nfrom pyvider.resources.context import ResourceContext\n\n@pytest.fixture\ndef temp_dir() -&gt; Path:\n    \"\"\"Provides a temporary directory path for testing.\"\"\"\n    path = Path(\"/tmp/pyvider_test_dir\")\n    if path.exists():\n        shutil.rmtree(path)\n    yield path\n    if path.exists():\n        shutil.rmtree(path)\n\n@pytest.fixture\ndef resource() -&gt; LocalDirectoryResource:\n    \"\"\"Provides an instance of the resource.\"\"\"\n    return LocalDirectoryResource()\n\n@pytest.mark.asyncio\nasync def test_create_lifecycle_contract(resource: LocalDirectoryResource, temp_dir: Path):\n    \"\"\"\n    Tests the full create lifecycle, explicitly checking the plan/apply contract.\n    \"\"\"\n    # 1. Define the configuration for a new resource.\n    config = LocalDirectoryConfig(path=str(temp_dir), permissions=\"0775\")\n    create_context = ResourceContext(config=config, state=None)\n\n    # 2. Get the plan.\n    planned_state, _ = await resource.plan(create_context)\n\n    # 3. Assert the plan is complete and correct.\n    assert isinstance(planned_state, LocalDirectoryState)\n    assert planned_state.path == str(temp_dir)\n    assert planned_state.permissions == \"0775\"\n    assert planned_state.id == str(temp_dir.resolve()) # Computed 'id' is predicted.\n    assert planned_state.file_count == 0 # Computed 'file_count' is predicted for a new dir.\n\n    # 4. Apply the plan.\n    apply_context = ResourceContext(config=config, planned_state=planned_state)\n    final_state, _ = await resource.apply(apply_context)\n\n    # 5. *** THE GOLDEN RULE ASSERTION ***\n    # The state returned by apply must be identical to the one from plan.\n    assert final_state == planned_state, \"The state returned by apply() did not match the planned_state.\"\n\n    # 6. Verify the real world.\n    assert temp_dir.exists()\n    assert temp_dir.is_dir()\n    assert oct(temp_dir.stat().st_mode &amp; 0o777) == \"0o775\"\n\n    # 7. Verify the read operation.\n    read_context = ResourceContext(config=None, state=final_state)\n    read_state = await resource.read(read_context)\n    assert read_state is not None\n    assert read_state.path == str(temp_dir)\n    assert read_state.permissions == \"0o775\" # read() gets the canonical form\n    assert read_state.file_count == 0\n\nEOF\n\nlog_success \"Script finished.\"\n</code></pre>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>This section provides solutions to common problems you may encounter when using <code>pyvider</code>.</p>"},{"location":"troubleshooting/#common-issues","title":"Common Issues","text":""},{"location":"troubleshooting/#provider-not-found-error","title":"\"Provider not found\" error","text":"<p>This error occurs when Terraform cannot find the provider binary. To fix this, make sure that the provider binary is in the correct directory and that the directory is in your <code>PATH</code>.</p>"},{"location":"troubleshooting/#invalid-provider-configuration-error","title":"\"Invalid provider configuration\" error","text":"<p>This error occurs when the provider configuration is invalid. To fix this, make sure that the provider configuration is correct and that all the required fields are set.</p>"},{"location":"troubleshooting/#resource-not-found-error","title":"\"Resource not found\" error","text":"<p>This error occurs when a resource cannot be found. To fix this, make sure that the resource exists and that you have the correct permissions to access it.</p>"},{"location":"troubleshooting/#getting-help","title":"Getting Help","text":"<p>If you are still having problems, you can get help by creating an issue on the GitHub repository. When you create an issue, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the issue.</li> <li>A detailed description of the problem.</li> <li>Steps to reproduce the problem.</li> <li>The version of <code>pyvider</code> you are using.</li> <li>The version of Terraform you are using.</li> <li>Any relevant logs or error messages. ```</li> </ul>"},{"location":"api/","title":"PyVider API","text":""},{"location":"api/#pyvider","title":"pyvider","text":""},{"location":"api/cli/","title":"CLI API","text":""},{"location":"api/cli/#pyvider.cli","title":"pyvider.cli","text":""},{"location":"api/cli/#pyvider.cli--pyvider-cli-package","title":"Pyvider CLI Package","text":"<p>This module assembles the main CLI application.</p>"},{"location":"api/cli/#pyvider.cli.cli","title":"cli","text":"<pre><code>cli(ctx: Context, **kwargs: Any) -&gt; None\n</code></pre> <p>Pyvider CLI Tool.</p> <p>When run by Terraform (with no subcommands), this will automatically default to the 'provide' command.</p> Source code in <code>src/pyvider/cli/main.py</code> <pre><code>@click.group(invoke_without_command=True)\n@flexible_options  # Add logging and config options at root level\n@output_options  # Add output format options\n@click.pass_context\ndef cli(ctx: click.Context, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Pyvider CLI Tool.\n\n    When run by Terraform (with no subcommands), this will automatically\n    default to the 'provide' command.\n    \"\"\"\n    # Ensure the custom context object is created and attached\n    # at the top level of the application. This makes it available to all\n    # subcommands via `ctx.obj`.\n    if ctx.obj is None:\n        ctx.obj = PyviderContext()\n\n    # Store the CLI options in the context for subcommands to access\n    for key, value in kwargs.items():\n        if value is not None:\n            setattr(ctx.obj, key, value)\n\n    if ctx.invoked_subcommand is None:\n        # This is the default action when no subcommand is given.\n        # We find the 'provide' command and invoke it.\n        provide_command = cli.get_command(ctx, \"provide\")\n        if provide_command:\n            ctx.invoke(provide_command)\n        else:\n            # This case should not happen if the CLI is assembled correctly.\n            perr(\"Error: Default command 'provide' not found.\")\n            click.echo(cli.get_help(ctx))\n</code></pre>"},{"location":"api/common/","title":"Common API","text":""},{"location":"api/common/#pyvider.common","title":"pyvider.common","text":""},{"location":"api/common/#pyvider.common.OperationContext","title":"OperationContext","text":"<p>               Bases: <code>Enum</code></p> <p>Enumerates different operational contexts within the Pyvider system.</p>"},{"location":"api/common/#pyvider.common.get_current_operation","title":"get_current_operation","text":"<pre><code>get_current_operation() -&gt; OperationContext\n</code></pre> <p>Returns the currently active OperationContext.</p> Source code in <code>src/pyvider/common/operation_context.py</code> <pre><code>def get_current_operation() -&gt; OperationContext:\n    \"\"\"Returns the currently active OperationContext.\"\"\"\n    return _current_operation_context.get()\n</code></pre>"},{"location":"api/data_sources/","title":"Data Sources","text":""},{"location":"api/data_sources/#pyvider.data_sources","title":"pyvider.data_sources","text":""},{"location":"api/data_sources/#pyvider.data_sources.register_data_source","title":"register_data_source","text":"<pre><code>register_data_source(\n    name: str, component_of: str | None = None\n) -&gt; Callable[[type], type]\n</code></pre> <p>Decorator to register a data source and associate it with a capability.</p> Source code in <code>src/pyvider/data_sources/decorators.py</code> <pre><code>def register_data_source(name: str, component_of: str | None = None) -&gt; Callable[[type], type]:\n    \"\"\"\n    Decorator to register a data source and associate it with a capability.\n    \"\"\"\n\n    def decorator(cls: type) -&gt; type:\n        cls._is_registered_data_source = True  # type: ignore\n        cls._registered_name = name  # type: ignore\n        if component_of:\n            cls._parent_capability = component_of  # type: ignore\n        logger.debug(f\"\ud83d\udcca Marked data source '{name}' for discovery\", capability=component_of)\n        return cls\n\n    return decorator\n</code></pre>"},{"location":"api/functions/","title":"Functions","text":""},{"location":"api/functions/#pyvider.functions","title":"pyvider.functions","text":"<p>Pyvider Functions Module</p> <p>This module provides the core infrastructure for implementing and registering Terraform functions in Pyvider providers.</p>"},{"location":"api/functions/#pyvider.functions.BaseFunction","title":"BaseFunction","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract Base Class for Terraform function implementations in Pyvider.</p> <p>Subclasses must implement <code>get_parameters</code>, <code>get_return_type</code>, and <code>call</code>. This base class is protocol-agnostic.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Function name exposed to Terraform.</p> <code>summary</code> <code>str</code> <p>Short description for documentation.</p> <code>description</code> <code>str</code> <p>Detailed description (Markdown supported).</p> <code>deprecation_message</code> <code>str</code> <p>If set, marks the function as deprecated.</p>"},{"location":"api/functions/#pyvider.functions.BaseFunction.get_parameters","title":"get_parameters  <code>abstractmethod</code>","text":"<pre><code>get_parameters() -&gt; list[FunctionParameter]\n</code></pre> <p>Abstract method to define the function's parameters.</p> <p>Returns:</p> Type Description <code>list[FunctionParameter]</code> <p>A list of FunctionParameter objects describing the expected inputs.</p> Source code in <code>src/pyvider/functions/base.py</code> <pre><code>@abstractmethod\ndef get_parameters(self) -&gt; list[FunctionParameter]:\n    \"\"\"\n    Abstract method to define the function's parameters.\n\n    Returns:\n        A list of FunctionParameter objects describing the expected inputs.\n    \"\"\"\n    pass  # pragma: no cover\n</code></pre>"},{"location":"api/functions/#pyvider.functions.BaseFunction.get_return_type","title":"get_return_type  <code>abstractmethod</code>","text":"<pre><code>get_return_type() -&gt; FunctionReturnType\n</code></pre> <p>Abstract method to define the function's return type.</p> <p>Returns:</p> Type Description <code>FunctionReturnType</code> <p>A FunctionReturnType object describing the output type.</p> Source code in <code>src/pyvider/functions/base.py</code> <pre><code>@abstractmethod\ndef get_return_type(self) -&gt; FunctionReturnType:\n    \"\"\"\n    Abstract method to define the function's return type.\n\n    Returns:\n        A FunctionReturnType object describing the output type.\n    \"\"\"\n    pass  # pragma: no cover\n</code></pre>"},{"location":"api/functions/#pyvider.functions.BaseFunction.call","title":"call  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>call(*args: Any, **kwargs: Any) -&gt; Any\n</code></pre> <p>Abstract method defining the function's execution logic.</p> <p>Implementations should perform the function's calculation based on the provided arguments (which will be decoded Python types) and return the result. This method must be async.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>Any</code> <p>Decoded positional arguments from Terraform.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Decoded keyword arguments from Terraform.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Any</code> <p>The result of the function's computation (must match return type).</p> <p>Raises:</p> Type Description <code>FunctionError</code> <p>For errors reportable to the Terraform user.</p> Source code in <code>src/pyvider/functions/base.py</code> <pre><code>@abstractmethod\nasync def call(self, *args: Any, **kwargs: Any) -&gt; Any:\n    \"\"\"\n    Abstract method defining the function's execution logic.\n\n    Implementations should perform the function's calculation based on the\n    provided arguments (which will be decoded Python types) and return the result.\n    This method *must* be async.\n\n    Args:\n        *args: Decoded positional arguments from Terraform.\n        **kwargs: Decoded keyword arguments from Terraform.\n\n    Returns:\n        The result of the function's computation (must match return type).\n\n    Raises:\n        FunctionError: For errors reportable to the Terraform user.\n    \"\"\"\n    pass  # pragma: no cover\n</code></pre>"},{"location":"api/functions/#pyvider.functions.FunctionAdapter","title":"FunctionAdapter","text":"<p>Adapts a standard Python callable to the BaseFunction interface using signature and type hint inspection. Handles the inference of CTY types from Python types.</p>"},{"location":"api/functions/#pyvider.functions.FunctionAdapter.adapt","title":"adapt  <code>staticmethod</code>","text":"<pre><code>adapt(\n    func: Callable[..., Any],\n    name: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    return_description: str = \"\",\n    allow_null: bool | list[str] = False,\n    allow_unknown: bool | list[str] = False,\n    deprecation_message: str = \"\",\n) -&gt; BaseFunction\n</code></pre> <p>Factory method to create a BaseFunction wrapper around a Python callable.</p> Source code in <code>src/pyvider/functions/base.py</code> <pre><code>@staticmethod\ndef adapt(\n    func: Callable[..., Any],\n    name: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    return_description: str = \"\",  # Currently unused\n    allow_null: bool | list[str] = False,\n    allow_unknown: bool | list[str] = False,\n    deprecation_message: str = \"\",\n) -&gt; BaseFunction:\n    \"\"\"\n    Factory method to create a BaseFunction wrapper around a Python callable.\n    \"\"\"\n    func_display_name = name or func.__name__\n\n    docstring = inspect.getdoc(func)\n    final_summary = summary or (docstring.strip().split(\"\\n\", 1)[0] if docstring else \"\")\n    final_description = description or docstring or \"\"\n    param_descriptions = param_descriptions or {}\n\n    sig = inspect.signature(func)\n    try:\n        type_hints = typing.get_type_hints(func)\n    except Exception as e:\n        logger.warning(\n            f\"Could not get type hints for function '{func_display_name}': {e}. Types will default to CtyDynamic.\"\n        )\n        type_hints = {}\n\n    parameters = FunctionAdapter._process_parameters(\n        func,\n        sig,\n        type_hints,\n        param_descriptions,\n        allow_null,\n        allow_unknown,\n    )\n\n    return_hint = type_hints.get(\"return\", typing.Any)\n    return_tf_type = FunctionAdapter._infer_cty_type_for_hint(return_hint)\n\n    function_return = FunctionReturnType(type=return_tf_type)\n\n    @define()\n    class AdaptedFunction(BaseFunction):\n        \"\"\"\n        Dynamically created BaseFunction wrapper for a Python callable.\n        \"\"\"\n\n        def get_parameters(self) -&gt; list[FunctionParameter]:\n            return parameters\n\n        def get_return_type(self) -&gt; FunctionReturnType:\n            return function_return\n\n        async def call(self, *args: Any, **kwargs: Any) -&gt; Any:\n            func_name_call = self.name\n            logger.debug(f\"\ud83e\uddf0\ud83d\udcdd\ud83d\ude80 Calling adapted function '{func_name_call}'...\")\n            try:\n                # THE FIX: Restore logic to handle both sync and async functions.\n                if asyncio.iscoroutinefunction(func):\n                    result = await func(*args, **kwargs)\n                else:\n                    result = func(*args, **kwargs)\n\n                logger.debug(f\"\ud83e\uddf0\ud83d\udcdd\u2705 Adapted function '{func_name_call}' successful.\")\n                return result\n            except FunctionError:\n                raise\n            except Exception as e:\n                logger.error(\n                    f\"\ud83e\uddf0\ud83d\udcdd\u274c Error executing adapted function '{func_name_call}': {e}\",\n                    exc_info=True,\n                )\n                raise FunctionError(f\"Function '{func_name_call}' execution failed: {e}\") from e\n\n    logger.debug(f\"\ud83e\uddf0\ud83d\udcdd\u2705 Adaptation complete for '{func_display_name}'.\")\n    return AdaptedFunction(\n        name=func_display_name,\n        summary=final_summary,\n        description=final_description,\n        deprecation_message=deprecation_message,\n    )\n</code></pre>"},{"location":"api/functions/#pyvider.functions.FunctionParameter","title":"FunctionParameter","text":"<p>Definition of a Terraform function parameter using attrs.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Parameter name in Terraform calls. Must be a valid identifier.</p> <code>type</code> <code>CtyType[Any]</code> <p>CTY type constraint for the parameter (instance of CtyType).</p> <code>description</code> <code>str</code> <p>Human-readable description (Markdown supported).</p> <code>allow_null</code> <code>bool</code> <p>If true, the function accepts a null value for this parameter.</p> <code>allow_unknown</code> <code>bool</code> <p>If true, the function accepts an unknown value for this parameter.</p>"},{"location":"api/functions/#pyvider.functions.FunctionReturnType","title":"FunctionReturnType","text":"<p>Definition of a Terraform function's return type using attrs.</p> <p>Attributes:</p> Name Type Description <code>type</code> <code>CtyType[Any]</code> <p>The CTY type constraint for the function's return value.</p>"},{"location":"api/functions/#pyvider.functions.register_function","title":"register_function","text":"<pre><code>register_function(\n    name: str,\n    component_of: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    deprecation_message: str = \"\",\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]\n</code></pre> <p>Decorator to register a function and associate it with a capability.</p> Source code in <code>src/pyvider/functions/decorators.py</code> <pre><code>def register_function(\n    name: str,\n    component_of: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    deprecation_message: str = \"\",\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]:\n    \"\"\"\n    Decorator to register a function and associate it with a capability.\n    \"\"\"\n\n    def decorator(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n        func._is_registered_function = True  # type: ignore\n        func._registered_name = name  # type: ignore\n        if component_of:\n            func._parent_capability = component_of  # type: ignore\n\n        metadata = {\n            \"name\": name,\n            \"type\": \"function\",\n            \"summary\": summary,\n            \"description\": description,\n            \"param_descriptions\": param_descriptions or {},\n            \"deprecation_message\": deprecation_message,\n            \"function_name\": func.__name__,\n            \"module\": func.__module__,\n            \"discovery_method\": \"decorator\",\n        }\n        func._function_metadata = metadata  # type: ignore\n\n        logger.debug(f\"\ud83e\uddf0 Marked function '{name}' for discovery\", capability=component_of)\n        return func\n\n    return decorator\n</code></pre>"},{"location":"api/hub/","title":"Hub","text":""},{"location":"api/hub/#pyvider.hub","title":"pyvider.hub","text":""},{"location":"api/hub/#pyvider.hub--pyvider-component-hub","title":"Pyvider Component Hub","text":"<p>This package provides the central registry and discovery mechanisms for all provider components (resources, data sources, functions).</p>"},{"location":"api/hub/#pyvider.hub.ComponentDiscovery","title":"ComponentDiscovery","text":"<pre><code>ComponentDiscovery(hub: ComponentRegistry)\n</code></pre> <p>Discovers and registers components by scanning for installed packages that declare the 'pyvider.components' entry point.</p> Source code in <code>src/pyvider/hub/discovery.py</code> <pre><code>def __init__(self, hub: ComponentRegistry) -&gt; None:\n    self.hub = hub\n    self._discovered_modules: set[str] = set()\n    self.import_errors: list[tuple[str, Exception]] = []\n</code></pre>"},{"location":"api/hub/#pyvider.hub.ComponentDiscovery.discover_all","title":"discover_all  <code>async</code>","text":"<pre><code>discover_all(strict: bool = False) -&gt; None\n</code></pre> <p>Discovers all components. In strict mode, it re-raises import errors. Enhanced with error handling and retry logic.</p> Source code in <code>src/pyvider/hub/discovery.py</code> <pre><code>@resilient()\n@retry(max_attempts=2, base_delay=1.0)\nasync def discover_all(self, strict: bool = False) -&gt; None:\n    \"\"\"\n    Discovers all components. In strict mode, it re-raises import errors.\n    Enhanced with error handling and retry logic.\n    \"\"\"\n    self.import_errors = []\n    logger.debug(\"\ud83d\udef0\ufe0f\ud83d\udd0d\ud83d\udd04 Starting component discovery\", group=self.ENTRY_POINT_GROUP)\n\n    try:\n        entry_points = importlib.metadata.entry_points(group=self.ENTRY_POINT_GROUP)\n    except Exception as e:\n        logger.error(\"\ud83d\udef0\ufe0f\ud83d\udd0d\u274c Failed to query for entry points\", error=e, exc_info=True)\n        return\n\n    if not entry_points:\n        logger.info(\" i No packages found declaring the 'pyvider.components' entry point.\")\n        logger.info(\" i Manually discovering built-in component packages.\")\n        await self._discover_package(\"pyvider.components\", strict=strict)\n        await self._discover_package(\"pyvider.providers.capabilities\", strict=strict)\n    else:\n        for entry_point in entry_points:\n            logger.debug(\n                \"\ud83d\udef0\ufe0f\ud83d\udd0d\ud83d\udce6 Found component package entry point\",\n                name=entry_point.name,\n                module=entry_point.value,\n            )\n            await self._discover_package(entry_point.value, strict=strict)\n\n    component_counts = {k: len(v) for k, v in self.hub.list_components().items()}\n    logger.info(\"\ud83d\udef0\ufe0f\ud83d\udd0d\u2705 Component discovery complete\", components=component_counts)\n</code></pre>"},{"location":"api/hub/#pyvider.hub.Validators","title":"Validators","text":"<p>Manages global registration and application of validators.</p>"},{"location":"api/hub/#pyvider.hub.Validators.register","title":"register  <code>classmethod</code>","text":"<pre><code>register(name: str) -&gt; Callable[..., Any]\n</code></pre> <p>Decorator to register a validator with a specific name.</p> Source code in <code>src/pyvider/hub/validators.py</code> <pre><code>@classmethod\ndef register(cls, name: str) -&gt; Callable[..., Any]:\n    \"\"\"Decorator to register a validator with a specific name.\"\"\"\n\n    def decorator(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n        cls._registry[name] = func\n        logger.debug(f\"Validator '{name}' registered.\")\n        return func\n\n    return decorator\n</code></pre>"},{"location":"api/hub/#pyvider.hub.Validators.attach","title":"attach  <code>classmethod</code>","text":"<pre><code>attach(metadata: Any, *validator_names: str) -&gt; None\n</code></pre> <p>Attach validators to AttributeMetadata by name.</p> Source code in <code>src/pyvider/hub/validators.py</code> <pre><code>@classmethod\n@resilient()\ndef attach(cls, metadata: Any, *validator_names: str) -&gt; None:\n    \"\"\"Attach validators to AttributeMetadata by name.\"\"\"\n    for name in validator_names:\n        if name not in cls._registry:\n            raise ConfigValidationError(f\"Validator '{name}' not registered.\")\n\n        validator = cls._registry[name]\n\n        if not hasattr(metadata, \"validators\") or not isinstance(metadata.validators, list):\n            logger.error(\n                f\"Cannot attach validator: 'metadata' object for '{getattr(metadata, 'description', 'unknown')}' lacks a list 'validators' attribute.\"\n            )\n            continue\n\n        metadata.validators.append(validator)\n        logger.debug(\n            f\"Validator '{name}' attached to '{getattr(metadata, 'description', 'unknown attribute')}'.\"\n        )\n</code></pre>"},{"location":"api/hub/#pyvider.hub.Validators.validate","title":"validate  <code>classmethod</code>","text":"<pre><code>validate(\n    validator_name: str, value: Any, metadata: Any\n) -&gt; None\n</code></pre> <p>Apply a specific validator at runtime.</p> Source code in <code>src/pyvider/hub/validators.py</code> <pre><code>@classmethod\n@resilient()\ndef validate(cls, validator_name: str, value: Any, metadata: Any) -&gt; None:\n    \"\"\"Apply a specific validator at runtime.\"\"\"\n    if validator_name not in cls._registry:\n        raise ConfigValidationError(f\"Validator '{validator_name}' not registered.\")\n\n    try:\n        cls._registry[validator_name](value, metadata)\n    except Exception as e:\n        raise ConfigValidationError(f\"Validation failed for '{validator_name}': {e!s}\") from e\n</code></pre>"},{"location":"api/hub/#pyvider.hub.register_data_source","title":"register_data_source","text":"<pre><code>register_data_source(\n    name: str, component_of: str | None = None\n) -&gt; Callable[[type], type]\n</code></pre> <p>Decorator to register a data source and associate it with a capability.</p> Source code in <code>src/pyvider/data_sources/decorators.py</code> <pre><code>def register_data_source(name: str, component_of: str | None = None) -&gt; Callable[[type], type]:\n    \"\"\"\n    Decorator to register a data source and associate it with a capability.\n    \"\"\"\n\n    def decorator(cls: type) -&gt; type:\n        cls._is_registered_data_source = True  # type: ignore\n        cls._registered_name = name  # type: ignore\n        if component_of:\n            cls._parent_capability = component_of  # type: ignore\n        logger.debug(f\"\ud83d\udcca Marked data source '{name}' for discovery\", capability=component_of)\n        return cls\n\n    return decorator\n</code></pre>"},{"location":"api/hub/#pyvider.hub.register_function","title":"register_function","text":"<pre><code>register_function(\n    name: str,\n    component_of: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    deprecation_message: str = \"\",\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]\n</code></pre> <p>Decorator to register a function and associate it with a capability.</p> Source code in <code>src/pyvider/functions/decorators.py</code> <pre><code>def register_function(\n    name: str,\n    component_of: str | None = None,\n    summary: str = \"\",\n    description: str = \"\",\n    param_descriptions: dict[str, str] | None = None,\n    deprecation_message: str = \"\",\n) -&gt; Callable[[Callable[..., Any]], Callable[..., Any]]:\n    \"\"\"\n    Decorator to register a function and associate it with a capability.\n    \"\"\"\n\n    def decorator(func: Callable[..., Any]) -&gt; Callable[..., Any]:\n        func._is_registered_function = True  # type: ignore\n        func._registered_name = name  # type: ignore\n        if component_of:\n            func._parent_capability = component_of  # type: ignore\n\n        metadata = {\n            \"name\": name,\n            \"type\": \"function\",\n            \"summary\": summary,\n            \"description\": description,\n            \"param_descriptions\": param_descriptions or {},\n            \"deprecation_message\": deprecation_message,\n            \"function_name\": func.__name__,\n            \"module\": func.__module__,\n            \"discovery_method\": \"decorator\",\n        }\n        func._function_metadata = metadata  # type: ignore\n\n        logger.debug(f\"\ud83e\uddf0 Marked function '{name}' for discovery\", capability=component_of)\n        return func\n\n    return decorator\n</code></pre>"},{"location":"api/hub/#pyvider.hub.register_resource","title":"register_resource","text":"<pre><code>register_resource(\n    name: str, component_of: str | None = None\n) -&gt; Callable[[type], type]\n</code></pre> <p>Decorator to register a resource and associate it with a capability.</p> Source code in <code>src/pyvider/resources/decorators.py</code> <pre><code>def register_resource(name: str, component_of: str | None = None) -&gt; Callable[[type], type]:\n    \"\"\"\n    Decorator to register a resource and associate it with a capability.\n    \"\"\"\n\n    def decorator(cls: type) -&gt; type:\n        cls._is_registered_resource = True  # type: ignore[attr-defined]\n        cls._registered_name = name  # type: ignore[attr-defined]\n        if component_of:\n            cls._parent_capability = component_of  # type: ignore\n        logger.debug(f\"\ud83d\udd27 Marked resource '{name}' for discovery\", capability=component_of)\n        return cls\n\n    return decorator\n</code></pre>"},{"location":"api/resources/","title":"Resources","text":""},{"location":"api/resources/#pyvider.resources","title":"pyvider.resources","text":""},{"location":"api/resources/#pyvider.resources.ResourceContext","title":"ResourceContext","text":"<p>               Bases: <code>BaseContext</code>, <code>Generic[ConfigType, StateType, PrivateStateType]</code></p>"},{"location":"api/resources/#pyvider.resources.ResourceContext.get_private_state","title":"get_private_state","text":"<pre><code>get_private_state(\n    private_state_class: type[PrivateStateType],\n) -&gt; PrivateStateType | None\n</code></pre> <p>Get typed private state with automatic casting.</p> <p>Parameters:</p> Name Type Description Default <code>private_state_class</code> <code>type[PrivateStateType]</code> <p>The private state class type to cast to</p> required <p>Returns:</p> Type Description <code>PrivateStateType | None</code> <p>Typed private state instance or None if no private state exists</p> Example <p>private_data = ctx.get_private_state(MyPrivateState) if private_data:     token = private_data.token</p> Source code in <code>src/pyvider/resources/context.py</code> <pre><code>def get_private_state(self, private_state_class: type[PrivateStateType]) -&gt; PrivateStateType | None:\n    \"\"\"\n    Get typed private state with automatic casting.\n\n    Args:\n        private_state_class: The private state class type to cast to\n\n    Returns:\n        Typed private state instance or None if no private state exists\n\n    Example:\n        private_data = ctx.get_private_state(MyPrivateState)\n        if private_data:\n            token = private_data.token\n    \"\"\"\n    if self.private_state:\n        # If it's already the correct type, return as-is\n        if isinstance(self.private_state, private_state_class):\n            return self.private_state\n        # Otherwise, convert from dict representation\n        if hasattr(self.private_state, \"__dict__\") or isinstance(self.private_state, dict):\n            state_dict = (\n                attrs.asdict(self.private_state)\n                if hasattr(self.private_state, \"__dict__\")\n                else self.private_state\n            )\n            return private_state_class(**state_dict)\n    return None\n</code></pre>"},{"location":"api/resources/#pyvider.resources.ResourceContext.has_private_state","title":"has_private_state","text":"<pre><code>has_private_state() -&gt; bool\n</code></pre> <p>Check if private state exists.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if private state is present, False otherwise</p> Source code in <code>src/pyvider/resources/context.py</code> <pre><code>def has_private_state(self) -&gt; bool:\n    \"\"\"\n    Check if private state exists.\n\n    Returns:\n        True if private state is present, False otherwise\n    \"\"\"\n    return self.private_state is not None\n</code></pre>"},{"location":"api/resources/#pyvider.resources.ResourceContext.is_field_unknown","title":"is_field_unknown","text":"<pre><code>is_field_unknown(\n    field_name: str, source: str = \"config\"\n) -&gt; bool\n</code></pre> <p>Check if a configuration or state field has an unknown value during planning.</p> <p>This is the proper way for resources to handle unknown values - check explicitly rather than catching errors or working around None values.</p> <p>Parameters:</p> Name Type Description Default <code>field_name</code> <code>str</code> <p>Name of the field to check</p> required <code>source</code> <code>str</code> <p>Which CTY value to check - \"config\" or \"planned_state\" (default: \"config\")</p> <code>'config'</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the field exists but has an unknown value, False otherwise</p> Example <p>async def _create(self, ctx: ResourceContext, base_plan: dict) -&gt; ...:     if ctx.is_field_unknown(\"content\"):         # Content is unknown during planning, can't compute hash         base_plan[\"exists\"] = True         return base_plan, None</p> <pre><code># Content is known, use typed config\nconfig = cast(FileContentConfig, ctx.config)\nbase_plan[\"content_hash\"] = hashlib.sha256(config.content.encode()).hexdigest()\nreturn base_plan, None\n</code></pre> Source code in <code>src/pyvider/resources/context.py</code> <pre><code>def is_field_unknown(self, field_name: str, source: str = \"config\") -&gt; bool:\n    \"\"\"\n    Check if a configuration or state field has an unknown value during planning.\n\n    This is the proper way for resources to handle unknown values - check explicitly\n    rather than catching errors or working around None values.\n\n    Args:\n        field_name: Name of the field to check\n        source: Which CTY value to check - \"config\" or \"planned_state\" (default: \"config\")\n\n    Returns:\n        True if the field exists but has an unknown value, False otherwise\n\n    Example:\n        async def _create(self, ctx: ResourceContext, base_plan: dict) -&gt; ...:\n            if ctx.is_field_unknown(\"content\"):\n                # Content is unknown during planning, can't compute hash\n                base_plan[\"exists\"] = True\n                return base_plan, None\n\n            # Content is known, use typed config\n            config = cast(FileContentConfig, ctx.config)\n            base_plan[\"content_hash\"] = hashlib.sha256(config.content.encode()).hexdigest()\n            return base_plan, None\n    \"\"\"\n    cty_value = self.config_cty if source == \"config\" else self.planned_state_cty\n\n    if not cty_value or cty_value.is_null:\n        return False\n\n    if not hasattr(cty_value, \"value\") or not isinstance(cty_value.value, dict):\n        return False\n\n    field_cty = cty_value.value.get(field_name)\n    if field_cty is None:\n        return False\n\n    # Check if it's a CtyValue with unknown marker\n    if isinstance(field_cty, CtyValue):\n        return field_cty.is_unknown\n\n    return False\n</code></pre>"},{"location":"api/resources/#pyvider.resources.PrivateState","title":"PrivateState","text":"<p>A base marker class for private state data structures. Resource-specific private state classes can inherit from this for clarity and type-hinting purposes.</p>"},{"location":"api/resources/#pyvider.resources.register_resource","title":"register_resource","text":"<pre><code>register_resource(\n    name: str, component_of: str | None = None\n) -&gt; Callable[[type], type]\n</code></pre> <p>Decorator to register a resource and associate it with a capability.</p> Source code in <code>src/pyvider/resources/decorators.py</code> <pre><code>def register_resource(name: str, component_of: str | None = None) -&gt; Callable[[type], type]:\n    \"\"\"\n    Decorator to register a resource and associate it with a capability.\n    \"\"\"\n\n    def decorator(cls: type) -&gt; type:\n        cls._is_registered_resource = True  # type: ignore[attr-defined]\n        cls._registered_name = name  # type: ignore[attr-defined]\n        if component_of:\n            cls._parent_capability = component_of  # type: ignore\n        logger.debug(f\"\ud83d\udd27 Marked resource '{name}' for discovery\", capability=component_of)\n        return cls\n\n    return decorator\n</code></pre>"},{"location":"api/conversion/","title":"Conversion","text":""},{"location":"api/conversion/#pyvider.conversion","title":"pyvider.conversion","text":"<p>The pyvider.conversion package provides the primary bridge between the wire protocol (via DynamicValue) and the framework's internal CtyValue representation.</p>"},{"location":"api/conversion/#pyvider.conversion.marshal","title":"marshal","text":"<pre><code>marshal(\n    value: CtyValue | Any, *, schema: PvsType | CtyType\n) -&gt; pb.DynamicValue\n</code></pre> <p>Marshals a Python or CtyValue into a protobuf DynamicValue.</p> Source code in <code>src/pyvider/conversion/marshaler.py</code> <pre><code>def marshal(value: CtyValue | Any, *, schema: PvsType | CtyType) -&gt; pb.DynamicValue:\n    \"\"\"Marshals a Python or CtyValue into a protobuf DynamicValue.\"\"\"\n    if not isinstance(schema, CtyType | PvsType):\n        raise TypeError(f\"Schema must be a CtyType or PvsType, but got {type(schema).__name__}\")\n\n    schema_cty_type = schema.to_cty_type() if hasattr(schema, \"to_cty_type\") else schema\n\n    if isinstance(value, CtyValue):\n        validated_value = value\n    else:\n        raw_value = attrs.asdict(value) if attrs.has(type(value)) else value\n        validated_value = schema_cty_type.validate(raw_value)\n\n    final_cty_value = _apply_schema_marks_iterative(validated_value, schema)\n\n    msgpack_data = cty_to_msgpack(final_cty_value, schema_cty_type)\n    return pb.DynamicValue(msgpack=msgpack_data)\n</code></pre>"},{"location":"api/conversion/#pyvider.conversion.unmarshal","title":"unmarshal","text":"<pre><code>unmarshal(\n    dv: DynamicValue, *, schema: PvsType | CtyType\n) -&gt; CtyValue\n</code></pre> <p>Unmarshals a DynamicValue from the wire protocol into a CtyValue.</p> Source code in <code>src/pyvider/conversion/marshaler.py</code> <pre><code>def unmarshal(dv: pb.DynamicValue, *, schema: PvsType | CtyType) -&gt; CtyValue:\n    \"\"\"Unmarshals a DynamicValue from the wire protocol into a CtyValue.\"\"\"\n    if not isinstance(schema, CtyType | PvsType):\n        raise TypeError(f\"Schema must be a CtyType or PvsType, but got {type(schema).__name__}\")\n\n    root_cty_type = schema.to_cty_type() if hasattr(schema, \"to_cty_type\") else schema\n\n    if dv.msgpack:\n        return cty_from_msgpack(dv.msgpack, root_cty_type)\n\n    if dv.json:\n        raise NotImplementedError(\"JSON unmarshalling is not yet implemented.\")\n\n    return CtyValue.null(root_cty_type)\n</code></pre>"},{"location":"api/conversion/#pyvider.conversion.pvs_schema_to_proto","title":"pvs_schema_to_proto  <code>async</code>","text":"<pre><code>pvs_schema_to_proto(schema: PvsSchema) -&gt; pb.Schema\n</code></pre> <p>Converts a PvsSchema into a protobuf Schema message.</p> Source code in <code>src/pyvider/conversion/schema_adapter.py</code> <pre><code>async def pvs_schema_to_proto(schema: PvsSchema) -&gt; pb.Schema:\n    \"\"\"Converts a PvsSchema into a protobuf Schema message.\"\"\"\n    proto_block = _pvs_object_type_to_proto(schema.block)\n    return pb.Schema(version=schema.version, block=proto_block)\n</code></pre>"},{"location":"api/conversion/#pyvider.conversion.unify_and_validate_list_of_objects","title":"unify_and_validate_list_of_objects","text":"<pre><code>unify_and_validate_list_of_objects(\n    dict_list: list[dict[str, Any]],\n) -&gt; CtyValue\n</code></pre> <p>Analyzes a list of dictionaries, infers a unified CtyObject schema, and returns a validated CtyValue representing a CtyList(CtyObject).</p> Source code in <code>src/pyvider/conversion/utils.py</code> <pre><code>def unify_and_validate_list_of_objects(dict_list: list[dict[str, Any]]) -&gt; CtyValue:\n    \"\"\"\n    Analyzes a list of dictionaries, infers a unified CtyObject schema,\n    and returns a validated CtyValue representing a CtyList(CtyObject).\n    \"\"\"\n    if not dict_list:\n        return CtyList(element_type=CtyDynamic()).validate([])\n\n    all_keys: set[str] = set()\n    attribute_types: dict[str, CtyType] = {}\n\n    for item in dict_list:\n        all_keys.update(item.keys())\n        for key, value in item.items():\n            inferred_type = infer_cty_type_from_raw(value)\n            if key not in attribute_types:\n                attribute_types[key] = inferred_type\n            elif not attribute_types[key].equal(inferred_type):\n                attribute_types[key] = CtyDynamic()\n\n    optional_keys = {key for key in all_keys if not all(key in item for item in dict_list)}\n\n    unified_object_type = CtyObject(\n        attribute_types=attribute_types, optional_attributes=frozenset(optional_keys)\n    )\n\n    final_list_type = CtyList(element_type=unified_object_type)\n\n    return final_list_type.validate(dict_list)\n</code></pre>"},{"location":"api/protocols/","title":"Protocols","text":""},{"location":"api/protocols/#pyvider.protocols","title":"pyvider.protocols","text":""},{"location":"api/providers/","title":"Providers","text":""},{"location":"api/providers/#pyvider.providers","title":"pyvider.providers","text":""},{"location":"api/providers/#pyvider.providers.BaseProvider","title":"BaseProvider","text":"<p>Base provider implementation that handles gRPC service initialization and provider lifecycle management.</p>"},{"location":"api/providers/#pyvider.providers.BaseProvider.schema","title":"schema  <code>property</code>","text":"<pre><code>schema: PvsSchema\n</code></pre> <p>Get the provider schema.</p>"},{"location":"api/providers/#pyvider.providers.BaseProvider.setup","title":"setup  <code>async</code>","text":"<pre><code>setup() -&gt; None\n</code></pre> <p>An initialization hook called by the framework after component discovery but before serving requests. This is the ideal place to assemble the final schema by integrating capabilities.</p> Source code in <code>src/pyvider/providers/base.py</code> <pre><code>async def setup(self) -&gt; None:\n    \"\"\"\n    An initialization hook called by the framework after component\n    discovery but before serving requests. This is the ideal place\n    to assemble the final schema by integrating capabilities.\n    \"\"\"\n    pass  # pragma: no cover\n</code></pre>"},{"location":"api/providers/#pyvider.providers.BaseProvider.configure","title":"configure  <code>async</code>","text":"<pre><code>configure(config: dict[str, CtyType]) -&gt; None\n</code></pre> <p>Configure the provider with the given configuration.</p> Source code in <code>src/pyvider/providers/base.py</code> <pre><code>async def configure(self, config: dict[str, CtyType]) -&gt; None:\n    \"\"\"Configure the provider with the given configuration.\"\"\"\n    async with asyncio.Lock():\n        if self._configured:\n            raise ProviderError(\"Provider already configured\")\n\n        logger.info(\"Provider configured with data (logic to be implemented).\")\n        self._configured = True\n</code></pre>"},{"location":"api/providers/#pyvider.providers.ProviderContext","title":"ProviderContext","text":"<p>               Bases: <code>BaseContext</code></p> <p>Holds the configured state of the provider. Inherits diagnostic reporting capabilities from BaseContext.</p>"},{"location":"api/providers/#pyvider.providers.register_provider","title":"register_provider","text":"<pre><code>register_provider(name: str) -&gt; Callable[[type], type]\n</code></pre> <p>Decorator to register a provider with a specific name.</p> Source code in <code>src/pyvider/providers/decorators.py</code> <pre><code>def register_provider(name: str) -&gt; Callable[[type], type]:\n    \"\"\"Decorator to register a provider with a specific name.\"\"\"\n\n    def decorator(cls: type) -&gt; type:\n        # Attach metadata to the class for discovery purposes\n        cls._is_registered_provider = True  # type: ignore\n        cls._registered_name = name  # type: ignore\n\n        # Register the provider class immediately\n        hub.register(\"provider\", name, cls)\n        logger.debug(f\"Registered provider '{name}' via decorator.\")\n        return cls\n\n    return decorator\n</code></pre>"},{"location":"api/providers/capabilities/","title":"Provider Capabilities","text":""},{"location":"api/providers/capabilities/#pyvider.providers.capabilities","title":"pyvider.providers.capabilities","text":""},{"location":"api/schema/","title":"Schema","text":""},{"location":"api/schema/#pyvider.schema","title":"pyvider.schema","text":""},{"location":"api/schema/#pyvider.schema.NestingMode","title":"NestingMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Defines how a nested block is represented in the configuration.</p>"},{"location":"api/schema/#pyvider.schema.PvsAttribute","title":"PvsAttribute","text":"<p>Represents a fully resolved schema attribute, holding a CtyType.</p>"},{"location":"api/schema/#pyvider.schema.PvsNestedBlock","title":"PvsNestedBlock","text":"<p>Defines a nested block type within a schema.</p>"},{"location":"api/schema/#pyvider.schema.PvsObjectType","title":"PvsObjectType","text":"<p>               Bases: <code>PvsType</code></p> <p>A schema object that holds full attribute definitions. It no longer inherits from CtyObject, but can produce one.</p>"},{"location":"api/schema/#pyvider.schema.PvsObjectType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyObject\n</code></pre> <p>Converts this schema definition into its equivalent CtyObject type for validation and data manipulation. This now correctly includes attributes derived from nested blocks.</p> Source code in <code>src/pyvider/schema/types/object.py</code> <pre><code>def to_cty_type(self) -&gt; CtyObject:\n    \"\"\"\n    Converts this schema definition into its equivalent CtyObject type\n    for validation and data manipulation. This now correctly includes\n    attributes derived from nested blocks.\n    \"\"\"\n    attribute_types = {name: attr.type for name, attr in self.attributes.items()}\n    optional_attributes = {\n        name for name, attr in self.attributes.items() if attr.optional or attr.computed\n    }\n\n    # FIX: Add types for nested blocks so the CtyObject is complete.\n    for block in self.block_types:\n        block_cty_type = block.block.to_cty_type()\n        if block.nesting == NestingMode.LIST:\n            attribute_types[block.type_name] = CtyList(element_type=block_cty_type)\n        elif block.nesting == NestingMode.SET:\n            attribute_types[block.type_name] = CtySet(element_type=block_cty_type)\n        elif block.nesting == NestingMode.MAP:\n            attribute_types[block.type_name] = CtyMap(element_type=block_cty_type)\n        else:  # SINGLE or GROUP\n            attribute_types[block.type_name] = block_cty_type\n\n        optional_attributes.add(block.type_name)\n\n    return CtyObject(\n        attribute_types=attribute_types,\n        optional_attributes=frozenset(optional_attributes),\n    )\n</code></pre>"},{"location":"api/schema/#pyvider.schema.PvsSchema","title":"PvsSchema","text":"<p>Represents a complete schema definition for a provider, resource, or data source. This class is the root of a schema tree.</p> <p>Attributes:</p> Name Type Description <code>version</code> <code>int</code> <p>An integer representing the schema version, used for state upgrades.</p> <code>block</code> <code>PvsObjectType</code> <p>The root block of the schema, defining its attributes and nested blocks.</p>"},{"location":"api/schema/#pyvider.schema.PvsSchema.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: Any) -&gt; None\n</code></pre> <p>Validates a configuration against this schema by converting the schema to its CtyType representation and invoking its validation logic.</p> <p>This method raises CtyValidationError on failure, which is the expected contract for direct validation. Higher-level handlers are responsible for catching this exception and creating diagnostics.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def validate_config(self, config: Any) -&gt; None:\n    \"\"\"\n    Validates a configuration against this schema by converting the schema\n    to its CtyType representation and invoking its validation logic.\n\n    This method raises CtyValidationError on failure, which is the\n    expected contract for direct validation. Higher-level handlers are\n    responsible for catching this exception and creating diagnostics.\n    \"\"\"\n    logger.debug(\"Validating configuration against schema\", schema_version=self.version)\n    if not isinstance(config, dict):\n        raise CtyValidationError(f\"Configuration must be a dictionary, but got {type(config).__name__}.\")\n\n    # Convert the schema's block to its CtyType representation to get the validator.\n    validator = self.block.to_cty_type()\n\n    # The CtyType's validate method will raise CtyValidationError on failure.\n    validator.validate(config)\n</code></pre>"},{"location":"api/schema/#pyvider.schema.PvsSchema.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; PvsObjectType\n</code></pre> <p>Returns the CtyType representation of the schema's root block. Since the block is now a PvsObjectType, this is a direct return.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def to_cty_type(self) -&gt; PvsObjectType:\n    \"\"\"\n    Returns the CtyType representation of the schema's root block.\n    Since the block is now a PvsObjectType, this is a direct return.\n    \"\"\"\n    return self.block\n</code></pre>"},{"location":"api/schema/#pyvider.schema.PvsType","title":"PvsType","text":"<p>               Bases: <code>Protocol</code></p> <p>A Protocol that all Pyvider Schema type definition classes must implement. It ensures that any schema type object can be converted into its corresponding CtyType, which is essential for the conversion and marshalling layers of the framework.</p>"},{"location":"api/schema/#pyvider.schema.PvsType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyType\n</code></pre> <p>Converts the Pyvider Schema type to its equivalent CtyType.</p> Source code in <code>src/pyvider/schema/types/types_base.py</code> <pre><code>def to_cty_type(self) -&gt; CtyType:\n    \"\"\"Converts the Pyvider Schema type to its equivalent CtyType.\"\"\"\n    ...\n</code></pre>"},{"location":"api/schema/#pyvider.schema.a_null","title":"a_null","text":"<pre><code>a_null(\n    schema_builder: PvsAttribute | PvsSchema,\n) -&gt; CtyValue\n</code></pre> <p>Creates a null CtyValue for a given schema attribute or object.</p> Source code in <code>src/pyvider/schema/factory.py</code> <pre><code>def a_null(schema_builder: PvsAttribute | PvsSchema) -&gt; CtyValue:\n    \"\"\"Creates a null CtyValue for a given schema attribute or object.\"\"\"\n    target_type: CtyType | None = None\n    if isinstance(schema_builder, PvsAttribute):\n        target_type = schema_builder.type\n    elif isinstance(schema_builder, PvsSchema):\n        target_type = schema_builder.block.to_cty_type()\n\n    if target_type is None:\n        raise TypeError(\"a_null() expects a schema builder instance like a_str() or s_resource()\")\n    return CtyValue.null(target_type)\n</code></pre>"},{"location":"api/schema/#pyvider.schema.a_unknown","title":"a_unknown","text":"<pre><code>a_unknown(\n    schema_builder: PvsAttribute | PvsSchema,\n) -&gt; CtyValue\n</code></pre> <p>Creates an unknown CtyValue for a given schema attribute or object.</p> Source code in <code>src/pyvider/schema/factory.py</code> <pre><code>def a_unknown(schema_builder: PvsAttribute | PvsSchema) -&gt; CtyValue:\n    \"\"\"Creates an unknown CtyValue for a given schema attribute or object.\"\"\"\n    target_type: CtyType | None = None\n    if isinstance(schema_builder, PvsAttribute):\n        target_type = schema_builder.type\n    elif isinstance(schema_builder, PvsSchema):\n        target_type = schema_builder.block.to_cty_type()\n\n    if target_type is None:\n        raise TypeError(\"a_unknown() expects a schema builder instance like a_str() or s_resource()\")\n    return CtyValue.unknown(target_type)\n</code></pre>"},{"location":"api/schema/types/","title":"Schema Types","text":""},{"location":"api/schema/types/#pyvider.schema.types","title":"pyvider.schema.types","text":""},{"location":"api/schema/types/#pyvider.schema.types.PvsAttribute","title":"PvsAttribute","text":"<p>Represents a fully resolved schema attribute, holding a CtyType.</p>"},{"location":"api/schema/types/#pyvider.schema.types.PvsNestedBlock","title":"PvsNestedBlock","text":"<p>Defines a nested block type within a schema.</p>"},{"location":"api/schema/types/#pyvider.schema.types.NestingMode","title":"NestingMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Defines how a nested block is represented in the configuration.</p>"},{"location":"api/schema/types/#pyvider.schema.types.StringKind","title":"StringKind","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Defines the interpretation of a string (e.g., for descriptions).</p>"},{"location":"api/schema/types/#pyvider.schema.types.PvsObjectType","title":"PvsObjectType","text":"<p>               Bases: <code>PvsType</code></p> <p>A schema object that holds full attribute definitions. It no longer inherits from CtyObject, but can produce one.</p>"},{"location":"api/schema/types/#pyvider.schema.types.PvsObjectType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyObject\n</code></pre> <p>Converts this schema definition into its equivalent CtyObject type for validation and data manipulation. This now correctly includes attributes derived from nested blocks.</p> Source code in <code>src/pyvider/schema/types/object.py</code> <pre><code>def to_cty_type(self) -&gt; CtyObject:\n    \"\"\"\n    Converts this schema definition into its equivalent CtyObject type\n    for validation and data manipulation. This now correctly includes\n    attributes derived from nested blocks.\n    \"\"\"\n    attribute_types = {name: attr.type for name, attr in self.attributes.items()}\n    optional_attributes = {\n        name for name, attr in self.attributes.items() if attr.optional or attr.computed\n    }\n\n    # FIX: Add types for nested blocks so the CtyObject is complete.\n    for block in self.block_types:\n        block_cty_type = block.block.to_cty_type()\n        if block.nesting == NestingMode.LIST:\n            attribute_types[block.type_name] = CtyList(element_type=block_cty_type)\n        elif block.nesting == NestingMode.SET:\n            attribute_types[block.type_name] = CtySet(element_type=block_cty_type)\n        elif block.nesting == NestingMode.MAP:\n            attribute_types[block.type_name] = CtyMap(element_type=block_cty_type)\n        else:  # SINGLE or GROUP\n            attribute_types[block.type_name] = block_cty_type\n\n        optional_attributes.add(block.type_name)\n\n    return CtyObject(\n        attribute_types=attribute_types,\n        optional_attributes=frozenset(optional_attributes),\n    )\n</code></pre>"},{"location":"api/schema/types/#pyvider.schema.types.PvsSchema","title":"PvsSchema","text":"<p>Represents a complete schema definition for a provider, resource, or data source. This class is the root of a schema tree.</p> <p>Attributes:</p> Name Type Description <code>version</code> <code>int</code> <p>An integer representing the schema version, used for state upgrades.</p> <code>block</code> <code>PvsObjectType</code> <p>The root block of the schema, defining its attributes and nested blocks.</p>"},{"location":"api/schema/types/#pyvider.schema.types.PvsSchema.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: Any) -&gt; None\n</code></pre> <p>Validates a configuration against this schema by converting the schema to its CtyType representation and invoking its validation logic.</p> <p>This method raises CtyValidationError on failure, which is the expected contract for direct validation. Higher-level handlers are responsible for catching this exception and creating diagnostics.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def validate_config(self, config: Any) -&gt; None:\n    \"\"\"\n    Validates a configuration against this schema by converting the schema\n    to its CtyType representation and invoking its validation logic.\n\n    This method raises CtyValidationError on failure, which is the\n    expected contract for direct validation. Higher-level handlers are\n    responsible for catching this exception and creating diagnostics.\n    \"\"\"\n    logger.debug(\"Validating configuration against schema\", schema_version=self.version)\n    if not isinstance(config, dict):\n        raise CtyValidationError(f\"Configuration must be a dictionary, but got {type(config).__name__}.\")\n\n    # Convert the schema's block to its CtyType representation to get the validator.\n    validator = self.block.to_cty_type()\n\n    # The CtyType's validate method will raise CtyValidationError on failure.\n    validator.validate(config)\n</code></pre>"},{"location":"api/schema/types/#pyvider.schema.types.PvsSchema.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; PvsObjectType\n</code></pre> <p>Returns the CtyType representation of the schema's root block. Since the block is now a PvsObjectType, this is a direct return.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def to_cty_type(self) -&gt; PvsObjectType:\n    \"\"\"\n    Returns the CtyType representation of the schema's root block.\n    Since the block is now a PvsObjectType, this is a direct return.\n    \"\"\"\n    return self.block\n</code></pre>"},{"location":"api/schema/types/#pyvider.schema.types.PvsType","title":"PvsType","text":"<p>               Bases: <code>Protocol</code></p> <p>A Protocol that all Pyvider Schema type definition classes must implement. It ensures that any schema type object can be converted into its corresponding CtyType, which is essential for the conversion and marshalling layers of the framework.</p>"},{"location":"api/schema/types/#pyvider.schema.types.PvsType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyType\n</code></pre> <p>Converts the Pyvider Schema type to its equivalent CtyType.</p> Source code in <code>src/pyvider/schema/types/types_base.py</code> <pre><code>def to_cty_type(self) -&gt; CtyType:\n    \"\"\"Converts the Pyvider Schema type to its equivalent CtyType.\"\"\"\n    ...\n</code></pre>"},{"location":"api-reference/01-provider-api/","title":"Provider API","text":"<p>This guide provides a reference for the <code>pyvider</code> Provider API.</p>"},{"location":"api-reference/01-provider-api/#pyviderprovider","title":"<code>PyviderProvider</code>","text":"<p>The <code>PyviderProvider</code> class is the main entry point for a provider. It is responsible for configuring the provider and creating the resources, data sources, and functions.</p>"},{"location":"api-reference/01-provider-api/#providerhandler","title":"<code>ProviderHandler</code>","text":"<p>The <code>ProviderHandler</code> class is responsible for handling the gRPC requests from Terraform.</p>"},{"location":"api-reference/02-resource-api/","title":"Resource API","text":"<p>This guide provides a reference for the <code>pyvider</code> Resource API.</p>"},{"location":"api-reference/02-resource-api/#resource","title":"<code>Resource</code>","text":"<p>The <code>Resource</code> class is the base class for all resources. It provides the basic functionality for managing a resource, such as creating, reading, updating, and deleting the resource.</p>"},{"location":"api-reference/02-resource-api/#resourceconfig","title":"<code>ResourceConfig</code>","text":"<p>The <code>ResourceConfig</code> class is used to define the schema for a resource.</p>"},{"location":"api-reference/03-data-source-api/","title":"Data Source API","text":"<p>This guide provides a reference for the <code>pyvider</code> Data Source API.</p>"},{"location":"api-reference/03-data-source-api/#datasource","title":"<code>DataSource</code>","text":"<p>The <code>DataSource</code> class is the base class for all data sources. It provides the basic functionality for reading data from an external API or service.</p>"},{"location":"api-reference/03-data-source-api/#datasourceconfig","title":"<code>DataSourceConfig</code>","text":"<p>The <code>DataSourceConfig</code> class is used to define the schema for a data source.</p>"},{"location":"api-reference/04-function-api/","title":"Function API","text":"<p>This guide provides a reference for the <code>pyvider</code> Function API.</p>"},{"location":"api-reference/04-function-api/#function","title":"<code>Function</code>","text":"<p>The <code>Function</code> class is the base class for all functions. It provides the basic functionality for performing custom logic.</p>"},{"location":"api-reference/04-function-api/#functionconfig","title":"<code>FunctionConfig</code>","text":"<p>The <code>FunctionConfig</code> class is used to define the schema for a function.</p>"},{"location":"api-reference/05-hub-api/","title":"Hub API","text":"<p>This guide provides a reference for the <code>pyvider</code> Hub API.</p>"},{"location":"api-reference/05-hub-api/#hub","title":"<code>Hub</code>","text":"<p>The <code>Hub</code> class is the central registry for all the components in the framework.</p>"},{"location":"api-reference/05-hub-api/#componentregistry","title":"<code>ComponentRegistry</code>","text":"<p>The <code>ComponentRegistry</code> class is used to register and discover components.</p>"},{"location":"api-reference/cli/","title":"CLI","text":""},{"location":"api-reference/cli/#pyvider.cli","title":"pyvider.cli","text":""},{"location":"api-reference/cli/#pyvider.cli--pyvider-cli-package","title":"Pyvider CLI Package","text":"<p>This module assembles the main CLI application.</p>"},{"location":"api-reference/cli/#pyvider.cli.cli","title":"cli","text":"<pre><code>cli(ctx: Context, **kwargs: Any) -&gt; None\n</code></pre> <p>Pyvider CLI Tool.</p> <p>When run by Terraform (with no subcommands), this will automatically default to the 'provide' command.</p> Source code in <code>src/pyvider/cli/main.py</code> <pre><code>@click.group(invoke_without_command=True)\n@flexible_options  # Add logging and config options at root level\n@output_options  # Add output format options\n@click.pass_context\ndef cli(ctx: click.Context, **kwargs: Any) -&gt; None:\n    \"\"\"\n    Pyvider CLI Tool.\n\n    When run by Terraform (with no subcommands), this will automatically\n    default to the 'provide' command.\n    \"\"\"\n    # Ensure the custom context object is created and attached\n    # at the top level of the application. This makes it available to all\n    # subcommands via `ctx.obj`.\n    if ctx.obj is None:\n        ctx.obj = PyviderContext()\n\n    # Store the CLI options in the context for subcommands to access\n    for key, value in kwargs.items():\n        if value is not None:\n            setattr(ctx.obj, key, value)\n\n    if ctx.invoked_subcommand is None:\n        # This is the default action when no subcommand is given.\n        # We find the 'provide' command and invoke it.\n        provide_command = cli.get_command(ctx, \"provide\")\n        if provide_command:\n            ctx.invoke(provide_command)\n        else:\n            # This case should not happen if the CLI is assembled correctly.\n            perr(\"Error: Default command 'provide' not found.\")\n            click.echo(cli.get_help(ctx))\n</code></pre>"},{"location":"api-reference/schema/","title":"Schema","text":""},{"location":"api-reference/schema/#pyvider.schema","title":"pyvider.schema","text":""},{"location":"api-reference/schema/#pyvider.schema.NestingMode","title":"NestingMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Defines how a nested block is represented in the configuration.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsAttribute","title":"PvsAttribute","text":"<p>Represents a fully resolved schema attribute, holding a CtyType.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsNestedBlock","title":"PvsNestedBlock","text":"<p>Defines a nested block type within a schema.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsObjectType","title":"PvsObjectType","text":"<p>               Bases: <code>PvsType</code></p> <p>A schema object that holds full attribute definitions. It no longer inherits from CtyObject, but can produce one.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsObjectType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyObject\n</code></pre> <p>Converts this schema definition into its equivalent CtyObject type for validation and data manipulation. This now correctly includes attributes derived from nested blocks.</p> Source code in <code>src/pyvider/schema/types/object.py</code> <pre><code>def to_cty_type(self) -&gt; CtyObject:\n    \"\"\"\n    Converts this schema definition into its equivalent CtyObject type\n    for validation and data manipulation. This now correctly includes\n    attributes derived from nested blocks.\n    \"\"\"\n    attribute_types = {name: attr.type for name, attr in self.attributes.items()}\n    optional_attributes = {\n        name for name, attr in self.attributes.items() if attr.optional or attr.computed\n    }\n\n    # FIX: Add types for nested blocks so the CtyObject is complete.\n    for block in self.block_types:\n        block_cty_type = block.block.to_cty_type()\n        if block.nesting == NestingMode.LIST:\n            attribute_types[block.type_name] = CtyList(element_type=block_cty_type)\n        elif block.nesting == NestingMode.SET:\n            attribute_types[block.type_name] = CtySet(element_type=block_cty_type)\n        elif block.nesting == NestingMode.MAP:\n            attribute_types[block.type_name] = CtyMap(element_type=block_cty_type)\n        else:  # SINGLE or GROUP\n            attribute_types[block.type_name] = block_cty_type\n\n        optional_attributes.add(block.type_name)\n\n    return CtyObject(\n        attribute_types=attribute_types,\n        optional_attributes=frozenset(optional_attributes),\n    )\n</code></pre>"},{"location":"api-reference/schema/#pyvider.schema.PvsSchema","title":"PvsSchema","text":"<p>Represents a complete schema definition for a provider, resource, or data source. This class is the root of a schema tree.</p> <p>Attributes:</p> Name Type Description <code>version</code> <code>int</code> <p>An integer representing the schema version, used for state upgrades.</p> <code>block</code> <code>PvsObjectType</code> <p>The root block of the schema, defining its attributes and nested blocks.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsSchema.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: Any) -&gt; None\n</code></pre> <p>Validates a configuration against this schema by converting the schema to its CtyType representation and invoking its validation logic.</p> <p>This method raises CtyValidationError on failure, which is the expected contract for direct validation. Higher-level handlers are responsible for catching this exception and creating diagnostics.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def validate_config(self, config: Any) -&gt; None:\n    \"\"\"\n    Validates a configuration against this schema by converting the schema\n    to its CtyType representation and invoking its validation logic.\n\n    This method raises CtyValidationError on failure, which is the\n    expected contract for direct validation. Higher-level handlers are\n    responsible for catching this exception and creating diagnostics.\n    \"\"\"\n    logger.debug(\"Validating configuration against schema\", schema_version=self.version)\n    if not isinstance(config, dict):\n        raise CtyValidationError(f\"Configuration must be a dictionary, but got {type(config).__name__}.\")\n\n    # Convert the schema's block to its CtyType representation to get the validator.\n    validator = self.block.to_cty_type()\n\n    # The CtyType's validate method will raise CtyValidationError on failure.\n    validator.validate(config)\n</code></pre>"},{"location":"api-reference/schema/#pyvider.schema.PvsSchema.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; PvsObjectType\n</code></pre> <p>Returns the CtyType representation of the schema's root block. Since the block is now a PvsObjectType, this is a direct return.</p> Source code in <code>src/pyvider/schema/types/schema.py</code> <pre><code>def to_cty_type(self) -&gt; PvsObjectType:\n    \"\"\"\n    Returns the CtyType representation of the schema's root block.\n    Since the block is now a PvsObjectType, this is a direct return.\n    \"\"\"\n    return self.block\n</code></pre>"},{"location":"api-reference/schema/#pyvider.schema.PvsType","title":"PvsType","text":"<p>               Bases: <code>Protocol</code></p> <p>A Protocol that all Pyvider Schema type definition classes must implement. It ensures that any schema type object can be converted into its corresponding CtyType, which is essential for the conversion and marshalling layers of the framework.</p>"},{"location":"api-reference/schema/#pyvider.schema.PvsType.to_cty_type","title":"to_cty_type","text":"<pre><code>to_cty_type() -&gt; CtyType\n</code></pre> <p>Converts the Pyvider Schema type to its equivalent CtyType.</p> Source code in <code>src/pyvider/schema/types/types_base.py</code> <pre><code>def to_cty_type(self) -&gt; CtyType:\n    \"\"\"Converts the Pyvider Schema type to its equivalent CtyType.\"\"\"\n    ...\n</code></pre>"},{"location":"api-reference/schema/#pyvider.schema.a_null","title":"a_null","text":"<pre><code>a_null(\n    schema_builder: PvsAttribute | PvsSchema,\n) -&gt; CtyValue\n</code></pre> <p>Creates a null CtyValue for a given schema attribute or object.</p> Source code in <code>src/pyvider/schema/factory.py</code> <pre><code>def a_null(schema_builder: PvsAttribute | PvsSchema) -&gt; CtyValue:\n    \"\"\"Creates a null CtyValue for a given schema attribute or object.\"\"\"\n    target_type: CtyType | None = None\n    if isinstance(schema_builder, PvsAttribute):\n        target_type = schema_builder.type\n    elif isinstance(schema_builder, PvsSchema):\n        target_type = schema_builder.block.to_cty_type()\n\n    if target_type is None:\n        raise TypeError(\"a_null() expects a schema builder instance like a_str() or s_resource()\")\n    return CtyValue.null(target_type)\n</code></pre>"},{"location":"api-reference/schema/#pyvider.schema.a_unknown","title":"a_unknown","text":"<pre><code>a_unknown(\n    schema_builder: PvsAttribute | PvsSchema,\n) -&gt; CtyValue\n</code></pre> <p>Creates an unknown CtyValue for a given schema attribute or object.</p> Source code in <code>src/pyvider/schema/factory.py</code> <pre><code>def a_unknown(schema_builder: PvsAttribute | PvsSchema) -&gt; CtyValue:\n    \"\"\"Creates an unknown CtyValue for a given schema attribute or object.\"\"\"\n    target_type: CtyType | None = None\n    if isinstance(schema_builder, PvsAttribute):\n        target_type = schema_builder.type\n    elif isinstance(schema_builder, PvsSchema):\n        target_type = schema_builder.block.to_cty_type()\n\n    if target_type is None:\n        raise TypeError(\"a_unknown() expects a schema builder instance like a_str() or s_resource()\")\n    return CtyValue.unknown(target_type)\n</code></pre>"},{"location":"api-reference/types/","title":"Types","text":""},{"location":"api-reference/types/#pyvider.common.types","title":"pyvider.common.types","text":"<p>Defines common, primitive type aliases used across the framework.</p>"},{"location":"capabilities/bundling-components/","title":"\ud83d\udce6 Bundling Components","text":"<p>This guide will help you bundle your <code>pyvider</code> components into reusable packages.</p>"},{"location":"capabilities/bundling-components/#overview","title":"\ud83d\udcc4 Overview","text":"<p><code>pyvider</code> allows you to bundle your resources, data sources, functions, and capabilities into reusable packages. This makes it easy to share your components between your providers.</p> <p>To create a component bundle, you need to create a new Python package with a <code>pyvider.toml</code> file. The <code>pyvider.toml</code> file should contain a <code>[components]</code> section that lists the components in the bundle.</p>"},{"location":"capabilities/bundling-components/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of a <code>pyvider.toml</code> file for a component bundle:</p> <pre><code># pyvider.toml\n\n[components]\nresources = [\n    \"my-resource\",\n]\ndata_sources = [\n    \"my-data-source\",\n]\nfunctions = [\n    \"my-function\",\n]\ncapabilities = [\n    \"my-capability\",\n]\n</code></pre> <p>In this example, we define a component bundle that contains a resource, a data source, a function, and a capability. ```</p>"},{"location":"capabilities/capability-composition/","title":"\ud83e\udde9 Capability Composition","text":"<p>This guide explains how to compose <code>pyvider</code> capabilities.</p>"},{"location":"capabilities/capability-composition/#overview","title":"\ud83d\udcc4 Overview","text":"<p><code>pyvider</code> allows you to compose capabilities to create more complex and powerful providers. You can compose capabilities by adding them to the <code>capabilities</code> list in your provider's <code>pyvider.toml</code> file.</p>"},{"location":"capabilities/capability-composition/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to compose capabilities:</p> <pre><code># pyvider.toml\n\n[provider]\nname = \"my-provider\"\n\n[build]\ncapabilities = [\n    \"my-capability-1\",\n    \"my-capability-2\",\n]\n</code></pre> <p>In this example, we add the <code>my-capability-1</code> and <code>my-capability-2</code> capabilities to our provider. <code>pyvider</code> will automatically discover and register the capabilities with the <code>hub</code>.</p>"},{"location":"capabilities/capability-composition/#capability-order","title":"\ud83e\udde0 Capability Order","text":"<p>The order in which you list the capabilities in the <code>pyvider.toml</code> file is important. <code>pyvider</code> will initialize and configure the capabilities in the order they are listed. This means that if you have a capability that depends on another capability, you should list the dependency first. ```</p>"},{"location":"capabilities/capability-lifecycle/","title":"\ud83d\udd04 Capability Lifecycle","text":"<p>This guide explains the <code>pyvider</code> capability lifecycle.</p>"},{"location":"capabilities/capability-lifecycle/#overview","title":"\ud83d\udcc4 Overview","text":"<p>The <code>pyvider</code> capability lifecycle consists of the following steps:</p> <ol> <li>Initialization: <code>pyvider</code> initializes the capability and discovers the resources, data sources, and functions.</li> <li>Configuration: <code>pyvider</code> configures the capability with the configuration from the Terraform configuration file.</li> <li>Termination: <code>pyvider</code> terminates the capability.</li> </ol>"},{"location":"capabilities/capability-lifecycle/#implementing-the-capability-logic","title":"\ud83e\udde0 Implementing the Capability Logic","text":"<p>You can hook into the capability lifecycle by implementing the following methods in your capability class:</p> <ul> <li><code>configure</code>: This method is called to configure the capability.</li> <li><code>setup</code>: This method is called to set up the capability.</li> <li><code>teardown</code>: This method is called to tear down the capability. ```</li> </ul>"},{"location":"capabilities/capability-marketplace/","title":"\ud83c\udfea Capability Marketplace","text":"<p>This guide provides a curated list of community-built capabilities that you can use in your <code>pyvider</code> provider.</p>"},{"location":"capabilities/capability-marketplace/#overview","title":"\ud83d\udcc4 Overview","text":"<p>The <code>pyvider</code> community is constantly creating new and exciting capabilities that you can use to extend the functionality of your provider. This guide provides a curated list of some of the best capabilities that are available.</p>"},{"location":"capabilities/capability-marketplace/#available-capabilities","title":"\ud83d\ude80 Available Capabilities","text":"<ul> <li>pyvider-auth0: A capability that adds support for Auth0 authentication.</li> <li>pyvider-sentry: A capability that adds support for Sentry error reporting.</li> <li>pyvider-datadog: A capability that adds support for Datadog monitoring.</li> </ul>"},{"location":"capabilities/capability-marketplace/#submitting-a-capability","title":"\ud83d\udcdd Submitting a Capability","text":"<p>If you've created a capability that you'd like to share with the community, please open a pull request to add it to this list. ```</p>"},{"location":"capabilities/creating-capabilities/","title":"\ud83d\udee0\ufe0f Creating Capabilities","text":"<p>This guide will help you create your own <code>pyvider</code> capabilities.</p>"},{"location":"capabilities/creating-capabilities/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Capabilities are created in the same way as resources, data sources, and functions. You can create a new capability by creating a new file in the <code>capabilities</code> directory.</p>"},{"location":"capabilities/creating-capabilities/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to create a capability:</p> <pre><code>from pyvider.capabilities import capability, CapabilityConfig\n\n@capability\nclass MyCapability(CapabilityConfig):\n    \"\"\"\n    MyCapability is a custom capability that does amazing things.\n    \"\"\"\n    my_api_key: str\n\n    def configure(self, provider):\n        # Configure the capability.\n        pass\n</code></pre> <p>In this example, we define a capability with a single configuration option, <code>my_api_key</code>. We also implement the <code>configure</code> method, which is called by <code>pyvider</code> to configure the capability. ```</p>"},{"location":"capabilities/overview/","title":"Chapter 5: Unlocking Superpowers - The Capabilities Guide","text":"<p>Welcome, hero, to the capabilities guide. In this chapter, you'll learn how to unlock your provider's hidden potential with capabilities. Capabilities are like superpowers that you can add to your provider to give it new abilities.</p>"},{"location":"capabilities/overview/#what-are-capabilities","title":"\ud83d\udca5 What are Capabilities?","text":"<p>Capabilities are reusable components that can be used to extend the functionality of your provider. They can be used to add new features to your provider, such as:</p> <ul> <li>Authentication: Add support for new authentication methods, such as OAuth or API keys.</li> <li>Logging: Add custom logging to your provider.</li> <li>Caching: Add caching to your provider to improve performance.</li> <li>And much more!</li> </ul>"},{"location":"capabilities/overview/#how-do-they-work","title":"\ud83d\ude80 How do they work?","text":"<p>Capabilities are discovered and registered with the <code>hub</code> just like resources, data sources, and functions. This means that you can easily create and share capabilities between your providers.</p> <p>When you add a capability to your provider, <code>pyvider</code> will automatically call the <code>setup</code> method of the capability. This is where you can add new resources, data sources, and functions to your provider.</p> <p>In the next section, we'll learn how to use capabilities in your provider. ```</p>"},{"location":"capabilities/using-capabilities/","title":"\ud83d\ude80 Using Capabilities","text":"<p>This guide will help you use capabilities in your <code>pyvider</code> provider.</p>"},{"location":"capabilities/using-capabilities/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Capabilities are used to extend the functionality of your provider. To use a capability, you need to add it to the <code>capabilities</code> list in your provider's <code>pyvider.toml</code> file.</p>"},{"location":"capabilities/using-capabilities/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to use a capability:</p> <pre><code># pyvider.toml\n\n[provider]\nname = \"my-provider\"\n\n[build]\ncapabilities = [\n    \"my-capability\",\n]\n</code></pre> <p>In this example, we add the <code>my-capability</code> capability to our provider. <code>pyvider</code> will automatically discover and register the capability with the <code>hub</code>. ```</p>"},{"location":"contributing/code-of-conduct/","title":"Code of Conduct","text":""},{"location":"contributing/code-of-conduct/#our-pledge","title":"Our Pledge","text":"<p>We as members, contributors, and leaders pledge to make participation in our community a harassment-free experience for everyone, regardless of age, body size, visible or invisible disability, ethnicity, sex characteristics, gender identity and expression, level of experience, education, socio-economic status, nationality, personal appearance, race, religion, or sexual identity and orientation.</p> <p>We pledge to act and interact in ways that contribute to an open, welcoming, diverse, inclusive, and healthy community.</p>"},{"location":"contributing/code-of-conduct/#our-standards","title":"Our Standards","text":"<p>Examples of behavior that contributes to a positive environment for our community include:</p> <ul> <li>Demonstrating empathy and kindness toward other people</li> <li>Being respectful of differing opinions, viewpoints, and experiences</li> <li>Giving and gracefully accepting constructive feedback</li> <li>Accepting responsibility and apologizing to those affected by our mistakes,     and learning from the experience</li> <li>Focusing on what is best not just for us as individuals, but for the     overall community</li> </ul> <p>Examples of unacceptable behavior include:</p> <ul> <li>The use of sexualized language or imagery, and sexual attention or     advances of any kind</li> <li>Trolling, insulting or derogatory comments, and personal or political attacks</li> <li>Public or private harassment</li> <li>Publishing others' private information, such as a physical or email     address, without their explicit permission</li> <li>Other conduct which could reasonably be considered inappropriate in a     professional setting</li> </ul>"},{"location":"contributing/code-of-conduct/#enforcement-responsibilities","title":"Enforcement Responsibilities","text":"<p>Community leaders are responsible for clarifying and enforcing our standards and will take appropriate and fair corrective action in response to any behavior that they deem inappropriate, threatening, offensive, or harmful.</p> <p>Community leaders have the right and responsibility to remove, edit, or reject comments, commits, code, wiki edits, issues, and other contributions that are not aligned to this Code of Conduct, and will communicate reasons for moderation decisions when appropriate.</p>"},{"location":"contributing/code-of-conduct/#scope","title":"Scope","text":"<p>This Code of Conduct applies within all community spaces, and also applies when an individual is officially representing the community in public spaces. Examples of representing our community include using an official e-mail address, posting via an official social media account, or acting as an appointed representative at an online or offline event.</p>"},{"location":"contributing/code-of-conduct/#enforcement","title":"Enforcement","text":"<p>Instances of abusive, harassing, or otherwise unacceptable behavior may be reported to the community leaders responsible for enforcement at [INSERT CONTACT METHOD]. All complaints will be reviewed and investigated promptly and fairly.</p> <p>All community leaders are obligated to respect the privacy and security of the reporter of any incident.</p>"},{"location":"contributing/code-of-conduct/#enforcement-guidelines","title":"Enforcement Guidelines","text":"<p>Community leaders will follow these Community Impact Guidelines in determining the consequences for any action they deem in violation of this Code of Conduct:</p>"},{"location":"contributing/code-of-conduct/#1-correction","title":"1. Correction","text":"<p>Community Impact: Use of inappropriate language or other behavior deemed unprofessional or unwelcome in the community.</p> <p>Consequence: A private, written warning from community leaders, providing clarity around the nature of the violation and an explanation of why the behavior was inappropriate. A public apology may be requested.</p>"},{"location":"contributing/code-of-conduct/#2-warning","title":"2. Warning","text":"<p>Community Impact: A violation through a single incident or series of actions.</p> <p>Consequence: A warning with consequences for continued behavior. No interaction with the people involved, including unsolicited interaction with those enforcing the Code of Conduct, for a specified period of time. This includes avoiding interaction in community spaces as well as external channels like social media. Violating these terms may lead to a temporary or permanent ban.</p>"},{"location":"contributing/code-of-conduct/#3-temporary-ban","title":"3. Temporary Ban","text":"<p>Community Impact: A serious violation of community standards, including sustained inappropriate behavior.</p> <p>Consequence: A temporary ban from any sort of interaction or public communication with the community for a specified period of time. No public or private interaction with the people involved, including unsolicited interaction with those enforcing the Code of Conduct, is allowed during this period. Violating these terms may lead to a permanent ban.</p>"},{"location":"contributing/code-of-conduct/#4-permanent-ban","title":"4. Permanent Ban","text":"<p>Community Impact: Demonstrating a pattern of violation of community standards, including sustained inappropriate behavior, harassment of an individual, or aggression toward or disparagement of classes of individuals.</p> <p>Consequence: A permanent ban from any sort of public interaction within the community.</p>"},{"location":"contributing/code-of-conduct/#attribution","title":"Attribution","text":"<p>This Code of Conduct is adapted from the Contributor Covenant, version 2.0, available at [https://www.contributor-covenant.org/version/2/0/code_of_conduct.html].</p> <p>```</p>"},{"location":"contributing/guidelines/","title":"Contributing to Pyvider","text":"<p>We welcome contributions to <code>pyvider</code>! This document outlines the process for contributing to the project.</p>"},{"location":"contributing/guidelines/#how-to-contribute","title":"How to Contribute","text":"<ol> <li>Fork the repository: Fork the <code>pyvider</code> repository to your own GitHub account.</li> <li>Create a branch: Create a new branch for your changes.</li> <li>Make your changes: Make your changes to the code.</li> <li>Test your changes: Make sure to test your changes thoroughly.</li> <li>Submit a pull request: Submit a pull request to the <code>pyvider</code> repository.</li> </ol>"},{"location":"contributing/guidelines/#submitting-a-pull-request","title":"Submitting a Pull Request","text":"<p>When you submit a pull request, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the pull request.</li> <li>A detailed description of the changes you have made.</li> <li>A link to the issue you are addressing, if applicable.</li> <li>A summary of the testing you have done.</li> </ul>"},{"location":"contributing/guidelines/#reporting-a-bug","title":"Reporting a Bug","text":"<p>If you find a bug in <code>pyvider</code>, please report it by creating an issue on the GitHub repository. When you report a bug, please make sure to include the following:</p> <ul> <li>A clear and descriptive title for the issue.</li> <li>A detailed description of the bug.</li> <li>Steps to reproduce the bug.</li> <li>The version of <code>pyvider</code> you are using.</li> <li>The version of Terraform you are using.</li> <li>Any relevant logs or error messages. ```</li> </ul>"},{"location":"core-concepts/architecture/","title":"\ud83c\udfdb\ufe0f Architecture Overview","text":"<p>Pyvider implements a sophisticated, layered architecture that seamlessly bridges Python code with Terraform's Plugin Protocol v6. This document provides a comprehensive understanding of how Pyvider transforms your Python classes into fully functional Terraform providers.</p>"},{"location":"core-concepts/architecture/#high-level-architecture","title":"\ud83d\udcca High-Level Architecture","text":"<pre><code>graph TB\n    subgraph \"Terraform Core\"\n        TC[Terraform CLI/Core]\n        TS[Terraform State]\n        TF[.tf Configuration Files]\n    end\n\n    subgraph \"Plugin Protocol Layer\"\n        PP[gRPC Plugin Protocol v6]\n        PB[Protocol Buffers]\n    end\n\n    subgraph \"Pyvider Framework\"\n        direction TB\n        PS[Protocol Service]\n        PH[Protocol Handlers]\n        CH[Component Hub]\n        SM[Schema Manager]\n        CV[Conversion Layer]\n        ST[State Management]\n    end\n\n    subgraph \"Your Provider Code\"\n        PC[Provider Class]\n        RC[Resources]\n        DS[Data Sources]\n        FN[Functions]\n        EP[Ephemerals]\n    end\n\n    TF --&gt; TC\n    TC &lt;--&gt; PP\n    PP &lt;--&gt; PB\n    PB &lt;--&gt; PS\n    PS --&gt; PH\n    PH &lt;--&gt; CH\n    CH &lt;--&gt; SM\n    PH &lt;--&gt; CV\n    PH &lt;--&gt; ST\n    CH --&gt; PC\n    CH --&gt; RC\n    CH --&gt; DS\n    CH --&gt; FN\n    CH --&gt; EP\n    TS &lt;--&gt; TC</code></pre>"},{"location":"core-concepts/architecture/#request-flow","title":"\ud83d\udd04 Request Flow","text":"<p>Understanding how a request flows through Pyvider is crucial for debugging and optimization:</p>"},{"location":"core-concepts/architecture/#1-resource-creation-flow","title":"1. Resource Creation Flow","text":"<pre><code>sequenceDiagram\n    participant T as Terraform\n    participant G as gRPC Server\n    participant H as Protocol Handler\n    participant C as Component Hub\n    participant R as Your Resource\n    participant S as State Manager\n\n    T-&gt;&gt;G: ApplyResourceChange (create)\n    G-&gt;&gt;H: Handle request\n    H-&gt;&gt;C: Get resource by type\n    C-&gt;&gt;R: Instantiate resource\n    H-&gt;&gt;R: Call create() method\n    R-&gt;&gt;R: Execute business logic\n    R--&gt;&gt;H: Return new state\n    H-&gt;&gt;S: Encrypt private state\n    S--&gt;&gt;H: Encrypted blob\n    H--&gt;&gt;G: Response with state\n    G--&gt;&gt;T: Success with new state</code></pre>"},{"location":"core-concepts/architecture/#2-data-source-read-flow","title":"2. Data Source Read Flow","text":"<pre><code>sequenceDiagram\n    participant T as Terraform\n    participant G as gRPC Server\n    participant H as Protocol Handler\n    participant C as Component Hub\n    participant D as Your Data Source\n\n    T-&gt;&gt;G: ReadDataSource\n    G-&gt;&gt;H: Handle request\n    H-&gt;&gt;C: Get data source by type\n    C-&gt;&gt;D: Instantiate data source\n    H-&gt;&gt;D: Call read() method\n    D-&gt;&gt;D: Fetch data\n    D--&gt;&gt;H: Return state\n    H--&gt;&gt;G: Response with data\n    G--&gt;&gt;T: Success with data</code></pre>"},{"location":"core-concepts/architecture/#core-components","title":"\ud83e\udde9 Core Components","text":""},{"location":"core-concepts/architecture/#1-component-hub-pyviderhub","title":"1. Component Hub (<code>pyvider.hub</code>)","text":"<p>The Component Hub is the central registry that manages all provider components:</p> <pre><code># Internal structure\nclass ComponentHub:\n    providers: dict[str, type[BaseProvider]]\n    resources: dict[str, type[BaseResource]]\n    data_sources: dict[str, type[BaseDataSource]]\n    functions: dict[str, type[BaseFunction]]\n    ephemerals: dict[str, type[BaseEphemeral]]\n</code></pre> <p>Responsibilities: - Automatic component discovery via decorators - Type validation and registration - Dependency injection for capabilities - Component lifecycle management</p>"},{"location":"core-concepts/architecture/#2-protocol-service-pyviderprotocolsservice","title":"2. Protocol Service (<code>pyvider.protocols.service</code>)","text":"<p>The Protocol Service implements the Terraform Plugin Protocol v6 gRPC service:</p> <pre><code>class TerraformProviderServicer:\n    async def GetProviderSchema(...)\n    async def ValidateProviderConfig(...)\n    async def ConfigureProvider(...)\n    async def ValidateResourceConfig(...)\n    async def PlanResourceChange(...)\n    async def ApplyResourceChange(...)\n    async def ReadResource(...)\n    async def ImportResourceState(...)\n    # ... and more\n</code></pre> <p>Key Features: - Full Protocol v6 implementation - Async/await support throughout - Automatic error handling and diagnostics - Request/response logging for debugging</p>"},{"location":"core-concepts/architecture/#3-schema-system-pyviderschema","title":"3. Schema System (<code>pyvider.schema</code>)","text":"<p>The Schema System provides type-safe data modeling:</p> <pre><code>@attrs.define\nclass ResourceConfig:\n    name: str = Attribute(required=True, description=\"Resource name\")\n    tags: dict[str, str] = Attribute(default_factory=dict)\n    size: int = Attribute(validators=[Range(min=1, max=100)])\n</code></pre> <p>Features: - Automatic schema generation from Python types - Built-in validators and constraints - Computed and sensitive attribute support - Nested blocks and complex types</p>"},{"location":"core-concepts/architecture/#4-conversion-layer-pyviderconversion","title":"4. Conversion Layer (<code>pyvider.conversion</code>)","text":"<p>Handles bidirectional conversion between Python and Terraform types:</p> <pre><code>graph LR\n    subgraph \"Python Types\"\n        PT[str, int, bool, dict, list]\n    end\n\n    subgraph \"CTY Types\"\n        CT[String, Number, Bool, Object, List]\n    end\n\n    subgraph \"Protocol Buffers\"\n        PB[DynamicValue, Schema]\n    end\n\n    PT &lt;--&gt; CT\n    CT &lt;--&gt; PB</code></pre> <p>Conversion Examples: - Python <code>dict</code> \u2194 CTY <code>Object</code> \u2194 Protocol Buffer <code>DynamicValue</code> - Python <code>list[str]</code> \u2194 CTY <code>List(String)</code> \u2194 Protocol Buffer <code>DynamicValue</code> - Python <code>@attrs.define</code> class \u2194 CTY <code>Object</code> with schema</p>"},{"location":"core-concepts/architecture/#5-state-management-pyviderresourcesprivate_state","title":"5. State Management (<code>pyvider.resources.private_state</code>)","text":"<p>Manages resource state with encryption for sensitive data:</p> <pre><code>class PrivateState:\n    \"\"\"Encrypted storage for sensitive provider data\"\"\"\n\n    @classmethod\n    def encrypt(cls, data: dict) -&gt; bytes:\n        # AES-256 encryption with key derivation\n\n    @classmethod\n    def decrypt(cls, encrypted: bytes) -&gt; dict:\n        # Secure decryption with validation\n</code></pre> <p>Security Features: - AES-256-GCM encryption - Key derivation with PBKDF2 - Automatic key rotation support - Tamper detection</p>"},{"location":"core-concepts/architecture/#protocol-implementation","title":"\ud83d\udd0c Protocol Implementation","text":""},{"location":"core-concepts/architecture/#terraform-plugin-protocol-v6","title":"Terraform Plugin Protocol v6","text":"<p>Pyvider implements the complete Terraform Plugin Protocol v6 specification:</p>"},{"location":"core-concepts/architecture/#supported-rpcs","title":"Supported RPCs","text":"RPC Method Purpose Pyvider Support <code>GetProviderSchema</code> Returns provider schema \u2705 Full <code>ValidateProviderConfig</code> Validates provider config \u2705 Full <code>ConfigureProvider</code> Configures provider instance \u2705 Full <code>ValidateResourceConfig</code> Validates resource config \u2705 Full <code>ValidateDataResourceConfig</code> Validates data source config \u2705 Full <code>UpgradeResourceState</code> Migrates resource state \u2705 Full <code>ReadResource</code> Refreshes resource state \u2705 Full <code>PlanResourceChange</code> Plans resource changes \u2705 Full <code>ApplyResourceChange</code> Applies resource changes \u2705 Full <code>ImportResourceState</code> Imports existing resources \u2705 Full <code>MoveResourceState</code> Moves resources \u2705 Full <code>ReadDataSource</code> Reads data source \u2705 Full <code>GetFunctions</code> Returns function definitions \u2705 Full <code>CallFunction</code> Executes functions \u2705 Full <code>OpenEphemeralResource</code> Opens ephemeral resource \u2705 Full <code>RenewEphemeralResource</code> Renews ephemeral resource \u2705 Full <code>CloseEphemeralResource</code> Closes ephemeral resource \u2705 Full <code>StopProvider</code> Graceful shutdown \u2705 Full"},{"location":"core-concepts/architecture/#message-flow","title":"Message Flow","text":"<pre><code>graph TD\n    subgraph \"Terraform\"\n        T1[terraform plan]\n        T2[terraform apply]\n        T3[terraform destroy]\n    end\n\n    subgraph \"Protocol Messages\"\n        M1[GetProviderSchema]\n        M2[ValidateResourceConfig]\n        M3[PlanResourceChange]\n        M4[ApplyResourceChange]\n        M5[ReadResource]\n    end\n\n    subgraph \"Pyvider Handlers\"\n        H1[get_provider_schema.py]\n        H2[validate_resource_config.py]\n        H3[plan_resource_change.py]\n        H4[apply_resource_change.py]\n        H5[read_resource.py]\n    end\n\n    T1 --&gt; M1 --&gt; H1\n    T1 --&gt; M2 --&gt; H2\n    T1 --&gt; M3 --&gt; H3\n    T2 --&gt; M4 --&gt; H4\n    T2 --&gt; M5 --&gt; H5\n    T3 --&gt; M3 --&gt; H3\n    T3 --&gt; M4 --&gt; H4</code></pre>"},{"location":"core-concepts/architecture/#component-discovery","title":"\ud83c\udfaf Component Discovery","text":"<p>Pyvider uses a sophisticated discovery mechanism to find and register components:</p>"},{"location":"core-concepts/architecture/#discovery-process","title":"Discovery Process","text":"<ol> <li>Entry Point Scanning: Looks for <code>pyvider.components</code> entry points</li> <li>Package Traversal: Recursively scans packages for decorated classes</li> <li>Decorator Detection: Identifies classes with registration decorators</li> <li>Validation: Ensures components meet interface requirements</li> <li>Registration: Adds valid components to the hub</li> </ol>"},{"location":"core-concepts/architecture/#registration-flow","title":"Registration Flow","text":"<pre><code># Your code\n@register_resource(\"my_resource\")\nclass MyResource(BaseResource):\n    pass\n\n# Discovery process\n1. Scanner finds @register_resource decorator\n2. Validates MyResource extends BaseResource\n3. Checks for required methods (create, read, update, delete)\n4. Registers in hub.resources[\"my_resource\"] = MyResource\n5. Generates Terraform schema from class definition\n</code></pre>"},{"location":"core-concepts/architecture/#lifecycle-hooks","title":"\ud83d\udd27 Lifecycle Hooks","text":"<p>Pyvider provides lifecycle hooks for advanced customization:</p>"},{"location":"core-concepts/architecture/#provider-lifecycle","title":"Provider Lifecycle","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; Discovered: Component Discovery\n    Discovered --&gt; Initialized: Provider.__init__()\n    Initialized --&gt; Setup: Provider.setup()\n    Setup --&gt; Configured: Provider.configure()\n    Configured --&gt; Ready: Ready for requests\n    Ready --&gt; Processing: Handle requests\n    Processing --&gt; Ready: Request complete\n    Ready --&gt; Stopping: StopProvider RPC\n    Stopping --&gt; [*]: Cleanup complete</code></pre>"},{"location":"core-concepts/architecture/#resource-lifecycle","title":"Resource Lifecycle","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; Creating: terraform apply (new)\n    Creating --&gt; Created: create() returns State\n    Created --&gt; Reading: terraform refresh\n    Reading --&gt; Read: read() returns State\n    Read --&gt; Updating: terraform apply (change)\n    Updating --&gt; Updated: update() returns State\n    Updated --&gt; Reading: Continue management\n    Read --&gt; Deleting: terraform destroy\n    Deleting --&gt; [*]: delete() completes</code></pre>"},{"location":"core-concepts/architecture/#performance-optimizations","title":"\u26a1 Performance Optimizations","text":""},{"location":"core-concepts/architecture/#1-async-everything","title":"1. Async Everything","text":"<p>All I/O operations use async/await for maximum concurrency:</p> <pre><code>async def create(self, config: Config) -&gt; State:\n    # Parallel API calls\n    results = await asyncio.gather(\n        self.create_network(),\n        self.allocate_storage(),\n        self.configure_security()\n    )\n    return State(...)\n</code></pre>"},{"location":"core-concepts/architecture/#2-connection-pooling","title":"2. Connection Pooling","text":"<p>gRPC connections are pooled and reused:</p> <pre><code># Automatic connection management\nchannel_pool = grpc.aio.insecure_channel(\n    target='localhost:50051',\n    options=[\n        ('grpc.max_connection_idle_ms', 30000),\n        ('grpc.keepalive_time_ms', 10000),\n    ]\n)\n</code></pre>"},{"location":"core-concepts/architecture/#3-schema-caching","title":"3. Schema Caching","text":"<p>Schemas are computed once and cached:</p> <pre><code>@cached_property\ndef schema(self) -&gt; PvsSchema:\n    return self._generate_schema()\n</code></pre>"},{"location":"core-concepts/architecture/#4-lazy-loading","title":"4. Lazy Loading","text":"<p>Components are loaded only when needed:</p> <pre><code>def get_resource(self, name: str) -&gt; type[BaseResource]:\n    if name not in self._loaded:\n        self._loaded[name] = self._load_resource(name)\n    return self._loaded[name]\n</code></pre>"},{"location":"core-concepts/architecture/#debugging-architecture","title":"\ud83d\udd0d Debugging Architecture","text":""},{"location":"core-concepts/architecture/#debug-logging","title":"Debug Logging","text":"<p>Enable comprehensive debug logging:</p> <pre><code>export PYVIDER_LOG_LEVEL=DEBUG\nexport FOUNDATION_LOG_LEVEL=DEBUG\n</code></pre>"},{"location":"core-concepts/architecture/#request-tracing","title":"Request Tracing","text":"<p>Every request includes trace IDs for correlation:</p> <pre><code>[2024-01-15 10:23:45] [INFO] [trace_id=abc123] ApplyResourceChange started\n[2024-01-15 10:23:45] [DEBUG] [trace_id=abc123] Resource type: my_resource\n[2024-01-15 10:23:46] [INFO] [trace_id=abc123] ApplyResourceChange completed\n</code></pre>"},{"location":"core-concepts/architecture/#performance-profiling","title":"Performance Profiling","text":"<p>Built-in profiling for optimization:</p> <pre><code>with timed_block(logger, \"resource_creation\"):\n    state = await resource.create(config)\n# Logs: [\u23f1\ufe0f] resource_creation duration_ms=234.56\n</code></pre>"},{"location":"core-concepts/architecture/#security-architecture","title":"\ud83d\udee1\ufe0f Security Architecture","text":""},{"location":"core-concepts/architecture/#1-input-validation","title":"1. Input Validation","text":"<p>All inputs are validated before processing:</p> <pre><code>@attrs.define\nclass Config:\n    api_key: str = Attribute(\n        validators=[\n            Length(min=32, max=64),\n            Regex(r'^[a-zA-Z0-9]+$')\n        ]\n    )\n</code></pre>"},{"location":"core-concepts/architecture/#2-secret-management","title":"2. Secret Management","text":"<p>Sensitive data never logged or exposed:</p> <pre><code>@attrs.define\nclass Config:\n    password: str = Attribute(sensitive=True)  # Never logged\n</code></pre>"},{"location":"core-concepts/architecture/#3-secure-communication","title":"3. Secure Communication","text":"<p>gRPC with TLS support:</p> <pre><code>credentials = grpc.ssl_channel_credentials()\nchannel = grpc.aio.secure_channel('localhost:50051', credentials)\n</code></pre>"},{"location":"core-concepts/architecture/#architecture-best-practices","title":"\ud83c\udf93 Architecture Best Practices","text":""},{"location":"core-concepts/architecture/#1-separation-of-concerns","title":"1. Separation of Concerns","text":"<ul> <li>Provider: Configuration and authentication</li> <li>Resources: CRUD operations for infrastructure</li> <li>Data Sources: Read-only data fetching</li> <li>Functions: Pure transformations</li> <li>Capabilities: Reusable functionality</li> </ul>"},{"location":"core-concepts/architecture/#2-error-handling","title":"2. Error Handling","text":"<pre><code>async def create(self, config: Config) -&gt; State:\n    try:\n        result = await self.api_call()\n    except ApiError as e:\n        raise ResourceError(f\"Failed to create: {e}\")\n    return State(...)\n</code></pre>"},{"location":"core-concepts/architecture/#3-resource-design","title":"3. Resource Design","text":"<ul> <li>Keep resources focused on a single concern</li> <li>Use composition via capabilities for shared functionality</li> <li>Implement proper error handling and rollback</li> <li>Always validate inputs</li> </ul>"},{"location":"core-concepts/architecture/#4-state-management","title":"4. State Management","text":"<ul> <li>Store only essential data in state</li> <li>Use private state for sensitive information</li> <li>Implement proper read() to detect drift</li> <li>Handle missing resources gracefully</li> </ul>"},{"location":"core-concepts/architecture/#further-reading","title":"\ud83d\udcda Further Reading","text":"<ul> <li>Component Model - Deep dive into component system</li> <li>Schema System - Advanced schema features</li> <li>Schema System - Schema documentation</li> </ul> <p>   Continue to Component Model \u2192 </p>"},{"location":"core-concepts/component-model/","title":"\ud83e\udde9 Component Model","text":"<p>Pyvider's component model provides a powerful, decorator-based system for building Terraform providers. This document explores how components are defined, discovered, registered, and managed throughout their lifecycle.</p>"},{"location":"core-concepts/component-model/#component-hierarchy","title":"\ud83d\udcca Component Hierarchy","text":"<pre><code>graph TB\n    subgraph \"Component Types\"\n        P[Provider]\n        R[Resources]\n        D[Data Sources]\n        F[Functions]\n        E[Ephemeral Resources]\n        C[Capabilities]\n    end\n\n    P --&gt; R\n    P --&gt; D\n    P --&gt; F\n    P --&gt; E\n    R -.-&gt; C\n    D -.-&gt; C\n    E -.-&gt; C\n\n    style P fill:#f9f,stroke:#333,stroke-width:4px\n    style C fill:#bbf,stroke:#333,stroke-width:2px,stroke-dasharray: 5 5</code></pre>"},{"location":"core-concepts/component-model/#component-types","title":"\ud83c\udfaf Component Types","text":""},{"location":"core-concepts/component-model/#1-provider-component","title":"1. Provider Component","text":"<p>The Provider is the root component that configures authentication and shared settings:</p> <pre><code>from pyvider.providers import register_provider, BaseProvider, ProviderMetadata\nimport attrs\n\n@register_provider(\"mycloud\")\nclass MyCloudProvider(BaseProvider):\n    \"\"\"Root provider component for MyCloud services.\"\"\"\n\n    def __init__(self):\n        super().__init__(\n            metadata=ProviderMetadata(\n                name=\"mycloud\",\n                version=\"1.0.0\",\n                protocol_version=\"6\"\n            )\n        )\n\n    @attrs.define\n    class Config:\n        \"\"\"Provider configuration schema.\"\"\"\n        api_key: str = Attribute(\n            required=True,\n            sensitive=True,\n            description=\"API key for authentication\"\n        )\n        endpoint: str = Attribute(\n            default=\"https://api.mycloud.com\",\n            description=\"API endpoint URL\"\n        )\n        timeout: int = Attribute(\n            default=30,\n            description=\"Request timeout in seconds\"\n        )\n\n    async def configure(self, config: Config) -&gt; None:\n        \"\"\"Configure provider with validated configuration.\"\"\"\n        self.client = MyCloudClient(\n            api_key=config.api_key,\n            endpoint=config.endpoint,\n            timeout=config.timeout\n        )\n        await self.client.authenticate()\n</code></pre> <p>Key Characteristics: - Singleton: Only one provider instance per Terraform configuration - Configuration Hub: Stores shared configuration for all resources - Authentication: Manages API credentials and client initialization - Metadata: Defines provider name, version, and capabilities</p>"},{"location":"core-concepts/component-model/#2-resource-component","title":"2. Resource Component","text":"<p>Resources represent manageable infrastructure with full CRUD lifecycle:</p> <pre><code>from pyvider.resources import register_resource, BaseResource\nfrom pyvider.schema import Attribute\nimport attrs\n\n@register_resource(\"server\")\nclass Server(BaseResource):\n    \"\"\"Manages a cloud server instance.\"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Resource configuration (from Terraform).\"\"\"\n        name: str = Attribute(required=True, description=\"Server name\")\n        size: str = Attribute(default=\"small\", description=\"Instance size\")\n        image: str = Attribute(required=True, description=\"OS image\")\n        tags: dict[str, str] = Attribute(\n            default_factory=dict,\n            description=\"Resource tags\"\n        )\n\n    @attrs.define\n    class State:\n        \"\"\"Resource state (tracked by Terraform).\"\"\"\n        id: str = Attribute(computed=True, description=\"Server ID\")\n        name: str = Attribute(description=\"Server name\")\n        size: str = Attribute(description=\"Instance size\")\n        image: str = Attribute(description=\"OS image\")\n        ip_address: str = Attribute(computed=True, description=\"Public IP\")\n        status: str = Attribute(computed=True, description=\"Server status\")\n        tags: dict[str, str] = Attribute(description=\"Resource tags\")\n\n    async def create(self, config: Config) -&gt; State:\n        \"\"\"Create a new server.\"\"\"\n        server = await self.provider.client.create_server(\n            name=config.name,\n            size=config.size,\n            image=config.image,\n            tags=config.tags\n        )\n        return State(\n            id=server.id,\n            name=server.name,\n            size=server.size,\n            image=server.image,\n            ip_address=server.public_ip,\n            status=server.status,\n            tags=server.tags\n        )\n\n    async def read(self, state: State) -&gt; State | None:\n        \"\"\"Refresh server state.\"\"\"\n        try:\n            server = await self.provider.client.get_server(state.id)\n            return State(\n                id=server.id,\n                name=server.name,\n                size=server.size,\n                image=server.image,\n                ip_address=server.public_ip,\n                status=server.status,\n                tags=server.tags\n            )\n        except NotFoundError:\n            return None  # Server was deleted outside Terraform\n\n    async def update(self, config: Config, state: State) -&gt; State:\n        \"\"\"Update server configuration.\"\"\"\n        # Update mutable attributes\n        if config.tags != state.tags:\n            await self.provider.client.update_tags(state.id, config.tags)\n\n        if config.size != state.size:\n            await self.provider.client.resize_server(state.id, config.size)\n\n        # Refresh and return new state\n        return await self.read(state)\n\n    async def delete(self, state: State) -&gt; None:\n        \"\"\"Delete the server.\"\"\"\n        await self.provider.client.delete_server(state.id)\n</code></pre> <p>Lifecycle Methods: - <code>create()</code>: Creates new infrastructure - <code>read()</code>: Refreshes current state - <code>update()</code>: Modifies existing infrastructure - <code>delete()</code>: Removes infrastructure</p>"},{"location":"core-concepts/component-model/#3-data-source-component","title":"3. Data Source Component","text":"<p>Data sources provide read-only access to existing infrastructure:</p> <pre><code>from pyvider.data_sources import register_data_source, BaseDataSource\nimport attrs\n\n@register_data_source(\"images\")\nclass Images(BaseDataSource):\n    \"\"\"Fetches available server images.\"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Data source configuration.\"\"\"\n        filter: str = Attribute(\n            default=\"*\",\n            description=\"Image name filter pattern\"\n        )\n        include_deprecated: bool = Attribute(\n            default=False,\n            description=\"Include deprecated images\"\n        )\n\n    @attrs.define\n    class State:\n        \"\"\"Data source output.\"\"\"\n        images: list[dict] = Attribute(\n            computed=True,\n            description=\"List of available images\"\n        )\n        total_count: int = Attribute(\n            computed=True,\n            description=\"Total number of images\"\n        )\n\n    async def read(self, config: Config) -&gt; State:\n        \"\"\"Fetch image data.\"\"\"\n        images = await self.provider.client.list_images(\n            filter=config.filter,\n            include_deprecated=config.include_deprecated\n        )\n\n        return State(\n            images=[\n                {\n                    \"id\": img.id,\n                    \"name\": img.name,\n                    \"version\": img.version,\n                    \"deprecated\": img.deprecated\n                }\n                for img in images\n            ],\n            total_count=len(images)\n        )\n</code></pre>"},{"location":"core-concepts/component-model/#4-function-component","title":"4. Function Component","text":"<p>Functions provide pure, callable transformations:</p> <pre><code>from pyvider.functions import register_function, BaseFunction\nimport attrs\nimport hashlib\n\n@register_function(name=\"hash_file\")\nclass HashFile(BaseFunction):\n    \"\"\"Computes SHA256 hash of file content.\"\"\"\n\n    @attrs.define\n    class Input:\n        \"\"\"Function input parameters.\"\"\"\n        content: str = Attribute(required=True, description=\"File content\")\n        algorithm: str = Attribute(\n            default=\"sha256\",\n            description=\"Hash algorithm\"\n        )\n\n    @attrs.define\n    class Output:\n        \"\"\"Function output.\"\"\"\n        hash: str = Attribute(description=\"Computed hash\")\n        algorithm: str = Attribute(description=\"Algorithm used\")\n\n    async def call(self, input: Input) -&gt; Output:\n        \"\"\"Execute the function.\"\"\"\n        if input.algorithm == \"sha256\":\n            hash_obj = hashlib.sha256(input.content.encode())\n        elif input.algorithm == \"md5\":\n            hash_obj = hashlib.md5(input.content.encode())\n        else:\n            raise ValueError(f\"Unsupported algorithm: {input.algorithm}\")\n\n        return Output(\n            hash=hash_obj.hexdigest(),\n            algorithm=input.algorithm\n        )\n</code></pre>"},{"location":"core-concepts/component-model/#5-ephemeral-resource-component","title":"5. Ephemeral Resource Component","text":"<p>Ephemeral resources manage short-lived connections or sessions:</p> <pre><code>from pyvider.ephemerals import register_ephemeral_resource, BaseEphemeral\nimport attrs\n\n@register_ephemeral_resource(\"database_connection\")\nclass DatabaseConnection(BaseEphemeral):\n    \"\"\"Manages a temporary database connection.\"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Connection configuration.\"\"\"\n        host: str = Attribute(required=True)\n        port: int = Attribute(default=5432)\n        database: str = Attribute(required=True)\n        username: str = Attribute(required=True)\n        password: str = Attribute(required=True, sensitive=True)\n\n    @attrs.define\n    class State:\n        \"\"\"Connection state.\"\"\"\n        connection_id: str = Attribute(computed=True)\n        connected_at: str = Attribute(computed=True)\n        expires_at: str = Attribute(computed=True)\n\n    async def open(self, config: Config) -&gt; State:\n        \"\"\"Open a new connection.\"\"\"\n        conn = await self.provider.db_pool.connect(\n            host=config.host,\n            port=config.port,\n            database=config.database,\n            username=config.username,\n            password=config.password\n        )\n\n        return State(\n            connection_id=conn.id,\n            connected_at=conn.created_at.isoformat(),\n            expires_at=conn.expires_at.isoformat()\n        )\n\n    async def renew(self, state: State) -&gt; State:\n        \"\"\"Renew the connection lease.\"\"\"\n        conn = await self.provider.db_pool.renew(state.connection_id)\n        return State(\n            connection_id=conn.id,\n            connected_at=state.connected_at,\n            expires_at=conn.expires_at.isoformat()\n        )\n\n    async def close(self, state: State) -&gt; None:\n        \"\"\"Close the connection.\"\"\"\n        await self.provider.db_pool.disconnect(state.connection_id)\n</code></pre>"},{"location":"core-concepts/component-model/#component-discovery","title":"\ud83d\udd0d Component Discovery","text":""},{"location":"core-concepts/component-model/#automatic-discovery-process","title":"Automatic Discovery Process","text":"<pre><code>sequenceDiagram\n    participant M as Main\n    participant D as Discovery\n    participant I as Importer\n    participant H as Hub\n    participant C as Component\n\n    M-&gt;&gt;D: discover_all()\n    D-&gt;&gt;I: Import modules\n    I-&gt;&gt;I: Find @register_* decorators\n    I-&gt;&gt;C: Load component class\n    C-&gt;&gt;H: Register component\n    H-&gt;&gt;H: Validate component\n    H--&gt;&gt;D: Registration complete\n    D--&gt;&gt;M: Discovery complete</code></pre>"},{"location":"core-concepts/component-model/#entry-points","title":"Entry Points","text":"<p>Components can be discovered through Python entry points:</p> <pre><code># pyproject.toml\n[project.entry-points.\"pyvider.components\"]\nmycloud = \"mycloud_provider.components\"\n</code></pre>"},{"location":"core-concepts/component-model/#manual-registration","title":"Manual Registration","text":"<p>For testing or dynamic components:</p> <pre><code>from pyvider.hub import hub\n\n# Manual registration\nhub.register(\"resource\", \"custom_resource\", CustomResource)\n\n# Verify registration\nassert \"custom_resource\" in hub.resources\n</code></pre>"},{"location":"core-concepts/component-model/#decorator-system","title":"\ud83c\udfa8 Decorator System","text":""},{"location":"core-concepts/component-model/#registration-decorators","title":"Registration Decorators","text":"<p>Each component type has its own registration decorator:</p> <pre><code># Provider\n@register_provider(\"name\")\nclass MyProvider(BaseProvider): ...\n\n# Resource\n@register_resource(\"name\")\nclass MyResource(BaseResource): ...\n\n# Data Source\n@register_data_source(\"name\")\nclass MyDataSource(BaseDataSource): ...\n\n# Function\n@register_function(name=\"name\")\nclass MyFunction(BaseFunction): ...\n\n# Ephemeral\n@register_ephemeral_resource(\"name\")\nclass MyEphemeral(BaseEphemeral): ...\n</code></pre>"},{"location":"core-concepts/component-model/#decorator-metadata","title":"Decorator Metadata","text":"<p>Decorators attach metadata for discovery:</p> <pre><code>@register_resource(\"server\")\nclass Server(BaseResource):\n    pass\n\n# Attached metadata:\n# Server._is_registered_resource = True\n# Server._registered_name = \"server\"\n</code></pre>"},{"location":"core-concepts/component-model/#component-relationships","title":"\ud83d\udd17 Component Relationships","text":""},{"location":"core-concepts/component-model/#provider-resource-relationship","title":"Provider-Resource Relationship","text":"<pre><code>@register_resource(\"server\")\nclass Server(BaseResource):\n    async def create(self, config: Config) -&gt; State:\n        # Access provider instance\n        client = self.provider.client\n\n        # Use provider configuration\n        region = self.provider.config.region\n\n        # Call provider methods\n        server = await client.create_server(...)\n        return State(...)\n</code></pre>"},{"location":"core-concepts/component-model/#capability-composition","title":"Capability Composition","text":"<p>Components can be enhanced with capabilities:</p> <pre><code>from pyvider.capabilities import register_capability\n\n@register_capability(\"taggable\")\nclass TaggableCapability:\n    \"\"\"Adds tagging functionality to resources.\"\"\"\n\n    def apply_tags(self, resource_id: str, tags: dict[str, str]):\n        # Tag management logic\n        pass\n\n@register_resource(\"server\", capabilities=[\"taggable\"])\nclass Server(BaseResource):\n    async def create(self, config: Config) -&gt; State:\n        # Create server\n        server = await self.provider.client.create_server(...)\n\n        # Use capability\n        self.capabilities.taggable.apply_tags(server.id, config.tags)\n\n        return State(...)\n</code></pre>"},{"location":"core-concepts/component-model/#schema-generation","title":"\ud83d\udccb Schema Generation","text":""},{"location":"core-concepts/component-model/#automatic-schema-discovery","title":"Automatic Schema Discovery","text":"<p>Pyvider automatically generates Terraform schemas from Python classes:</p> <pre><code>@attrs.define\nclass Config:\n    name: str = Attribute(required=True)\n    size: int = Attribute(default=10, validators=[Range(1, 100)])\n    tags: dict[str, str] = Attribute(default_factory=dict)\n\n# Generated Terraform schema:\n{\n    \"name\": {\n        \"type\": \"string\",\n        \"required\": true\n    },\n    \"size\": {\n        \"type\": \"number\",\n        \"optional\": true,\n        \"default\": 10,\n        \"validators\": [{\n            \"type\": \"range\",\n            \"min\": 1,\n            \"max\": 100\n        }]\n    },\n    \"tags\": {\n        \"type\": \"map\",\n        \"element_type\": \"string\",\n        \"optional\": true\n    }\n}\n</code></pre>"},{"location":"core-concepts/component-model/#type-mappings","title":"Type Mappings","text":"Python Type Terraform Type Example <code>str</code> <code>string</code> <code>\"hello\"</code> <code>int</code> <code>number</code> <code>42</code> <code>float</code> <code>number</code> <code>3.14</code> <code>bool</code> <code>bool</code> <code>true</code> <code>list[T]</code> <code>list(T)</code> <code>[\"a\", \"b\"]</code> <code>dict[str, T]</code> <code>map(T)</code> <code>{\"key\": \"value\"}</code> <code>set[T]</code> <code>set(T)</code> <code>[\"unique\"]</code> <code>@attrs.define</code> <code>object</code> Complex nested object"},{"location":"core-concepts/component-model/#component-lifecycle","title":"\ud83d\udd04 Component Lifecycle","text":""},{"location":"core-concepts/component-model/#initialization-flow","title":"Initialization Flow","text":"<pre><code>stateDiagram-v2\n    [*] --&gt; Decorated: @register_* applied\n    Decorated --&gt; Discovered: Discovery scan\n    Discovered --&gt; Registered: Added to hub\n    Registered --&gt; Validated: Schema validation\n    Validated --&gt; Ready: Available for use\n    Ready --&gt; Instantiated: Request received\n    Instantiated --&gt; Configured: Provider injected\n    Configured --&gt; Active: Processing requests\n    Active --&gt; [*]: Request complete</code></pre>"},{"location":"core-concepts/component-model/#resource-instance-lifecycle","title":"Resource Instance Lifecycle","text":"<pre><code># 1. Instantiation (per request)\nresource = hub.get_resource(\"server\")()\n\n# 2. Provider injection\nresource.provider = configured_provider\n\n# 3. Context injection (if needed)\nresource.context = ResourceContext(\n    resource_type=\"mycloud_server\",\n    resource_name=\"web_server\"\n)\n\n# 4. Method invocation\nstate = await resource.create(config)\n\n# 5. Cleanup (automatic)\n# Instance is garbage collected after request\n</code></pre>"},{"location":"core-concepts/component-model/#validation","title":"\ud83d\udee1\ufe0f Validation","text":""},{"location":"core-concepts/component-model/#component-validation","title":"Component Validation","text":"<p>Components are validated during registration:</p> <pre><code>def validate_resource(cls: type) -&gt; None:\n    \"\"\"Validate resource component.\"\"\"\n    # Check base class\n    if not issubclass(cls, BaseResource):\n        raise ValidationError(\"Must inherit from BaseResource\")\n\n    # Check required methods\n    required = ['create', 'read', 'update', 'delete']\n    for method in required:\n        if not hasattr(cls, method):\n            raise ValidationError(f\"Missing required method: {method}\")\n\n    # Check schema classes\n    if not hasattr(cls, 'Config'):\n        raise ValidationError(\"Missing Config class\")\n    if not hasattr(cls, 'State'):\n        raise ValidationError(\"Missing State class\")\n</code></pre>"},{"location":"core-concepts/component-model/#schema-validation","title":"Schema Validation","text":"<pre><code>@attrs.define\nclass Config:\n    port: int = Attribute(\n        required=True,\n        validators=[\n            Range(min=1, max=65535),\n            lambda x: x != 22 or ValueError(\"SSH port not allowed\")\n        ]\n    )\n</code></pre>"},{"location":"core-concepts/component-model/#best-practices","title":"\ud83c\udfaf Best Practices","text":""},{"location":"core-concepts/component-model/#1-component-design","title":"1. Component Design","text":"<p>Do: - Keep components focused on a single responsibility - Use descriptive names that match Terraform conventions - Implement comprehensive error handling - Document all attributes and methods</p> <p>Don't: - Mix concerns in a single component - Use generic names like \"Resource\" or \"Data\" - Ignore error cases - Skip validation</p>"},{"location":"core-concepts/component-model/#2-schema-design","title":"2. Schema Design","text":"<pre><code>@attrs.define\nclass Config:\n    # Good: Clear, typed, documented\n    instance_type: str = Attribute(\n        required=True,\n        description=\"EC2 instance type (e.g., t3.micro)\",\n        validators=[OneOf([\"t3.micro\", \"t3.small\", \"t3.medium\"])]\n    )\n\n    # Bad: Vague, untyped, no validation\n    config: dict = Attribute()  # Too generic!\n</code></pre>"},{"location":"core-concepts/component-model/#3-state-management","title":"3. State Management","text":"<pre><code>class State:\n    # Include only essential state\n    id: str = Attribute(computed=True)  # Always include ID\n    name: str = Attribute()  # User-provided values\n\n    # Computed values that might change\n    status: str = Attribute(computed=True)\n    ip_address: str = Attribute(computed=True)\n\n    # Don't include:\n    # - Temporary values\n    # - Values that can be recomputed\n    # - Large data blobs\n</code></pre>"},{"location":"core-concepts/component-model/#4-error-handling","title":"4. Error Handling","text":"<pre><code>async def create(self, config: Config) -&gt; State:\n    try:\n        result = await self.provider.client.create(...)\n    except ClientError as e:\n        if e.code == \"QUOTA_EXCEEDED\":\n            raise ResourceError(\n                \"Quota exceeded for this resource type\",\n                details={\"limit\": e.limit, \"current\": e.current}\n            )\n        raise ResourceError(f\"Failed to create resource: {e}\")\n\n    return State(...)\n</code></pre>"},{"location":"core-concepts/component-model/#advanced-topics","title":"\ud83d\udcda Advanced Topics","text":""},{"location":"core-concepts/component-model/#dynamic-component-registration","title":"Dynamic Component Registration","text":"<pre><code># Register components at runtime\ndef create_dynamic_resource(table_name: str):\n    @register_resource(f\"dynamodb_{table_name}\")\n    class DynamicTable(BaseResource):\n        # Implementation\n        pass\n\n    return DynamicTable\n\n# Create and register\nUserTable = create_dynamic_resource(\"users\")\n</code></pre>"},{"location":"core-concepts/component-model/#component-inheritance","title":"Component Inheritance","text":"<pre><code>class BaseCloudResource(BaseResource):\n    \"\"\"Shared functionality for cloud resources.\"\"\"\n\n    async def apply_common_tags(self, resource_id: str):\n        # Common tagging logic\n        pass\n\n@register_resource(\"server\")\nclass Server(BaseCloudResource):\n    # Inherits common functionality\n    pass\n</code></pre>"},{"location":"core-concepts/component-model/#testing-components","title":"Testing Components","text":"<pre><code>import pytest\nfrom pyvider.testing import ComponentTestCase\n\nclass TestServer(ComponentTestCase):\n    component_class = Server\n\n    async def test_create(self):\n        config = Server.Config(name=\"test\", size=\"small\")\n        state = await self.component.create(config)\n\n        assert state.id is not None\n        assert state.name == \"test\"\n        assert state.status == \"running\"\n</code></pre>"},{"location":"core-concepts/component-model/#related-documentation","title":"\ud83d\udd17 Related Documentation","text":"<ul> <li>Architecture Overview - System architecture</li> <li>Schema System - Schema definition and validation</li> <li>Creating Providers - Provider development guide</li> <li>Creating Resources - Resource implementation guide</li> </ul> <p>   Continue to Schema System \u2192 </p>"},{"location":"core-concepts/schema-system/","title":"Schema System","text":"<p>Note: This documentation is currently under development.</p> <p>PyVider's schema system provides a comprehensive way to define and validate Terraform provider schemas.</p>"},{"location":"core-concepts/schema-system/#overview","title":"Overview","text":"<p>The schema system includes: - Resource schemas - Data source schemas - Provider configuration schemas - Type validation and conversion</p>"},{"location":"core-concepts/schema-system/#coming-soon","title":"Coming Soon","text":"<ul> <li>Detailed schema definition syntax</li> <li>Validation rules and constraints</li> <li>Type system integration</li> <li>Best practices and examples</li> </ul> <p>For current schema documentation, see the API Reference.</p>"},{"location":"development/roadmap/","title":"Pyvider Roadmap","text":"<p>This document outlines planned features and future development directions for Pyvider.</p>"},{"location":"development/roadmap/#planned-cli-commands","title":"Planned CLI Commands","text":""},{"location":"development/roadmap/#pyvider-new","title":"<code>pyvider new</code>","text":"<p>Status: Planned Description: Scaffold a new provider project with recommended structure</p> <pre><code>pyvider new my-provider\n</code></pre> <p>This would create a new directory with: - <code>pyproject.toml</code> with pyvider dependency - Source directory structure (<code>src/my_provider/</code>) - Sample provider, resource, and data source implementations - Test structure with pytest configuration - README and documentation templates</p>"},{"location":"development/roadmap/#pyvider-build","title":"<code>pyvider build</code>","text":"<p>Status: Planned Description: Build and package provider for distribution</p> <pre><code>pyvider build --output terraform-provider-mycloud\n</code></pre> <p>This would: - Package the provider using Flavor - Create a distributable binary - Generate plugin metadata - Optionally sign the binary</p> <p>Note: Currently, use <code>python scripts/build_provider.py</code> as documented in CLAUDE.md</p>"},{"location":"development/roadmap/#pyvider-validate","title":"<code>pyvider validate</code>","text":"<p>Status: Planned Description: Validate provider schema and configuration</p> <pre><code>pyvider validate\npyvider validate --schema-only\npyvider validate --with-terraform\n</code></pre>"},{"location":"development/roadmap/#future-feature-phases","title":"Future Feature Phases","text":""},{"location":"development/roadmap/#phase-3-telemetry-monitoring-integration","title":"Phase 3: Telemetry &amp; Monitoring Integration","text":"<p>Leverage provide.foundation's telemetry capabilities for comprehensive observability:</p> <ul> <li>Resource Operations Monitoring</li> <li>Track plan/apply execution time and success rates</li> <li>Monitor read() performance for data sources</li> <li> <p>Track ephemeral resource lifecycle metrics</p> </li> <li> <p>Provider Operations</p> </li> <li>Monitor provider configuration latency</li> <li>Track initialization success/failure rates</li> <li> <p>Measure schema generation time</p> </li> <li> <p>Function Operations</p> </li> <li>Track function call frequency and latency</li> <li>Monitor validation performance</li> <li> <p>Measure serialization time</p> </li> <li> <p>Error Telemetry</p> </li> <li>Automatic error rate capture by exception type</li> <li>Error severity distribution tracking</li> <li>Error recovery success rates</li> <li> <p>Correlation IDs linking errors to requests</p> </li> <li> <p>Performance Monitoring</p> </li> <li>Handler execution metrics</li> <li>CTY conversion performance</li> <li>Hub operations tracking</li> <li> <p>P50/P95/P99 latency percentiles</p> </li> <li> <p>Operational Dashboards</p> </li> <li>Provider health scores</li> <li>Error rate trends</li> <li>Resource operation success rates</li> <li>Active gRPC connection monitoring</li> </ul>"},{"location":"development/roadmap/#phase-4-circuit-breaker-patterns","title":"Phase 4: Circuit Breaker Patterns","text":"<p>Add resilience patterns for external API calls:</p> <ul> <li>Circuit breaker implementation for provider operations</li> <li>Automatic failover strategies</li> <li>Rate limiting integration</li> <li>Request timeout configuration</li> <li>Retry policies with exponential backoff</li> </ul>"},{"location":"development/roadmap/#phase-5-advanced-error-recovery-strategies","title":"Phase 5: Advanced Error Recovery Strategies","text":"<p>Enhanced error handling and recovery:</p> <ul> <li>Automatic retry logic for transient failures</li> <li>Graceful degradation patterns</li> <li>Error context preservation across async boundaries</li> <li>Enhanced diagnostic generation</li> <li>Recovery state persistence</li> </ul>"},{"location":"development/roadmap/#phase-6-distributed-tracing-support","title":"Phase 6: Distributed Tracing Support","text":"<p>Full distributed tracing integration:</p> <ul> <li>OpenTelemetry integration</li> <li>Request flow tracking across components</li> <li>Performance bottleneck identification</li> <li>Cross-service correlation</li> <li>Trace sampling strategies</li> </ul>"},{"location":"development/roadmap/#phase-7-advanced-diagnostics","title":"Phase 7: Advanced Diagnostics","text":"<p>Enhanced diagnostic capabilities:</p> <ul> <li>Rich error messages with remediation suggestions</li> <li>Interactive debugging tools</li> <li>Provider health checks</li> <li>Schema validation diagnostics</li> <li>Terraform plan diff analysis</li> </ul>"},{"location":"development/roadmap/#phase-8-performance-optimization","title":"Phase 8: Performance Optimization","text":"<p>Optimization of critical paths:</p> <ul> <li>Schema caching strategies</li> <li>Connection pooling for provider clients</li> <li>Lazy loading of components</li> <li>Memory usage optimization</li> <li>Parallel resource operations</li> </ul>"},{"location":"development/roadmap/#enterprise-hardening-initiatives","title":"Enterprise Hardening Initiatives","text":""},{"location":"development/roadmap/#resource-lifecycle-cleanup","title":"Resource Lifecycle &amp; Cleanup","text":"<p>Prevent memory leaks and ensure graceful shutdown:</p> <ul> <li>Proper resource cleanup on provider shutdown</li> <li>Connection pool management</li> <li>File handle tracking and cleanup</li> <li>Memory leak detection and prevention</li> <li>Graceful shutdown with timeout handling</li> </ul>"},{"location":"development/roadmap/#input-validation-sanitization","title":"Input Validation &amp; Sanitization","text":"<p>Prevent DoS attacks and injection vulnerabilities:</p> <ul> <li>Comprehensive input validation</li> <li>Size limits on all inputs</li> <li>Rate limiting on expensive operations</li> <li>Sanitization of user-provided strings</li> <li>Path traversal prevention</li> </ul>"},{"location":"development/roadmap/#contributing-to-the-roadmap","title":"Contributing to the Roadmap","text":"<p>Have ideas for future Pyvider features? We'd love to hear them!</p> <ul> <li>Open an issue on GitHub with the <code>enhancement</code> label</li> <li>Discuss in our community Discord</li> <li>Submit a feature proposal PR to this roadmap</li> </ul> <p>Last Updated: 2025-10-24</p>"},{"location":"getting-started/installation/","title":"\ud83d\udce6 Installation Guide","text":"<p>This guide covers everything you need to install Pyvider and set up your development environment for building Terraform providers in Python.</p>"},{"location":"getting-started/installation/#system-requirements","title":"\ud83d\udea6 System Requirements","text":""},{"location":"getting-started/installation/#minimum-requirements","title":"Minimum Requirements","text":"<ul> <li>Python: 3.11 or higher</li> <li>Operating System: Linux, macOS, or Windows</li> <li>Memory: 2GB RAM minimum</li> <li>Disk Space: 500MB for Pyvider and dependencies</li> </ul>"},{"location":"getting-started/installation/#recommended-setup","title":"Recommended Setup","text":"<ul> <li>Python: 3.11+ (latest stable version)</li> <li>Package Manager: uv for fast dependency management</li> <li>IDE: VS Code or PyCharm with Python extensions</li> <li>Memory: 4GB+ RAM for comfortable development</li> </ul>"},{"location":"getting-started/installation/#installation-methods","title":"\ud83c\udfaf Installation Methods","text":""},{"location":"getting-started/installation/#method-1-using-pip-simple","title":"Method 1: Using pip (Simple)","text":"<p>The quickest way to get started with Pyvider:</p> <pre><code># Basic installation\npip install pyvider\n\n# With all optional dependencies\npip install pyvider[all]\n\n# For development (includes testing tools)\npip install pyvider[dev]\n</code></pre>"},{"location":"getting-started/installation/#method-2-using-uv-recommended","title":"Method 2: Using uv (Recommended)","text":"<p>uv is a blazing-fast Python package manager that we recommend for Pyvider development:</p> <pre><code># Install uv if you haven't already\ncurl -LsSf https://astral.sh/uv/install.sh | sh\n\n# Create a new project\nuv init my-provider\ncd my-provider\n\n# Add Pyvider as a dependency\nuv add pyvider\n\n# For development setup\nuv add --dev pytest pytest-asyncio pytest-cov mypy ruff\n</code></pre>"},{"location":"getting-started/installation/#method-3-using-poetry","title":"Method 3: Using Poetry","text":"<p>If you prefer Poetry for dependency management:</p> <pre><code># Create a new project\npoetry new my-provider\ncd my-provider\n\n# Add Pyvider\npoetry add pyvider\n\n# Add development dependencies\npoetry add --group dev pytest pytest-asyncio pytest-cov mypy ruff\n</code></pre>"},{"location":"getting-started/installation/#method-4-from-source-advanced","title":"Method 4: From Source (Advanced)","text":"<p>For contributing or testing the latest features:</p> <pre><code># Clone the repository\ngit clone https://github.com/provide-io/pyvider.git\ncd pyvider\n\n# Set up development environment\nuv sync  # On Windows: .\\env.ps1\n\n# Install in editable mode\npip install -e .\n\n# Run tests to verify installation\npytest\n</code></pre>"},{"location":"getting-started/installation/#environment-setup","title":"\ud83d\udd27 Environment Setup","text":""},{"location":"getting-started/installation/#1-virtual-environment-recommended","title":"1. Virtual Environment (Recommended)","text":"<p>Always use a virtual environment to avoid dependency conflicts:</p> <pre><code># Create virtual environment\npython -m venv .venv\n\n# Activate it\n# On Linux/macOS:\nsource .venv/bin/activate\n# On Windows:\n.venv\\Scripts\\activate\n\n# Install Pyvider\npip install pyvider\n</code></pre>"},{"location":"getting-started/installation/#2-development-environment-setup","title":"2. Development Environment Setup","text":"<p>For provider development, set up your project structure:</p> <pre><code># Create project structure\nmkdir -p my_provider/{provider,resources,data_sources,functions,tests}\ntouch my_provider/__init__.py\ntouch my_provider/provider/__init__.py\ntouch my_provider/resources/__init__.py\ntouch my_provider/data_sources/__init__.py\ntouch my_provider/functions/__init__.py\n</code></pre>"},{"location":"getting-started/installation/#3-environment-variables","title":"3. Environment Variables","text":"<p>Configure optional environment variables:</p> <pre><code># Enable debug logging\nexport PYVIDER_LOG_LEVEL=DEBUG\n\n# Set custom plugin directory\nexport PYVIDER_PLUGIN_DIR=~/.terraform.d/plugins\n\n# Enable performance profiling\nexport PYVIDER_PROFILE=1\n</code></pre>"},{"location":"getting-started/installation/#dependency-configuration","title":"\ud83d\udccb Dependency Configuration","text":""},{"location":"getting-started/installation/#pyprojecttoml-example","title":"pyproject.toml Example","text":"<p>Here's a complete <code>pyproject.toml</code> for a Pyvider provider project:</p> <pre><code>[project]\nname = \"my-terraform-provider\"\nversion = \"0.1.0\"\ndescription = \"A Terraform provider built with Pyvider\"\nreadme = \"README.md\"\nrequires-python = \"&gt;=3.11\"\ndependencies = [\n    \"pyvider&gt;=1.0.0\",\n    \"httpx&gt;=0.24.0\",  # For HTTP APIs\n    \"pydantic&gt;=2.0.0\",  # For validation\n]\n\n[project.optional-dependencies]\ndev = [\n    \"pytest&gt;=7.0.0\",\n    \"pytest-asyncio&gt;=0.21.0\",\n    \"pytest-cov&gt;=4.0.0\",\n    \"mypy&gt;=1.0.0\",\n    \"ruff&gt;=0.1.0\",\n    \"black&gt;=23.0.0\",\n]\n\n[build-system]\nrequires = [\"setuptools&gt;=65\", \"wheel\"]\nbuild-backend = \"setuptools.build_meta\"\n\n[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\nasyncio_mode = \"auto\"\naddopts = \"-v --cov=my_provider --cov-report=term-missing\"\n\n[tool.mypy]\npython_version = \"3.11\"\nstrict = true\nwarn_return_any = true\nwarn_unused_configs = true\n\n[tool.ruff]\nline-length = 100\ntarget-version = \"py311\"\nselect = [\"E\", \"F\", \"I\", \"N\", \"W\", \"B\", \"UP\"]\nignore = [\"E501\"]  # Line too long\n\n[tool.black]\nline-length = 100\ntarget-version = [\"py311\"]\n</code></pre>"},{"location":"getting-started/installation/#verification","title":"\u2705 Verification","text":"<p>After installation, verify everything is working:</p>"},{"location":"getting-started/installation/#1-check-pyvider-installation","title":"1. Check Pyvider Installation","text":"<pre><code># Verify Pyvider is installed\npython -c \"import pyvider; print(pyvider.__version__)\"\n\n# Check CLI availability\npyvider --version\n\n# List available commands\npyvider --help\n</code></pre>"},{"location":"getting-started/installation/#2-run-component-discovery-test","title":"2. Run Component Discovery Test","text":"<p>Create a simple test file to verify component discovery:</p> <pre><code># test_installation.py\nfrom pyvider import provider, resource\n\n@provider\nclass TestProvider:\n    \"\"\"Test provider to verify installation\"\"\"\n    metadata = {\"name\": \"test\", \"version\": \"0.1.0\"}\n\n@resource\nclass TestResource:\n    \"\"\"Test resource\"\"\"\n    pass\n\nif __name__ == \"__main__\":\n    from pyvider.hub import ComponentHub\n    hub = ComponentHub()\n    print(f\"Found {len(hub.providers)} provider(s)\")\n    print(f\"Found {len(hub.resources)} resource(s)\")\n</code></pre> <p>Run it:</p> <pre><code>python test_installation.py\n# Should output:\n# Found 1 provider(s)\n# Found 1 resource(s)\n</code></pre>"},{"location":"getting-started/installation/#3-test-cli-commands","title":"3. Test CLI Commands","text":"<pre><code># List discovered components\npyvider components list\n\n# Validate configuration\npyvider config validate\n\n# Show system information\npyvider info\n</code></pre>"},{"location":"getting-started/installation/#ide-setup","title":"\ud83d\udd0c IDE Setup","text":""},{"location":"getting-started/installation/#visual-studio-code","title":"Visual Studio Code","text":"<p>Install recommended extensions:</p> <pre><code>// .vscode/extensions.json\n{\n    \"recommendations\": [\n        \"ms-python.python\",\n        \"ms-python.vscode-pylance\",\n        \"charliermarsh.ruff\",\n        \"ms-python.mypy-type-checker\",\n        \"littlefoxteam.vscode-python-test-adapter\"\n    ]\n}\n</code></pre> <p>Configure settings:</p> <pre><code>// .vscode/settings.json\n{\n    \"python.linting.enabled\": true,\n    \"python.linting.ruffEnabled\": true,\n    \"python.formatting.provider\": \"black\",\n    \"python.testing.pytestEnabled\": true,\n    \"python.testing.unittestEnabled\": false,\n    \"[python]\": {\n        \"editor.formatOnSave\": true,\n        \"editor.codeActionsOnSave\": {\n            \"source.organizeImports\": true\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/installation/#pycharm","title":"PyCharm","text":"<ol> <li>Open Settings \u2192 Project \u2192 Python Interpreter</li> <li>Add Pyvider to your project interpreter</li> <li>Enable pytest as the test runner</li> <li>Configure mypy for type checking</li> <li>Set up ruff for linting</li> </ol>"},{"location":"getting-started/installation/#docker-setup-optional","title":"\ud83d\udc33 Docker Setup (Optional)","text":"<p>For containerized development:</p> <pre><code># Dockerfile\nFROM python:3.11-slim\n\nWORKDIR /app\n\n# Install system dependencies\nRUN apt-get update &amp;&amp; apt-get install -y \\\n    git \\\n    build-essential \\\n    &amp;&amp; rm -rf /var/lib/apt/lists/*\n\n# Install Pyvider\nRUN pip install --no-cache-dir pyvider[all]\n\n# Copy your provider code\nCOPY . .\n\n# Install provider dependencies\nRUN pip install --no-cache-dir -e .\n\n# Run provider\nCMD [\"pyvider\", \"provide\"]\n</code></pre> <p>Build and run:</p> <pre><code>docker build -t my-provider .\ndocker run -p 50051:50051 my-provider\n</code></pre>"},{"location":"getting-started/installation/#common-installation-issues","title":"\ud83d\udea8 Common Installation Issues","text":""},{"location":"getting-started/installation/#issue-python-version-too-old","title":"Issue: Python Version Too Old","text":"<pre><code>ERROR: pyvider requires Python 3.11 or higher\n</code></pre> <p>Solution: Upgrade Python or use pyenv to manage versions:</p> <pre><code># Install pyenv\ncurl https://pyenv.run | bash\n\n# Install Python 3.11\npyenv install 3.11.7\npyenv global 3.11.7\n</code></pre>"},{"location":"getting-started/installation/#issue-missing-build-tools","title":"Issue: Missing Build Tools","text":"<pre><code>ERROR: Microsoft Visual C++ 14.0 is required (Windows)\nERROR: error: Microsoft Visual C++ 14.0 or greater is required\n</code></pre> <p>Solution: Install build tools: - Windows: Install Visual Studio Build Tools - macOS: Install Xcode Command Line Tools: <code>xcode-select --install</code> - Linux: Install build-essential: <code>sudo apt-get install build-essential</code></p>"},{"location":"getting-started/installation/#issue-permission-denied","title":"Issue: Permission Denied","text":"<pre><code>ERROR: Could not install packages due to an EnvironmentError: [Errno 13] Permission denied\n</code></pre> <p>Solution: Use a virtual environment or install with <code>--user</code>:</p> <pre><code>pip install --user pyvider\n</code></pre>"},{"location":"getting-started/installation/#issue-conflicting-dependencies","title":"Issue: Conflicting Dependencies","text":"<pre><code>ERROR: pip's dependency resolver does not currently take into account all the packages that are installed\n</code></pre> <p>Solution: Use uv or create a fresh virtual environment:</p> <pre><code># With uv\nuv venv\nuv pip install pyvider\n\n# With standard venv\npython -m venv fresh_env\nsource fresh_env/bin/activate\npip install pyvider\n</code></pre>"},{"location":"getting-started/installation/#upgrading-pyvider","title":"\ud83d\udd04 Upgrading Pyvider","text":"<p>Keep Pyvider up to date for the latest features and fixes:</p> <pre><code># Using pip\npip install --upgrade pyvider\n\n# Using uv\nuv add --upgrade pyvider\n\n# Using poetry\npoetry update pyvider\n\n# Check current version\npyvider --version\n</code></pre>"},{"location":"getting-started/installation/#next-steps","title":"\ud83d\udcda Next Steps","text":"<p>Now that you have Pyvider installed:</p> <ol> <li>Quick Start Guide - Build your first provider in 5 minutes</li> <li>First Provider Tutorial - Comprehensive walkthrough</li> <li>Architecture Overview - Understand how Pyvider works</li> <li>API Reference - Explore the schema API</li> </ol>"},{"location":"getting-started/installation/#getting-help","title":"\ud83d\udcac Getting Help","text":"<p>If you encounter issues during installation:</p> <ul> <li>Check the Troubleshooting Guide</li> <li>Search GitHub Issues</li> <li>Ask in our Discord Community</li> <li>Post on Stack Overflow with tag <code>pyvider</code></li> </ul> <p>   Ready to build? Continue to the Quick Start Guide \u2192 </p>"},{"location":"getting-started/quick-start/","title":"\ud83d\ude80 Quick Start Guide","text":"<p>Build your first Terraform provider in Python in just 5 minutes! This guide will walk you through creating a simple but functional provider that manages local files.</p>"},{"location":"getting-started/quick-start/#prerequisites","title":"\ud83d\udccb Prerequisites","text":"<p>Before starting, ensure you have:</p> <ul> <li>\u2705 Python 3.11+ installed</li> <li>\u2705 Pyvider installed (<code>pip install pyvider</code>)</li> <li>\u2705 Terraform installed (for testing)</li> <li>\u2705 5 minutes of your time!</li> </ul>"},{"location":"getting-started/quick-start/#what-well-build","title":"\ud83c\udfaf What We'll Build","text":"<p>We'll create a LocalFile Provider that can: - Create text files on your local filesystem - Read file contents as data sources - Update files when content changes - Delete files when resources are destroyed</p>"},{"location":"getting-started/quick-start/#step-1-create-the-provider","title":"\ud83d\udcdd Step 1: Create the Provider","text":"<p>Create a new file called <code>local_provider.py</code>:</p> <pre><code>#!/usr/bin/env python3\n\"\"\"\nLocal File Provider - A simple Terraform provider for managing local files.\n\"\"\"\n\nfrom pathlib import Path\nimport hashlib\nimport attrs\nfrom pyvider.providers import register_provider, BaseProvider, ProviderMetadata\nfrom pyvider.resources import register_resource, BaseResource\nfrom pyvider.data_sources import register_data_source, BaseDataSource\nfrom pyvider.schema import Attribute\n\n# ============================================\n# PROVIDER DEFINITION\n# ============================================\n\n@register_provider(\"local\")\nclass LocalProvider(BaseProvider):\n    \"\"\"Provider for managing local files.\"\"\"\n\n    def __init__(self):\n        super().__init__(\n            metadata=ProviderMetadata(\n                name=\"local\",\n                version=\"0.1.0\",\n                protocol_version=\"6\"\n            )\n        )\n\n    @attrs.define\n    class Config:\n        \"\"\"Provider configuration.\"\"\"\n        base_directory: str = Attribute(\n            default=\".\",\n            description=\"Base directory for file operations\"\n        )\n        create_directories: bool = Attribute(\n            default=True,\n            description=\"Automatically create parent directories\"\n        )\n\n# ============================================\n# FILE RESOURCE\n# ============================================\n\n@register_resource(\"file\")\nclass File(BaseResource):\n    \"\"\"Manages a local text file.\"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"File resource configuration.\"\"\"\n        path: str = Attribute(\n            required=True,\n            description=\"Path to the file (relative to base directory)\"\n        )\n        content: str = Attribute(\n            required=True,\n            description=\"Content to write to the file\"\n        )\n        permissions: str = Attribute(\n            default=\"644\",\n            description=\"File permissions (octal notation)\"\n        )\n\n    @attrs.define\n    class State:\n        \"\"\"File resource state.\"\"\"\n        id: str = Attribute(\n            computed=True,\n            description=\"Unique identifier for the file\"\n        )\n        path: str = Attribute(\n            description=\"Absolute path to the file\"\n        )\n        content: str = Attribute(\n            description=\"Current file content\"\n        )\n        permissions: str = Attribute(\n            description=\"Current file permissions\"\n        )\n        checksum: str = Attribute(\n            computed=True,\n            description=\"SHA256 checksum of the content\"\n        )\n        size: int = Attribute(\n            computed=True,\n            description=\"File size in bytes\"\n        )\n\n    async def create(self, config: Config) -&gt; State:\n        \"\"\"Create a new file.\"\"\"\n        # Get provider configuration\n        provider_config = self.provider.config\n        base_dir = Path(provider_config.base_directory)\n\n        # Construct full path\n        file_path = base_dir / config.path\n\n        # Create parent directories if needed\n        if provider_config.create_directories:\n            file_path.parent.mkdir(parents=True, exist_ok=True)\n\n        # Write the file\n        file_path.write_text(config.content)\n\n        # Set permissions\n        octal_perms = int(config.permissions, 8)\n        file_path.chmod(octal_perms)\n\n        # Calculate checksum\n        checksum = hashlib.sha256(config.content.encode()).hexdigest()\n\n        # Return state\n        return State(\n            id=str(file_path.absolute()),\n            path=str(file_path.absolute()),\n            content=config.content,\n            permissions=config.permissions,\n            checksum=checksum,\n            size=len(config.content)\n        )\n\n    async def read(self, state: State) -&gt; State | None:\n        \"\"\"Read the current state of the file.\"\"\"\n        file_path = Path(state.path)\n\n        # Check if file exists\n        if not file_path.exists():\n            return None  # File was deleted outside of Terraform\n\n        # Read current content\n        content = file_path.read_text()\n\n        # Get current permissions\n        mode = file_path.stat().st_mode\n        permissions = oct(mode)[-3:]\n\n        # Calculate checksum\n        checksum = hashlib.sha256(content.encode()).hexdigest()\n\n        # Return updated state\n        return State(\n            id=state.id,\n            path=state.path,\n            content=content,\n            permissions=permissions,\n            checksum=checksum,\n            size=len(content)\n        )\n\n    async def update(self, config: Config, state: State) -&gt; State:\n        \"\"\"Update an existing file.\"\"\"\n        file_path = Path(state.path)\n\n        # Update content if changed\n        if config.content != state.content:\n            file_path.write_text(config.content)\n\n        # Update permissions if changed\n        if config.permissions != state.permissions:\n            octal_perms = int(config.permissions, 8)\n            file_path.chmod(octal_perms)\n\n        # Calculate new checksum\n        checksum = hashlib.sha256(config.content.encode()).hexdigest()\n\n        # Return updated state\n        return State(\n            id=state.id,\n            path=state.path,\n            content=config.content,\n            permissions=config.permissions,\n            checksum=checksum,\n            size=len(config.content)\n        )\n\n    async def delete(self, state: State) -&gt; None:\n        \"\"\"Delete the file.\"\"\"\n        file_path = Path(state.path)\n        if file_path.exists():\n            file_path.unlink()\n\n# ============================================\n# FILE DATA SOURCE\n# ============================================\n\n@register_data_source(\"file_content\")\nclass FileContent(BaseDataSource):\n    \"\"\"Reads content from an existing file.\"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Data source configuration.\"\"\"\n        path: str = Attribute(\n            required=True,\n            description=\"Path to the file to read\"\n        )\n\n    @attrs.define\n    class State:\n        \"\"\"Data source state.\"\"\"\n        id: str = Attribute(\n            computed=True,\n            description=\"File path as ID\"\n        )\n        content: str = Attribute(\n            computed=True,\n            description=\"File content\"\n        )\n        size: int = Attribute(\n            computed=True,\n            description=\"File size in bytes\"\n        )\n        checksum: str = Attribute(\n            computed=True,\n            description=\"SHA256 checksum\"\n        )\n        exists: bool = Attribute(\n            computed=True,\n            description=\"Whether the file exists\"\n        )\n\n    async def read(self, config: Config) -&gt; State:\n        \"\"\"Read file content.\"\"\"\n        provider_config = self.provider.config\n        base_dir = Path(provider_config.base_directory)\n        file_path = base_dir / config.path\n\n        if file_path.exists():\n            content = file_path.read_text()\n            checksum = hashlib.sha256(content.encode()).hexdigest()\n            return State(\n                id=str(file_path.absolute()),\n                content=content,\n                size=len(content),\n                checksum=checksum,\n                exists=True\n            )\n        else:\n            return State(\n                id=str(file_path.absolute()),\n                content=\"\",\n                size=0,\n                checksum=\"\",\n                exists=False\n            )\n\n# ============================================\n# MAIN ENTRY POINT\n# ============================================\n\nif __name__ == \"__main__\":\n    # This allows the provider to be run directly\n    from pyvider.cli import main\n    main()\n</code></pre>"},{"location":"getting-started/quick-start/#step-2-test-the-provider-locally","title":"\ud83e\uddea Step 2: Test the Provider Locally","text":"<p>Create a test file <code>test_provider.py</code>:</p> <pre><code>#!/usr/bin/env python3\n\"\"\"Test the local file provider.\"\"\"\n\nimport asyncio\nfrom pathlib import Path\nimport tempfile\nimport shutil\n\nasync def test_provider():\n    \"\"\"Test provider operations.\"\"\"\n    # Create a temporary directory for testing\n    with tempfile.TemporaryDirectory() as tmpdir:\n        print(f\"\ud83d\udcc1 Testing in: {tmpdir}\")\n\n        # Import our provider components\n        from local_provider import LocalProvider, File, FileContent\n\n        # Create provider instance\n        provider = LocalProvider()\n        provider.config = LocalProvider.Config(\n            base_directory=tmpdir,\n            create_directories=True\n        )\n\n        # Test creating a file\n        print(\"\\n\u2705 Testing file creation...\")\n        file_resource = File()\n        file_resource.provider = provider\n\n        config = File.Config(\n            path=\"test/hello.txt\",\n            content=\"Hello, Terraform!\",\n            permissions=\"644\"\n        )\n\n        state = await file_resource.create(config)\n        print(f\"  Created: {state.path}\")\n        print(f\"  Content: {state.content}\")\n        print(f\"  Checksum: {state.checksum}\")\n\n        # Verify file exists\n        actual_path = Path(tmpdir) / \"test/hello.txt\"\n        assert actual_path.exists()\n        assert actual_path.read_text() == \"Hello, Terraform!\"\n\n        # Test reading the file\n        print(\"\\n\ud83d\udcd6 Testing file read...\")\n        read_state = await file_resource.read(state)\n        assert read_state is not None\n        assert read_state.content == \"Hello, Terraform!\"\n\n        # Test updating the file\n        print(\"\\n\ud83d\udd04 Testing file update...\")\n        new_config = File.Config(\n            path=\"test/hello.txt\",\n            content=\"Updated content!\",\n            permissions=\"600\"\n        )\n\n        updated_state = await file_resource.update(new_config, state)\n        assert updated_state.content == \"Updated content!\"\n        assert actual_path.read_text() == \"Updated content!\"\n\n        # Test data source\n        print(\"\\n\ud83d\udcca Testing data source...\")\n        data_source = FileContent()\n        data_source.provider = provider\n\n        ds_config = FileContent.Config(path=\"test/hello.txt\")\n        ds_state = await data_source.read(ds_config)\n        assert ds_state.exists\n        assert ds_state.content == \"Updated content!\"\n\n        # Test deletion\n        print(\"\\n\ud83d\uddd1\ufe0f Testing file deletion...\")\n        await file_resource.delete(updated_state)\n        assert not actual_path.exists()\n\n        print(\"\\n\u2728 All tests passed!\")\n\nif __name__ == \"__main__\":\n    asyncio.run(test_provider())\n</code></pre> <p>Run the test:</p> <pre><code>python test_provider.py\n</code></pre> <p>You should see: <pre><code>\ud83d\udcc1 Testing in: /tmp/tmp_xyz123\n\n\u2705 Testing file creation...\n  Created: /tmp/tmp_xyz123/test/hello.txt\n  Content: Hello, Terraform!\n  Checksum: 3b7e72f9c8a5d4e2f1a6b8c9d0e3f4g5h6i7j8k9\n\n\ud83d\udcd6 Testing file read...\n\n\ud83d\udd04 Testing file update...\n\n\ud83d\udcca Testing data source...\n\n\ud83d\uddd1\ufe0f Testing file deletion...\n\n\u2728 All tests passed!\n</code></pre></p>"},{"location":"getting-started/quick-start/#step-3-use-with-terraform","title":"\ud83d\udd27 Step 3: Use with Terraform","text":"<p>Create a Terraform configuration <code>main.tf</code>:</p> <pre><code>terraform {\n  required_providers {\n    local = {\n      source = \"example.com/tutorial/local\"\n      version = \"0.1.0\"\n    }\n  }\n}\n\n# Configure the provider\nprovider \"local\" {\n  base_directory     = \"./managed_files\"\n  create_directories = true\n}\n\n# Create a file resource\nresource \"local_file\" \"config\" {\n  path    = \"config/app.conf\"\n  content = &lt;&lt;-EOT\n    # Application Configuration\n    app_name = \"MyApp\"\n    version = \"1.0.0\"\n    debug = false\n  EOT\n  permissions = \"644\"\n}\n\n# Create another file that references the first\nresource \"local_file\" \"readme\" {\n  path    = \"README.md\"\n  content = &lt;&lt;-EOT\n    # My Application\n\n    Configuration file: ${local_file.config.path}\n    Checksum: ${local_file.config.checksum}\n    Size: ${local_file.config.size} bytes\n  EOT\n}\n\n# Read an existing file\ndata \"local_file_content\" \"license\" {\n  path = \"../LICENSE\"\n}\n\n# Output values\noutput \"config_checksum\" {\n  value = local_file.config.checksum\n}\n\noutput \"license_exists\" {\n  value = data.local_file_content.license.exists\n}\n\noutput \"files_created\" {\n  value = [\n    local_file.config.path,\n    local_file.readme.path\n  ]\n}\n</code></pre>"},{"location":"getting-started/quick-start/#step-4-package-and-run-the-provider","title":"\ud83d\ude80 Step 4: Package and Run the Provider","text":""},{"location":"getting-started/quick-start/#option-1-development-mode","title":"Option 1: Development Mode","text":"<p>For development, run the provider directly:</p> <pre><code># Start the provider in development mode\npyvider provide --debug\n\n# In another terminal, run Terraform\nterraform init -upgrade\nterraform plan\nterraform apply\n</code></pre>"},{"location":"getting-started/quick-start/#option-2-build-and-install","title":"Option 2: Build and Install","text":"<p>Package the provider for distribution using the Flavor build system:</p> <pre><code># Build the provider binary using the build script\n# (See CLAUDE.md and scripts/build_provider.py for details)\npython scripts/build_provider.py\n\n# The built provider binary will be in the dist/ directory\n# Move to Terraform plugin directory\nmkdir -p ~/.terraform.d/plugins/example.com/tutorial/local/0.1.0/linux_amd64\ncp dist/terraform-provider-local ~/.terraform.d/plugins/example.com/tutorial/local/0.1.0/linux_amd64/\n\n# Now Terraform can find it\nterraform init\nterraform apply\n</code></pre>"},{"location":"getting-started/quick-start/#step-5-verify-results","title":"\ud83d\udcca Step 5: Verify Results","text":"<p>After running <code>terraform apply</code>, you should see:</p> <pre><code>Apply complete! Resources: 2 added, 0 changed, 0 destroyed.\n\nOutputs:\n\nconfig_checksum = \"a3f5c7d9e1b3...\"\nlicense_exists = true\nfiles_created = [\n  \"./managed_files/config/app.conf\",\n  \"./managed_files/README.md\"\n]\n</code></pre> <p>Check the created files:</p> <pre><code>$ tree managed_files/\nmanaged_files/\n\u251c\u2500\u2500 config/\n\u2502   \u2514\u2500\u2500 app.conf\n\u2514\u2500\u2500 README.md\n\n$ cat managed_files/config/app.conf\n# Application Configuration\napp_name = \"MyApp\"\nversion = \"1.0.0\"\ndebug = false\n</code></pre>"},{"location":"getting-started/quick-start/#congratulations","title":"\ud83c\udf89 Congratulations!","text":"<p>You've just built your first Terraform provider in Python! In just a few minutes, you've:</p> <ul> <li>\u2705 Created a complete provider with configuration</li> <li>\u2705 Implemented a full CRUD resource (File)</li> <li>\u2705 Added a data source (FileContent)</li> <li>\u2705 Tested the provider locally</li> <li>\u2705 Used it with real Terraform configuration</li> </ul>"},{"location":"getting-started/quick-start/#whats-happening-behind-the-scenes","title":"\ud83d\udd0d What's Happening Behind the Scenes?","text":"<p>When you run your provider, Pyvider:</p> <ol> <li>Discovers Components: Finds all <code>@provider</code>, <code>@resource</code>, and <code>@data_source</code> decorators</li> <li>Generates Schema: Converts Python types to Terraform schema</li> <li>Handles Protocol: Manages all gRPC communication with Terraform</li> <li>Manages State: Tracks resource state between operations</li> <li>Provides Type Safety: Ensures data matches your type definitions</li> </ol>"},{"location":"getting-started/quick-start/#key-concepts-demonstrated","title":"\ud83d\udcda Key Concepts Demonstrated","text":""},{"location":"getting-started/quick-start/#decorators","title":"\ud83c\udfaf Decorators","text":"<ul> <li><code>@provider</code>: Registers your provider class</li> <li><code>@resource</code>: Defines a manageable resource</li> <li><code>@data_source</code>: Creates a read-only data source</li> </ul>"},{"location":"getting-started/quick-start/#schema-definition","title":"\ud83d\udccb Schema Definition","text":"<ul> <li><code>@attrs.define</code>: Creates type-safe configuration classes</li> <li><code>Attribute()</code>: Defines schema fields with validation</li> <li><code>computed=True</code>: Fields calculated by the provider</li> <li><code>required=True</code>: Fields that must be provided</li> </ul>"},{"location":"getting-started/quick-start/#resource-lifecycle","title":"\ud83d\udd04 Resource Lifecycle","text":"<ul> <li><code>create()</code>: Called when resource is first created</li> <li><code>read()</code>: Refreshes resource state</li> <li><code>update()</code>: Modifies existing resource</li> <li><code>delete()</code>: Removes resource</li> </ul>"},{"location":"getting-started/quick-start/#next-steps","title":"\ud83d\udea6 Next Steps","text":"<p>Now that you understand the basics:</p>"},{"location":"getting-started/quick-start/#1-enhance-the-provider","title":"1. Enhance the Provider","text":"<p>Add more features to your local file provider:</p> <pre><code>@resource\nclass Directory:\n    \"\"\"Manages a local directory.\"\"\"\n    # Implementation here\n\n@function\nclass HashFile:\n    \"\"\"Computes hash of a file.\"\"\"\n    # Implementation here\n</code></pre>"},{"location":"getting-started/quick-start/#2-add-error-handling","title":"2. Add Error Handling","text":"<pre><code>async def create(self, config: Config) -&gt; State:\n    try:\n        # ... file operations ...\n    except PermissionError:\n        raise ResourceError(\"Insufficient permissions\")\n    except OSError as e:\n        raise ResourceError(f\"Failed to create file: {e}\")\n</code></pre>"},{"location":"getting-started/quick-start/#3-add-validation","title":"3. Add Validation","text":"<pre><code>@attrs.define\nclass Config:\n    path: str = Attribute(\n        required=True,\n        validators=[\n            lambda x: not x.startswith(\"/\"),  # No absolute paths\n            lambda x: \"..\" not in x,  # No parent directory access\n        ]\n    )\n</code></pre>"},{"location":"getting-started/quick-start/#4-add-import-support","title":"4. Add Import Support","text":"<pre><code>async def import_resource(self, resource_id: str) -&gt; State:\n    \"\"\"Import existing file into Terraform state.\"\"\"\n    file_path = Path(resource_id)\n    if not file_path.exists():\n        raise ResourceError(f\"File not found: {resource_id}\")\n\n    content = file_path.read_text()\n    # ... return state ...\n</code></pre>"},{"location":"getting-started/quick-start/#learn-more","title":"\ud83d\udcd6 Learn More","text":"<p>Ready to dive deeper? Check out:</p> <ul> <li>First Provider Tutorial - Complete walkthrough with advanced features</li> <li>Architecture Guide - Understand Pyvider's internals</li> <li>Schema System - Master schema definition</li> <li>Testing Guide - Write comprehensive tests</li> </ul>"},{"location":"getting-started/quick-start/#tips-for-success","title":"\ud83d\udca1 Tips for Success","text":"<ol> <li>Start Simple: Begin with basic resources before adding complexity</li> <li>Test Early: Write tests as you develop</li> <li>Use Type Hints: Leverage Python's type system for safety</li> <li>Handle Errors: Provide clear error messages for users</li> <li>Document Well: Add docstrings to all components</li> </ol>"},{"location":"getting-started/quick-start/#getting-help","title":"\ud83c\udd98 Getting Help","text":"<p>If you run into issues:</p> <ul> <li>Check the Troubleshooting Guide</li> <li>Search GitHub Issues</li> <li>Join our Discord Community</li> <li>Ask on Stack Overflow</li> </ul> <p>   \ud83c\udf8a Congratulations on building your first provider! \ud83c\udf8a   Ready for more? Continue to the Complete Tutorial \u2192 </p>"},{"location":"getting-started/what-is-pyvider/","title":"\ud83d\udc0d Welcome to Pyvider!","text":"<p>Have you ever wanted to manage your infrastructure with the elegance and power of Python? Have you ever dreamed of creating your own Terraform providers, but were intimidated by the complexity of the plugin protocol?</p> <p>If so, you've come to the right place.</p> <p>Pyvider is a framework for building Terraform providers in Python. It's designed to be simple, flexible, and fun to use. With <code>pyvider</code>, you can create custom providers for your own APIs, services, and devices, and manage them with the same tools and workflows you use for the rest of your infrastructure.</p>"},{"location":"getting-started/what-is-pyvider/#who-is-this-for","title":"\ud83e\udd14 Who is this for?","text":"<ul> <li>You're a Python developer who wants to get into the world of Infrastructure as Code.</li> <li>You're a DevOps engineer who wants to automate the management of your custom infrastructure.</li> <li>You're a platform engineer who wants to build a library of reusable infrastructure components for your team.</li> <li>You're a dreamer, a builder, a creator who wants to bring your ideas to life.</li> </ul>"},{"location":"getting-started/what-is-pyvider/#our-philosophy","title":"\ud83d\udc96 Our Philosophy","text":"<p>We believe that building infrastructure should be a creative and empowering experience. We believe that the tools we use should be simple, elegant, and fun to use. We believe that everyone should have the power to create their own infrastructure, and we're here to help you do it.</p> <p>We're not just building a framework; we're building a community. We're here to support you on your journey, and we're excited to see what you create.</p>"},{"location":"guides/advanced-examples/","title":"\ud83d\ude80 Advanced Examples","text":"<p>This guide provides advanced examples of how to use <code>pyvider</code>.</p>"},{"location":"guides/advanced-examples/#composing-resources-and-functions","title":"\ud83e\udde9 Composing Resources and Functions","text":"<p>Here is an example of how to compose resources and functions to create a more complex infrastructure. You can find the full, testable example in the <code>examples/04-advanced-composition</code> directory.</p> <pre><code>resource \"pyvider_file_content\" \"example\" {\n  path    = \"hello.txt\"\n  content = pyvider_upper(\"hello world\")\n}\n\ndata \"pyvider_file_info\" \"example\" {\n  path = pyvider_file_content.example.path\n}\n\noutput \"file_info\" {\n  value = data.pyvider_file_info.example\n}\n</code></pre> <p>This example creates a new resource of type <code>pyvider_file_content</code> with the name <code>example</code>. The <code>content</code> attribute is set to the output of the <code>pyvider_upper</code> function. The output of the <code>pyvider_file_content</code> resource is then used to configure the <code>path</code> attribute of the <code>pyvider_file_info</code> data source.</p>"},{"location":"guides/advanced-usage/","title":"\ud83d\ude80 Advanced Usage","text":"<p>This guide provides advanced examples of how to use <code>pyvider</code>.</p>"},{"location":"guides/advanced-usage/#depends_on","title":"<code>depends_on</code>","text":"<p>The <code>depends_on</code> meta-argument is used to create explicit dependencies between resources. This is useful when you have a resource that depends on another resource, but there is no explicit reference to the other resource in the configuration.</p> <pre><code>resource \"pyvider_null_resource\" \"a\" {\n}\n\nresource \"pyvider_null_resource\" \"b\" {\n  depends_on = [pyvider_null_resource.a]\n}\n</code></pre> <p>In this example, the <code>pyvider_null_resource</code> <code>b</code> depends on the <code>pyvider_null_resource</code> <code>a</code>. This means that <code>a</code> will be created before <code>b</code>.</p>"},{"location":"guides/advanced-usage/#count","title":"<code>count</code>","text":"<p>The <code>count</code> meta-argument is used to create multiple instances of a resource.</p> <pre><code>resource \"pyvider_null_resource\" \"example\" {\n  count = 3\n}\n</code></pre> <p>In this example, three instances of the <code>pyvider_null_resource</code> will be created.</p>"},{"location":"guides/advanced-usage/#for_each","title":"<code>for_each</code>","text":"<p>The <code>for_each</code> meta-argument is used to create multiple instances of a resource based on a map or a set of strings.</p> <pre><code>resource \"pyvider_null_resource\" \"example\" {\n  for_each = toset([\"a\", \"b\", \"c\"])\n}\n</code></pre> <p>In this example, three instances of the <code>pyvider_null_resource</code> will be created, one for each element in the set. ```</p>"},{"location":"guides/best-practices/","title":"Best Practices","text":"<p>This guide provides best practices for developing <code>pyvider</code> providers.</p>"},{"location":"guides/best-practices/#error-handling-and-diagnostics","title":"Error Handling and Diagnostics","text":"<p><code>pyvider</code> includes a robust error handling and diagnostics system. You should use this system to provide clear and actionable error messages to the user.</p>"},{"location":"guides/best-practices/#logging","title":"Logging","text":"<p><code>pyvider</code> includes a logging system that you can use to log information about your provider.</p>"},{"location":"guides/best-practices/#performance-considerations","title":"Performance Considerations","text":"<p>You should consider the performance of your provider when you are developing it. You should try to minimize the number of API calls and the amount of data that you transfer.</p>"},{"location":"guides/configuration/","title":"Configuration","text":""},{"location":"guides/configuration/#the-magic-words-configuration","title":"\ud83d\udcdc The Magic Words: Configuration","text":"<p>Now that we have our provider installed, it's time to learn the magic words that will bring it to life. We'll start by configuring the provider in our Terraform configuration file.</p> <p>Create a new file called <code>main.tf</code> and add the following:</p> <pre><code>terraform {\n  required_providers {\n    \"my-provider\" = {\n      source  = \"local/providers/my-provider\"\n      version = \"0.1.0\"\n    }\n  }\n}\n\nprovider \"my-provider\" {\n  # Our journey begins here...\n}\n</code></pre> <p>This tells Terraform that we want to use our custom provider. Now, let's add some real configuration.</p>"},{"location":"guides/configuration/#unlocking-the-gates-authentication","title":"\ud83d\udd11 Unlocking the Gates: Authentication","text":"<p>Most providers need to authenticate with an external API or service. <code>pyvider</code> provides a capability system that makes it easy to add authentication to your provider. We'll learn more about capabilities in a later chapter, but for now, let's assume our provider needs an API key.</p> <p>We can add the API key to our provider configuration like this:</p> <pre><code>provider \"my-provider\" {\n  api_key = \"my-super-secret-api-key\"\n}\n</code></pre> <p>And with that, we've unlocked the gates to our provider's power. In the next section, we'll learn how to create our first resource.</p>"},{"location":"guides/creating-data-sources/","title":"\ud83d\udcca Creating a Data Source","text":"<p>This guide will help you create a new data source for your <code>pyvider</code> provider.</p>"},{"location":"guides/creating-data-sources/#defining-the-data-source-schema","title":"\ud83d\udcc4 Defining the Data Source Schema","text":"<p>The data source schema defines the configuration options for the data source. You can define the data source schema in a new file in the <code>data_sources</code> directory, using the <code>@data_source</code> decorator.</p> <pre><code>from pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass MyDataSource(DataSourceConfig):\n    \"\"\"\n    MyDataSource is a custom data source that does amazing things.\n    \"\"\"\n    name: str\n</code></pre> <p>In this example, we define a data source with a single configuration option, <code>name</code>.</p>"},{"location":"guides/creating-data-sources/#implementing-the-data-source-logic","title":"\ud83e\udde0 Implementing the Data Source Logic","text":"<p>The data source logic is implemented in the same file as the data source schema. The data source logic is responsible for reading the data from the external API or service.</p> <pre><code>from pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass MyDataSource(DataSourceConfig):\n    \"\"\"\n    MyDataSource is a custom data source that does amazing things.\n    \"\"\"\n    name: str\n\n    def read(self, ctx):\n        # Read the data from the external API or service.\n        return {\n            \"id\": \"my-id\",\n            \"name\": self.name,\n            \"output\": \"my-output\",\n        }\n</code></pre> <p>In this example, we implement the <code>read</code> method, which is called by <code>pyvider</code> to read the data from the data source.</p>"},{"location":"guides/creating-functions/","title":"\ud83e\uddee Creating a Function","text":"<p>This guide will help you create a new function for your <code>pyvider</code> provider.</p>"},{"location":"guides/creating-functions/#defining-the-function-schema","title":"\ud83d\udcc4 Defining the Function Schema","text":"<p>The function schema defines the arguments and return value for the function. You can define the function schema in a new file in the <code>functions</code> directory, using the <code>@function</code> decorator.</p> <pre><code>from pyvider.functions import function\n\n@function\ndef my_function(input: str) -&gt; str:\n    \"\"\"\n    MyFunction is a custom function that does amazing things.\n    \"\"\"\n    return input.upper()\n</code></pre> <p>In this example, we define a function with a single string argument and a string return value.</p>"},{"location":"guides/creating-functions/#implementing-the-function-logic","title":"\ud83e\udde0 Implementing the Function Logic","text":"<p>The function logic is implemented in the same file as the function schema. The function logic is responsible for performing the custom logic.</p> <pre><code>from pyvider.functions import function\n\n@function\ndef my_function(input: str) -&gt; str:\n    \"\"\"\n    MyFunction is a custom function that does amazing things.\n    \"\"\"\n    return input.upper()\n</code></pre> <p>In this example, we implement the function logic directly in the function body.</p>"},{"location":"guides/creating-providers/","title":"\ud83c\udfd7\ufe0f Creating Providers","text":"<p>This comprehensive guide walks you through creating production-ready Terraform providers using Pyvider. We'll cover everything from basic setup to advanced features.</p>"},{"location":"guides/creating-providers/#prerequisites","title":"\ud83d\udccb Prerequisites","text":"<p>Before creating a provider, ensure you understand: - Basic Terraform concepts (providers, resources, state) - Python async/await programming - attrs for class definitions - Type hints in Python</p>"},{"location":"guides/creating-providers/#provider-anatomy","title":"\ud83c\udfaf Provider Anatomy","text":"<p>A complete provider consists of several components:</p> <pre><code>my_provider/\n\u251c\u2500\u2500 __init__.py           # Package initialization\n\u251c\u2500\u2500 provider.py           # Provider definition\n\u251c\u2500\u2500 resources/           # Resource implementations\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u251c\u2500\u2500 server.py\n\u2502   \u2514\u2500\u2500 network.py\n\u251c\u2500\u2500 data_sources/        # Data source implementations\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u2514\u2500\u2500 images.py\n\u251c\u2500\u2500 functions/           # Provider functions\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u2514\u2500\u2500 validators.py\n\u2514\u2500\u2500 tests/              # Test suite\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 test_provider.py\n</code></pre>"},{"location":"guides/creating-providers/#step-by-step-provider-creation","title":"\ud83d\ude80 Step-by-Step Provider Creation","text":""},{"location":"guides/creating-providers/#step-1-define-the-provider-class","title":"Step 1: Define the Provider Class","text":"<pre><code># provider.py\nfrom pyvider.providers import register_provider, BaseProvider, ProviderMetadata\nfrom pyvider.schema import Attribute\nimport attrs\nimport httpx\n\n@register_provider(\"mycloud\")\nclass MyCloudProvider(BaseProvider):\n    \"\"\"\n    MyCloud Infrastructure Provider\n\n    This provider manages resources in the MyCloud platform.\n    \"\"\"\n\n    def __init__(self):\n        \"\"\"Initialize provider with metadata.\"\"\"\n        super().__init__(\n            metadata=ProviderMetadata(\n                name=\"mycloud\",\n                version=\"1.0.0\",\n                protocol_version=\"6\"\n            )\n        )\n        self.client = None\n\n    @attrs.define\n    class Config:\n        \"\"\"Provider configuration schema.\"\"\"\n\n        # Required authentication\n        api_key: str = Attribute(\n            required=True,\n            sensitive=True,\n            description=\"API key for MyCloud authentication\"\n        )\n\n        # Optional configuration\n        api_endpoint: str = Attribute(\n            default=\"https://api.mycloud.com/v1\",\n            description=\"MyCloud API endpoint\"\n        )\n\n        region: str = Attribute(\n            default=\"us-east-1\",\n            description=\"Default region for resources\",\n            validators=[\n                OneOf([\"us-east-1\", \"us-west-2\", \"eu-central-1\"])\n            ]\n        )\n\n        timeout: int = Attribute(\n            default=30,\n            description=\"API request timeout in seconds\",\n            validators=[Range(min=5, max=300)]\n        )\n\n        max_retries: int = Attribute(\n            default=3,\n            description=\"Maximum API retry attempts\",\n            validators=[Range(min=0, max=10)]\n        )\n\n    async def configure(self, config: Config) -&gt; None:\n        \"\"\"\n        Configure the provider with validated configuration.\n\n        This method is called once after provider initialization\n        with the configuration from Terraform.\n        \"\"\"\n        # Create HTTP client with configuration\n        self.client = httpx.AsyncClient(\n            base_url=config.api_endpoint,\n            headers={\n                \"Authorization\": f\"Bearer {config.api_key}\",\n                \"X-Region\": config.region,\n                \"User-Agent\": f\"terraform-provider-mycloud/{self.metadata.version}\"\n            },\n            timeout=config.timeout,\n            follow_redirects=True\n        )\n\n        # Store configuration for resource access\n        self.config = config\n\n        # Validate authentication\n        try:\n            response = await self.client.get(\"/auth/validate\")\n            response.raise_for_status()\n        except httpx.HTTPError as e:\n            raise ProviderError(f\"Authentication failed: {e}\")\n\n        logger.info(\"MyCloud provider configured successfully\", region=config.region)\n\n    async def close(self) -&gt; None:\n        \"\"\"Clean up provider resources.\"\"\"\n        if self.client:\n            await self.client.aclose()\n</code></pre>"},{"location":"guides/creating-providers/#step-2-implement-resources","title":"Step 2: Implement Resources","text":"<pre><code># resources/server.py\nfrom pyvider.resources import register_resource, BaseResource\nfrom pyvider.schema import Attribute\nfrom pyvider.exceptions import ResourceError\nimport attrs\nfrom datetime import datetime\n\n@register_resource(\"server\")\nclass Server(BaseResource):\n    \"\"\"\n    Manages MyCloud server instances.\n\n    Example:\n        resource \"mycloud_server\" \"web\" {\n          name = \"web-server-01\"\n          type = \"t3.medium\"\n          image = \"ubuntu-22.04\"\n\n          network {\n            vpc_id = mycloud_vpc.main.id\n            subnet_id = mycloud_subnet.public.id\n          }\n\n          tags = {\n            Environment = \"production\"\n            Team = \"platform\"\n          }\n        }\n    \"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Server configuration.\"\"\"\n\n        # Required attributes\n        name: str = Attribute(\n            required=True,\n            description=\"Server name\",\n            validators=[\n                Length(min=1, max=63),\n                Regex(r\"^[a-z0-9-]+$\")\n            ]\n        )\n\n        type: str = Attribute(\n            required=True,\n            description=\"Instance type\",\n            validators=[\n                OneOf([\"t3.micro\", \"t3.small\", \"t3.medium\", \"t3.large\"])\n            ]\n        )\n\n        image: str = Attribute(\n            required=True,\n            description=\"Operating system image\"\n        )\n\n        # Optional attributes\n        ssh_key: str = Attribute(\n            description=\"SSH public key for access\"\n        )\n\n        user_data: str = Attribute(\n            description=\"Cloud-init user data script\"\n        )\n\n        # Nested block for network configuration\n        network: NetworkConfig = Attribute(\n            required=True,\n            description=\"Network configuration\"\n        )\n\n        # Complex types\n        tags: dict[str, str] = Attribute(\n            default_factory=dict,\n            description=\"Resource tags\"\n        )\n\n        security_groups: list[str] = Attribute(\n            default_factory=list,\n            description=\"Security group IDs\"\n        )\n\n    @attrs.define\n    class NetworkConfig:\n        \"\"\"Nested network configuration.\"\"\"\n        vpc_id: str = Attribute(required=True)\n        subnet_id: str = Attribute(required=True)\n        assign_public_ip: bool = Attribute(default=True)\n\n    @attrs.define\n    class State:\n        \"\"\"Server state.\"\"\"\n\n        # Identifiers\n        id: str = Attribute(\n            computed=True,\n            description=\"Server ID\"\n        )\n\n        # Configuration echo\n        name: str = Attribute()\n        type: str = Attribute()\n        image: str = Attribute()\n        network: NetworkConfig = Attribute()\n        tags: dict[str, str] = Attribute()\n        security_groups: list[str] = Attribute()\n\n        # Computed attributes\n        public_ip: str = Attribute(\n            computed=True,\n            description=\"Public IP address\"\n        )\n\n        private_ip: str = Attribute(\n            computed=True,\n            description=\"Private IP address\"\n        )\n\n        status: str = Attribute(\n            computed=True,\n            description=\"Server status\"\n        )\n\n        created_at: str = Attribute(\n            computed=True,\n            description=\"Creation timestamp\"\n        )\n\n    async def create(self, config: Config) -&gt; State:\n        \"\"\"Create a new server.\"\"\"\n        logger.info(\"Creating server\", name=config.name, type=config.type)\n\n        # Prepare API request\n        request_data = {\n            \"name\": config.name,\n            \"type\": config.type,\n            \"image\": config.image,\n            \"vpc_id\": config.network.vpc_id,\n            \"subnet_id\": config.network.subnet_id,\n            \"assign_public_ip\": config.network.assign_public_ip,\n            \"tags\": config.tags,\n            \"security_groups\": config.security_groups\n        }\n\n        if config.ssh_key:\n            request_data[\"ssh_key\"] = config.ssh_key\n\n        if config.user_data:\n            request_data[\"user_data\"] = config.user_data\n\n        try:\n            # Create server via API\n            response = await self.provider.client.post(\n                \"/servers\",\n                json=request_data\n            )\n            response.raise_for_status()\n            server_data = response.json()\n\n            # Wait for server to be ready\n            server_id = server_data[\"id\"]\n            server = await self._wait_for_status(server_id, \"running\")\n\n            # Return state\n            return State(\n                id=server[\"id\"],\n                name=server[\"name\"],\n                type=server[\"type\"],\n                image=server[\"image\"],\n                network=config.network,\n                tags=server.get(\"tags\", {}),\n                security_groups=server.get(\"security_groups\", []),\n                public_ip=server.get(\"public_ip\", \"\"),\n                private_ip=server[\"private_ip\"],\n                status=server[\"status\"],\n                created_at=server[\"created_at\"]\n            )\n\n        except httpx.HTTPError as e:\n            raise ResourceError(f\"Failed to create server: {e}\")\n\n    async def read(self, state: State) -&gt; State | None:\n        \"\"\"Read current server state.\"\"\"\n        try:\n            response = await self.provider.client.get(f\"/servers/{state.id}\")\n\n            if response.status_code == 404:\n                return None  # Server no longer exists\n\n            response.raise_for_status()\n            server = response.json()\n\n            # Update state with current values\n            return State(\n                id=server[\"id\"],\n                name=server[\"name\"],\n                type=server[\"type\"],\n                image=server[\"image\"],\n                network=state.network,  # Preserve network config\n                tags=server.get(\"tags\", {}),\n                security_groups=server.get(\"security_groups\", []),\n                public_ip=server.get(\"public_ip\", \"\"),\n                private_ip=server[\"private_ip\"],\n                status=server[\"status\"],\n                created_at=server[\"created_at\"]\n            )\n\n        except httpx.HTTPError as e:\n            raise ResourceError(f\"Failed to read server: {e}\")\n\n    async def update(self, config: Config, state: State) -&gt; State:\n        \"\"\"Update server configuration.\"\"\"\n        updates = {}\n\n        # Check what needs updating\n        if config.tags != state.tags:\n            updates[\"tags\"] = config.tags\n\n        if config.security_groups != state.security_groups:\n            updates[\"security_groups\"] = config.security_groups\n\n        if config.type != state.type:\n            # Instance type change requires stop/modify/start\n            await self._resize_instance(state.id, config.type)\n\n        if updates:\n            try:\n                response = await self.provider.client.patch(\n                    f\"/servers/{state.id}\",\n                    json=updates\n                )\n                response.raise_for_status()\n            except httpx.HTTPError as e:\n                raise ResourceError(f\"Failed to update server: {e}\")\n\n        # Read and return current state\n        return await self.read(state)\n\n    async def delete(self, state: State) -&gt; None:\n        \"\"\"Delete the server.\"\"\"\n        try:\n            response = await self.provider.client.delete(f\"/servers/{state.id}\")\n\n            if response.status_code == 404:\n                return  # Already deleted\n\n            response.raise_for_status()\n\n            # Wait for deletion to complete\n            await self._wait_for_deletion(state.id)\n\n        except httpx.HTTPError as e:\n            raise ResourceError(f\"Failed to delete server: {e}\")\n\n    async def import_resource(self, resource_id: str) -&gt; State:\n        \"\"\"Import existing server into Terraform state.\"\"\"\n        try:\n            response = await self.provider.client.get(f\"/servers/{resource_id}\")\n            response.raise_for_status()\n            server = response.json()\n\n            # Reconstruct state from API data\n            return State(\n                id=server[\"id\"],\n                name=server[\"name\"],\n                type=server[\"type\"],\n                image=server[\"image\"],\n                network=NetworkConfig(\n                    vpc_id=server[\"vpc_id\"],\n                    subnet_id=server[\"subnet_id\"],\n                    assign_public_ip=bool(server.get(\"public_ip\"))\n                ),\n                tags=server.get(\"tags\", {}),\n                security_groups=server.get(\"security_groups\", []),\n                public_ip=server.get(\"public_ip\", \"\"),\n                private_ip=server[\"private_ip\"],\n                status=server[\"status\"],\n                created_at=server[\"created_at\"]\n            )\n\n        except httpx.HTTPError as e:\n            raise ResourceError(f\"Failed to import server {resource_id}: {e}\")\n\n    # Helper methods\n    async def _wait_for_status(self, server_id: str, target_status: str, timeout: int = 300):\n        \"\"\"Wait for server to reach target status.\"\"\"\n        start_time = datetime.now()\n\n        while (datetime.now() - start_time).total_seconds() &lt; timeout:\n            response = await self.provider.client.get(f\"/servers/{server_id}\")\n            response.raise_for_status()\n            server = response.json()\n\n            if server[\"status\"] == target_status:\n                return server\n\n            if server[\"status\"] == \"error\":\n                raise ResourceError(f\"Server entered error state: {server.get('error_message')}\")\n\n            await asyncio.sleep(5)\n\n        raise ResourceError(f\"Timeout waiting for server to reach {target_status} status\")\n\n    async def _wait_for_deletion(self, server_id: str, timeout: int = 120):\n        \"\"\"Wait for server deletion to complete.\"\"\"\n        start_time = datetime.now()\n\n        while (datetime.now() - start_time).total_seconds() &lt; timeout:\n            response = await self.provider.client.get(f\"/servers/{server_id}\")\n\n            if response.status_code == 404:\n                return  # Successfully deleted\n\n            await asyncio.sleep(5)\n\n        raise ResourceError(\"Timeout waiting for server deletion\")\n\n    async def _resize_instance(self, server_id: str, new_type: str):\n        \"\"\"Resize instance type.\"\"\"\n        # Stop instance\n        await self.provider.client.post(f\"/servers/{server_id}/stop\")\n        await self._wait_for_status(server_id, \"stopped\")\n\n        # Change type\n        response = await self.provider.client.patch(\n            f\"/servers/{server_id}\",\n            json={\"type\": new_type}\n        )\n        response.raise_for_status()\n\n        # Start instance\n        await self.provider.client.post(f\"/servers/{server_id}/start\")\n        await self._wait_for_status(server_id, \"running\")\n</code></pre>"},{"location":"guides/creating-providers/#step-3-add-data-sources","title":"Step 3: Add Data Sources","text":"<pre><code># data_sources/images.py\nfrom pyvider.data_sources import register_data_source, BaseDataSource\nfrom pyvider.schema import Attribute\nimport attrs\n\n@register_data_source(\"images\")\nclass Images(BaseDataSource):\n    \"\"\"\n    Data source for available server images.\n\n    Example:\n        data \"mycloud_images\" \"ubuntu\" {\n          filter = \"ubuntu-*\"\n          architecture = \"x86_64\"\n          most_recent = true\n        }\n    \"\"\"\n\n    @attrs.define\n    class Config:\n        \"\"\"Query configuration.\"\"\"\n        filter: str = Attribute(\n            default=\"*\",\n            description=\"Name filter pattern\"\n        )\n\n        architecture: str = Attribute(\n            default=\"x86_64\",\n            description=\"CPU architecture\",\n            validators=[OneOf([\"x86_64\", \"arm64\"])]\n        )\n\n        most_recent: bool = Attribute(\n            default=False,\n            description=\"Return only the most recent image\"\n        )\n\n    @attrs.define\n    class State:\n        \"\"\"Query results.\"\"\"\n        images: list[dict] = Attribute(\n            computed=True,\n            description=\"List of matching images\"\n        )\n\n        total: int = Attribute(\n            computed=True,\n            description=\"Total number of matching images\"\n        )\n\n    async def read(self, config: Config) -&gt; State:\n        \"\"\"Fetch image data.\"\"\"\n        params = {\n            \"filter\": config.filter,\n            \"architecture\": config.architecture\n        }\n\n        response = await self.provider.client.get(\"/images\", params=params)\n        response.raise_for_status()\n\n        images = response.json()[\"images\"]\n\n        if config.most_recent and images:\n            # Sort by creation date and take the most recent\n            images = sorted(\n                images,\n                key=lambda x: x[\"created_at\"],\n                reverse=True\n            )[:1]\n\n        return State(\n            images=[\n                {\n                    \"id\": img[\"id\"],\n                    \"name\": img[\"name\"],\n                    \"version\": img[\"version\"],\n                    \"architecture\": img[\"architecture\"],\n                    \"created_at\": img[\"created_at\"]\n                }\n                for img in images\n            ],\n            total=len(images)\n        )\n</code></pre>"},{"location":"guides/creating-providers/#testing-your-provider","title":"\ud83e\uddea Testing Your Provider","text":"<pre><code># tests/test_provider.py\nimport pytest\nfrom pyvider.testing import ProviderTestCase\nimport httpx\nfrom unittest.mock import AsyncMock, patch\n\nclass TestMyCloudProvider(ProviderTestCase):\n    \"\"\"Test suite for MyCloud provider.\"\"\"\n\n    @pytest.fixture\n    async def configured_provider(self):\n        \"\"\"Provide a configured provider instance.\"\"\"\n        from my_provider.provider import MyCloudProvider\n\n        provider = MyCloudProvider()\n\n        # Mock HTTP client\n        with patch.object(provider, 'client', new=AsyncMock(spec=httpx.AsyncClient)):\n            # Mock auth validation\n            provider.client.get.return_value.raise_for_status = AsyncMock()\n\n            await provider.configure(\n                MyCloudProvider.Config(\n                    api_key=\"test-key\",\n                    region=\"us-east-1\"\n                )\n            )\n\n            yield provider\n\n    async def test_provider_configuration(self, configured_provider):\n        \"\"\"Test provider configures correctly.\"\"\"\n        assert configured_provider.config.api_key == \"test-key\"\n        assert configured_provider.config.region == \"us-east-1\"\n        assert configured_provider.client is not None\n\n    async def test_server_create(self, configured_provider):\n        \"\"\"Test server resource creation.\"\"\"\n        from my_provider.resources.server import Server\n\n        server = Server()\n        server.provider = configured_provider\n\n        # Mock API responses\n        configured_provider.client.post.return_value.json.return_value = {\n            \"id\": \"srv-123\",\n            \"status\": \"pending\"\n        }\n\n        configured_provider.client.get.return_value.json.return_value = {\n            \"id\": \"srv-123\",\n            \"name\": \"test-server\",\n            \"type\": \"t3.medium\",\n            \"image\": \"ubuntu-22.04\",\n            \"status\": \"running\",\n            \"private_ip\": \"10.0.1.5\",\n            \"public_ip\": \"203.0.113.42\",\n            \"created_at\": \"2024-01-15T10:00:00Z\"\n        }\n\n        # Create server\n        config = Server.Config(\n            name=\"test-server\",\n            type=\"t3.medium\",\n            image=\"ubuntu-22.04\",\n            network=Server.NetworkConfig(\n                vpc_id=\"vpc-123\",\n                subnet_id=\"subnet-456\"\n            )\n        )\n\n        state = await server.create(config)\n\n        # Verify state\n        assert state.id == \"srv-123\"\n        assert state.status == \"running\"\n        assert state.public_ip == \"203.0.113.42\"\n</code></pre>"},{"location":"guides/creating-providers/#best-practices","title":"\ud83c\udfa8 Best Practices","text":""},{"location":"guides/creating-providers/#1-provider-design","title":"1. Provider Design","text":"<ul> <li>Single Responsibility: Provider handles authentication and configuration only</li> <li>Shared Client: Create reusable API client for all resources</li> <li>Configuration Validation: Validate all inputs in the schema</li> <li>Graceful Degradation: Handle API failures gracefully</li> </ul>"},{"location":"guides/creating-providers/#2-resource-implementation","title":"2. Resource Implementation","text":"<ul> <li>Idempotency: All operations must be idempotent</li> <li>Drift Detection: Implement proper <code>read()</code> to detect changes</li> <li>Partial Updates: Only update changed attributes</li> <li>Error Context: Provide detailed error messages</li> </ul>"},{"location":"guides/creating-providers/#3-state-management","title":"3. State Management","text":"<ul> <li>Minimal State: Store only essential information</li> <li>Computed Attributes: Mark generated values as computed</li> <li>Sensitive Data: Use private state for secrets</li> <li>Import Support: Always implement import functionality</li> </ul>"},{"location":"guides/creating-providers/#4-performance","title":"4. Performance","text":"<ul> <li>Async Operations: Use async/await for all I/O</li> <li>Connection Pooling: Reuse HTTP connections</li> <li>Retries: Implement exponential backoff</li> <li>Timeouts: Set reasonable timeouts</li> </ul>"},{"location":"guides/creating-providers/#security-considerations","title":"\ud83d\udd10 Security Considerations","text":""},{"location":"guides/creating-providers/#handling-sensitive-data","title":"Handling Sensitive Data","text":"<pre><code>@attrs.define\nclass Config:\n    # Mark sensitive attributes\n    api_key: str = Attribute(\n        required=True,\n        sensitive=True  # Won't be logged\n    )\n\n    # Use private state for runtime secrets\n    _session_token: str = field(default=None, init=False)\n</code></pre>"},{"location":"guides/creating-providers/#input-validation","title":"Input Validation","text":"<pre><code>@attrs.define\nclass Config:\n    email: str = Attribute(\n        required=True,\n        validators=[\n            Email(),  # Validate email format\n            NoSQLInjection(),  # Prevent injection\n        ]\n    )\n</code></pre>"},{"location":"guides/creating-providers/#advanced-features","title":"\ud83d\udcda Advanced Features","text":""},{"location":"guides/creating-providers/#custom-validators","title":"Custom Validators","text":"<pre><code>def validate_cidr(value: str) -&gt; None:\n    \"\"\"Validate CIDR notation.\"\"\"\n    import ipaddress\n    try:\n        ipaddress.ip_network(value)\n    except ValueError as e:\n        raise ValueError(f\"Invalid CIDR: {e}\")\n\n@attrs.define\nclass Config:\n    cidr_block: str = Attribute(\n        required=True,\n        validators=[validate_cidr]\n    )\n</code></pre>"},{"location":"guides/creating-providers/#dynamic-schema","title":"Dynamic Schema","text":"<pre><code>@register_resource(\"dynamic_table\")\nclass DynamicTable(BaseResource):\n    @classmethod\n    def get_schema(cls) -&gt; PvsSchema:\n        \"\"\"Generate schema dynamically.\"\"\"\n        # Load schema from external source\n        schema_def = load_schema_from_api()\n        return build_schema(schema_def)\n</code></pre>"},{"location":"guides/creating-providers/#provider-capabilities","title":"Provider Capabilities","text":"<pre><code>@register_provider(\"advanced\")\nclass AdvancedProvider(BaseProvider):\n    def __init__(self):\n        super().__init__(\n            metadata=ProviderMetadata(\n                name=\"advanced\",\n                version=\"1.0.0\",\n                capabilities=ProviderCapabilities(\n                    plan_destroy=True,\n                    move_resource_state=True,\n                    get_provider_schema_optional=False\n                )\n            )\n        )\n</code></pre>"},{"location":"guides/creating-providers/#next-steps","title":"\ud83d\ude80 Next Steps","text":"<ul> <li>Creating Resources - Deep dive into resources</li> <li>Creating Data Sources - Implementing data sources</li> <li>Testing Guide - Comprehensive testing</li> <li>Debugging - Troubleshooting providers</li> </ul> <p>   Ready to build? Start with the First Provider Tutorial \u2192 </p>"},{"location":"guides/creating-resources/","title":"\ud83d\udce6 Creating a Resource","text":"<p>This guide will help you create a new resource for your <code>pyvider</code> provider.</p>"},{"location":"guides/creating-resources/#defining-the-resource-schema","title":"\ud83d\udcc4 Defining the Resource Schema","text":"<p>The resource schema defines the configuration options for the resource. You can define the resource schema in a new file in the <code>resources</code> directory, using the <code>@resource</code> decorator.</p> <pre><code>from pyvider.resources import resource, ResourceConfig\n\n@resource\nclass MyResource(ResourceConfig):\n    \"\"\"\n    MyResource is a custom resource that does amazing things.\n    \"\"\"\n    name: str\n</code></pre> <p>In this example, we define a resource with a single configuration option, <code>name</code>.</p>"},{"location":"guides/creating-resources/#implementing-the-resource-logic","title":"\ud83e\udde0 Implementing the Resource Logic","text":"<p>The resource logic is implemented in the same file as the resource schema. The resource logic is responsible for creating, reading, updating, and deleting the resource.</p> <pre><code>from pyvider.resources import resource, ResourceConfig\n\n@resource\nclass MyResource(ResourceConfig):\n    \"\"\"\n    MyResource is a custom resource that does amazing things.\n    \"\"\"\n    name: str\n\n    def create(self, ctx):\n        # Create the resource.\n        pass\n\n    def read(self, ctx):\n        # Read the resource.\n        pass\n\n    def update(self, ctx):\n        # Update the resource.\n        pass\n\n    def delete(self, ctx):\n        # Delete the resource.\n        pass\n</code></pre> <p>In this example, we implement the <code>create</code>, <code>read</code>, <code>update</code>, and <code>delete</code> methods, which are called by <code>pyvider</code> to manage the resource lifecycle.</p>"},{"location":"guides/creating-resources/#handling-state","title":"\ud83d\udcbe Handling State","text":"<p><code>pyvider</code> handles the resource state for you. The framework automatically saves the resource state to the Terraform state file. You can access the resource state in your resource logic using the <code>ctx.state</code> object.</p>"},{"location":"guides/debugging/","title":"\ud83d\udc1b Debugging","text":"<p>This guide provides tips and tricks for debugging your <code>pyvider</code> provider.</p>"},{"location":"guides/debugging/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Debugging <code>pyvider</code> providers can be tricky, but there are a few tools and techniques that can help.</p>"},{"location":"guides/debugging/#using-the-debugger","title":"\ud83d\ude80 Using the Debugger","text":"<p>You can use the <code>pdb</code> debugger to debug your provider. To do this, you'll need to set the <code>TF_REATTACH_PROVIDERS</code> environment variable to <code>true</code>. This will prevent Terraform from killing the provider process when it's finished.</p> <pre><code>export TF_REATTACH_PROVIDERS=true\n</code></pre> <p>You can then attach to the provider process with <code>pdb</code>:</p> <pre><code>python -m pdb -p &lt;pid&gt;\n</code></pre>"},{"location":"guides/debugging/#logging","title":"\ud83d\udcdd Logging","text":"<p>You can use the logging system to get more information about what's happening in your provider. See the Logging documentation for more information.</p>"},{"location":"guides/debugging/#tracing","title":"\ud83d\udd2c Tracing","text":"<p>You can use a tool like <code>strace</code> or <code>dtruss</code> to trace the system calls that your provider is making. This can be useful for debugging low-level issues. ```</p>"},{"location":"guides/error-handling/","title":"\ud83d\udea8 Error Handling","text":"<p>This guide explains how to handle errors in your <code>pyvider</code> provider.</p>"},{"location":"guides/error-handling/#overview","title":"\ud83d\udcc4 Overview","text":"<p><code>pyvider</code> provides a set of built-in exceptions that you can use to handle errors in your provider. These exceptions are all subclasses of the <code>PyviderError</code> exception.</p>"},{"location":"guides/error-handling/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to handle an error in your provider:</p> <pre><code>from pyvider.exceptions import PyviderError\n\ndef my_function():\n    raise PyviderError(\"This is my error message.\")\n</code></pre> <p>In this example, we raise a <code>PyviderError</code> exception with a custom error message.</p>"},{"location":"guides/error-handling/#available-exceptions","title":"\u2699\ufe0f Available Exceptions","text":"<p>The following exceptions are available:</p> <ul> <li><code>PyviderError</code>: The base class for all <code>pyvider</code> exceptions.</li> <li><code>ConversionError</code>: An error occurred during type conversion.</li> <li><code>FrameworkConfigurationError</code>: An error occurred during framework configuration.</li> <li><code>PluginError</code>: An error occurred in the plugin.</li> <li><code>PyviderValueError</code>: An invalid value was encountered.</li> <li><code>InvalidTypeError</code>: An invalid type was encountered.</li> <li><code>UnsupportedTypeError</code>: An unsupported type was encountered.</li> <li><code>ComponentConfigurationError</code>: An error occurred during component configuration.</li> <li><code>FunctionError</code>: An error occurred in a function.</li> <li><code>FunctionRegistrationError</code>: An error occurred during function registration.</li> <li><code>FunctionValidationError</code>: An error occurred during function validation.</li> <li><code>GRPCError</code>: An error occurred in the gRPC layer.</li> <li><code>ProviderError</code>: An error occurred in the provider.</li> <li><code>ProviderConfigurationError</code>: An error occurred during provider configuration.</li> <li><code>ProviderInitializationError</code>: An error occurred during provider initialization.</li> <li><code>ComponentRegistryError</code>: An error occurred in the component registry.</li> <li><code>ValidatorRegistrationError</code>: An error occurred during validator registration.</li> <li><code>ResourceError</code>: An error occurred in a resource.</li> <li><code>DataSourceError</code>: An error occurred in a data source.</li> <li><code>CapabilityError</code>: An error occurred in a capability.</li> <li><code>ResourceValidationError</code>: An error occurred during resource validation.</li> <li><code>ResourceNotFoundError</code>: A resource was not found.</li> <li><code>ResourceOperationError</code>: An error occurred during a resource operation.</li> <li><code>ResourceLifecycleContractError</code>: An error occurred in the resource lifecycle contract.</li> <li><code>SchemaError</code>: An error occurred in the schema.</li> <li><code>SchemaValidationError</code>: An error occurred during schema validation.</li> <li><code>SchemaRegistrationError</code>: An error occurred during schema registration.</li> <li><code>SchemaParseError</code>: An error occurred during schema parsing.</li> <li><code>SchemaConversionError</code>: An error occurred during schema conversion.</li> <li><code>SerializationError</code>: An error occurred during serialization.</li> <li><code>DeserializationError</code>: An error occurred during deserialization.</li> <li><code>ValidationError</code>: An error occurred during validation.</li> <li><code>AttributeValidationError</code>: An error occurred during attribute validation. ```</li> </ul>"},{"location":"guides/logging/","title":"\ud83e\udeb5 Logging","text":"<p>This guide explains how to use the logging system in your <code>pyvider</code> provider.</p>"},{"location":"guides/logging/#overview","title":"\ud83d\udcc4 Overview","text":"<p><code>pyvider</code> uses the <code>structlog</code> library for logging. This library provides a powerful and flexible logging system that allows you to create structured logs.</p>"},{"location":"guides/logging/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to use the logging system in your provider:</p> <pre><code>from provide.foundation import logger\n\ndef my_function():\n    logger.info(\"This is my log message.\")\n</code></pre> <p>In this example, we log an informational message to the console.</p>"},{"location":"guides/logging/#log-levels","title":"\u2699\ufe0f Log Levels","text":"<p>The following log levels are available:</p> <ul> <li><code>debug</code>: Detailed information, typically of interest only when diagnosing problems.</li> <li><code>info</code>: Confirmation that things are working as expected.</li> <li><code>warning</code>: An indication that something unexpected happened, or indicative of some problem in the near future (e.g. \u2018disk space low\u2019). The software is still working as expected.</li> <li><code>error</code>: Due to a more serious problem, the software has not been able to perform some function.</li> <li><code>critical</code>: A serious error, indicating that the program itself may be unable to continue running.</li> </ul>"},{"location":"guides/logging/#structured-logging","title":"\ud83d\udcdd Structured Logging","text":"<p>You can use the <code>bind</code> method to add structured data to your logs.</p> <pre><code>from provide.foundation import logger\n\ndef my_function():\n    log = logger.bind(user_id=123)\n    log.info(\"This is my log message.\")\n</code></pre> <p>In this example, we bind the <code>user_id</code> to the logger. This will add a <code>user_id=123</code> field to all the logs that are created with this logger. ```</p>"},{"location":"guides/managing-resources/","title":"Managing Resources","text":""},{"location":"guides/managing-resources/#creating-your-first-masterpiece-the-resource","title":"\u2728 Creating Your First Masterpiece: The Resource","text":"<p>Now that we've configured our provider, it's time to create our first masterpiece: a resource. Resources are the heart of Terraform, and they represent the infrastructure objects that we want to manage.</p> <p>In this example, we'll create a simple resource that represents a file on the local filesystem. Add the following to your <code>main.tf</code> file:</p> <pre><code>resource \"my-provider_my-file\" \"example\" {\n  path    = \"hello.txt\"\n  content = \"Hello, Pyvider!\"\n}\n</code></pre> <p>This tells Terraform that we want to create a new resource of type <code>my-provider_my-file</code> with the name <code>example</code>. We're also giving it two attributes: <code>path</code> and <code>content</code>.</p> <p>Now, run <code>terraform apply</code> to create the resource:</p> <pre><code>terraform apply\n</code></pre> <p>Terraform will ask you to confirm that you want to create the resource. Type <code>yes</code> and press Enter.</p> <p>And with that, you've created your first resource! You should now see a new file called <code>hello.txt</code> in your directory with the content \"Hello, Pyvider!\".</p> <p>In the next section, we'll learn how to read data from a data source.</p>"},{"location":"guides/provider-lifecycle/","title":"\ud83d\udd04 Provider Lifecycle","text":"<p>This guide explains the <code>pyvider</code> provider lifecycle.</p>"},{"location":"guides/provider-lifecycle/#overview","title":"\ud83d\udcc4 Overview","text":"<p>The <code>pyvider</code> provider lifecycle consists of the following steps:</p> <ol> <li>Initialization: <code>pyvider</code> initializes the provider and discovers the resources, data sources, and functions.</li> <li>Configuration: <code>pyvider</code> configures the provider with the configuration from the Terraform configuration file.</li> <li>Plannning: Terraform calls the <code>plan</code> method of the resources to create a plan for the changes to be made.</li> <li>Application: Terraform calls the <code>apply</code> method of the resources to apply the changes to the infrastructure.</li> <li>Termination: <code>pyvider</code> terminates the provider.</li> </ol>"},{"location":"guides/provider-lifecycle/#implementing-the-provider-logic","title":"\ud83e\udde0 Implementing the Provider Logic","text":"<p>You can hook into the provider lifecycle by implementing the following methods in your provider class:</p> <ul> <li><code>configure</code>: This method is called to configure the provider.</li> <li><code>setup</code>: This method is called to set up the provider.</li> <li><code>teardown</code>: This method is called to tear down the provider. ```</li> </ul>"},{"location":"guides/testing/","title":"Testing","text":"<p>This guide will help you test your <code>pyvider</code> provider.</p>"},{"location":"guides/testing/#unit-testing","title":"Unit Testing","text":"<p>You can use the <code>pytest</code> framework to write unit tests for your provider.</p>"},{"location":"guides/testing/#integration-testing","title":"Integration Testing","text":"<p>You can use the <code>pytest</code> framework to write integration tests for your provider.</p>"},{"location":"guides/testing/#end-to-end-testing","title":"End-to-end Testing","text":"<p>You can use the <code>pytest</code> framework to write end-to-end tests for your provider.</p>"},{"location":"guides/using-data-sources/","title":"Using Data Sources","text":""},{"location":"guides/using-data-sources/#peering-into-the-crystal-ball-the-data-source","title":"\ud83d\udd2e Peering into the Crystal Ball: The Data Source","text":"<p>Sometimes, we need to fetch information from the outside world to help us build our infrastructure. This is where data sources come in. Data sources are like read-only views of the world, and they allow us to fetch information from APIs, services, and devices.</p> <p>In this example, we'll create a data source that reads the content of our <code>hello.txt</code> file. Add the following to your <code>main.tf</code> file:</p> <pre><code>data \"my-provider_my-file\" \"example\" {\n  path = \"hello.txt\"\n}\n\noutput \"file_content\" {\n  value = data.my-provider_my-file.example.content\n}\n</code></pre> <p>This tells Terraform that we want to read data from a data source of type <code>my-provider_my-file</code> with the name <code>example</code>. We're also giving it one attribute: <code>path</code>.</p> <p>Now, run <code>terraform apply</code> to read the data from the data source:</p> <pre><code>terraform apply\n</code></pre> <p>Terraform will show you the content of the <code>hello.txt</code> file as an output variable.</p> <p>And with that, you've peered into the crystal ball and read data from the outside world! In the next section, we'll learn how to use functions to perform custom logic.</p>"},{"location":"guides/using-functions/","title":"Using Functions","text":""},{"location":"guides/using-functions/#weaving-spells-the-function","title":"\ud83e\uddd9\u200d\u2642\ufe0f Weaving Spells: The Function","text":"<p>Sometimes, we need to perform custom logic that is not possible with the built-in Terraform functions. This is where <code>pyvider</code> functions come in. Functions are like spells that we can cast to perform calculations, transformations, and other magical feats.</p> <p>In this example, we'll create a function that converts a string to uppercase. Add the following to your <code>main.tf</code> file:</p> <pre><code>output \"uppercase_hello\" {\n  value = my-provider_upper(\"hello, pyvider!\")\n}\n</code></pre> <p>This tells Terraform that we want to call a function named <code>my-provider_upper</code> with the input <code>\"hello, pyvider!\"</code>.</p> <p>Now, run <code>terraform apply</code> to call the function:</p> <pre><code>terraform apply\n</code></pre> <p>Terraform will show you the uppercase version of the string as an output variable.</p> <p>And with that, you've woven your first spell and performed a magical transformation! In the next section, we'll learn how to put all the pieces together to create a truly powerful provider.</p>"},{"location":"schema/attributes/","title":"\ud83d\udce6 Attributes","text":"<p>This guide provides a reference for the <code>pyvider</code> Attribute schema type.</p>"},{"location":"schema/attributes/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Attributes are used to define the simple values in your schema, such as strings, numbers, and booleans. Attributes are defined using the <code>Attribute</code> class.</p>"},{"location":"schema/attributes/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to define an attribute.</p>"},{"location":"schema/attributes/#terraform-configuration-hcl","title":"Terraform Configuration (HCL)","text":"<pre><code>resource \"my-provider_my-resource\" \"example\" {\n  my_attribute = \"hello world\"\n}\n</code></pre>"},{"location":"schema/attributes/#pyvider-schema","title":"Pyvider Schema","text":"<pre><code>from pyvider.schema import Attribute\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    required=True,\n    description=\"This is my attribute.\",\n)\n</code></pre> <p>In this example, we define an attribute named <code>my_attribute</code> with a type of <code>string</code>. The <code>required</code> attribute is set to <code>True</code>, which means that the attribute must be set in the Terraform configuration. The <code>description</code> attribute is used to provide a description of the attribute.</p>"},{"location":"schema/attributes/#arguments","title":"\u2699\ufe0f Arguments","text":"<p>The <code>Attribute</code> class accepts the following arguments:</p> <ul> <li><code>name</code>: The name of the attribute.</li> <li><code>type</code>: The type of the attribute. See the Types documentation for a list of available types.</li> <li><code>required</code>: A boolean indicating whether the attribute is required.</li> <li><code>optional</code>: A boolean indicating whether the attribute is optional.</li> <li><code>computed</code>: A boolean indicating whether the attribute is computed.</li> <li><code>sensitive</code>: A boolean indicating whether the attribute is sensitive.</li> <li><code>description</code>: A description of the attribute.</li> <li><code>validators</code>: A list of validators to apply to the attribute. See the Validators documentation for a list of available validators. ```</li> </ul>"},{"location":"schema/best-practices/","title":"\u2728 Best Practices","text":"<p>This guide provides best practices for designing clean and maintainable schemas.</p>"},{"location":"schema/best-practices/#keep-it-simple","title":"\ud83d\udcc4 Keep it Simple","text":"<p>The best schemas are simple and easy to understand. Avoid creating complex schemas with many nested blocks and attributes.</p>"},{"location":"schema/best-practices/#be-consistent","title":"\ud83d\udcc4 Be Consistent","text":"<p>Use a consistent naming convention for your attributes and blocks. This will make your schema easier to read and understand.</p>"},{"location":"schema/best-practices/#be-descriptive","title":"\ud83d\udcc4 Be Descriptive","text":"<p>Use the <code>description</code> attribute to provide a clear and descriptive explanation of your attributes and blocks. This will help your users understand how to use your provider.</p>"},{"location":"schema/best-practices/#use-validators","title":"\ud83d\udcc4 Use Validators","text":"<p>Use validators to enforce constraints on the values in your schema. This will help to prevent errors and ensure that your provider is used correctly. ```</p>"},{"location":"schema/blocks/","title":"\ud83e\uddf1 Blocks","text":"<p>This guide provides a reference for the <code>pyvider</code> Block schema type.</p>"},{"location":"schema/blocks/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Blocks are used to define the nested objects in your schema. Blocks are defined using the <code>Block</code> class.</p>"},{"location":"schema/blocks/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to define a block.</p>"},{"location":"schema/blocks/#terraform-configuration-hcl","title":"Terraform Configuration (HCL)","text":"<pre><code>resource \"my-provider_my-resource\" \"example\" {\n  my_block {\n    my_attribute = \"hello world\"\n  }\n}\n</code></pre>"},{"location":"schema/blocks/#pyvider-schema","title":"Pyvider Schema","text":"<pre><code>from pyvider.schema import Block, Attribute\n\nmy_block = Block(\n    name=\"my_block\",\n    attributes=[\n        Attribute(\n            name=\"my_attribute\",\n            type=\"string\",\n            required=True,\n            description=\"This is my attribute.\",\n        )\n    ],\n)\n</code></pre> <p>In this example, we define a block named <code>my_block</code> with a single attribute named <code>my_attribute</code>.</p>"},{"location":"schema/blocks/#arguments","title":"\u2699\ufe0f Arguments","text":"<p>The <code>Block</code> class accepts the following arguments:</p> <ul> <li><code>name</code>: The name of the block.</li> <li><code>attributes</code>: A list of attributes in the block.</li> <li><code>blocks</code>: A list of nested blocks in the block.</li> <li><code>required</code>: A boolean indicating whether the block is required.</li> <li><code>optional</code>: A boolean indicating whether the block is optional.</li> <li><code>computed</code>: A boolean indicating whether the block is computed.</li> <li><code>sensitive</code>: A boolean indicating whether the block is sensitive.</li> <li><code>description</code>: A description of the block.</li> <li><code>validators</code>: A list of validators to apply to the block. See the Validators documentation for a list of available validators. ```</li> </ul>"},{"location":"schema/common-patterns/","title":"\ud83e\udde9 Common Patterns","text":"<p>This guide provides solutions to common schema design problems.</p>"},{"location":"schema/common-patterns/#optional-with-default","title":"\ud83d\udcc4 Optional with Default","text":"<p>Sometimes, you want to have an attribute that is optional, but has a default value if it's not set. You can do this using the <code>optional</code> and <code>default</code> arguments in the <code>Attribute</code> class.</p> <pre><code>from pyvider.schema import Attribute\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    optional=True,\n    default=\"my-default-value\",\n    description=\"This is my attribute.\",\n)\n</code></pre>"},{"location":"schema/common-patterns/#required-and-computed","title":"\ud83d\udcc4 Required and Computed","text":"<p>Sometimes, you want to have an attribute that is required, but is also computed. This is useful when you have an attribute that is required, but its value is not known until the resource is created.</p> <p>You can do this by setting both the <code>required</code> and <code>computed</code> arguments to <code>True</code>.</p> <p><pre><code>from pyvider.schema import Attribute\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    required=True,\n    computed=True,\n    description=\"This is my attribute.\",\n)\n</code></pre> ```</p>"},{"location":"schema/computed-attributes/","title":"\ud83e\uddee Computed Attributes","text":"<p>This guide explains how to use computed attributes in your <code>pyvider</code> schema.</p>"},{"location":"schema/computed-attributes/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Computed attributes are attributes whose values are not known until the resource is created or updated. Computed attributes are defined using the <code>computed</code> argument in the <code>Attribute</code> class.</p>"},{"location":"schema/computed-attributes/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to define a computed attribute:</p> <pre><code>from pyvider.schema import Attribute\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    computed=True,\n    description=\"This is my computed attribute.\",\n)\n</code></pre> <p>In this example, we define a computed attribute named <code>my_attribute</code> with a type of <code>string</code>.</p>"},{"location":"schema/computed-attributes/#implementing-the-resource-logic","title":"\ud83e\udde0 Implementing the Resource Logic","text":"<p>When you have a computed attribute, you need to set its value in the <code>create</code> and <code>update</code> methods of your resource.</p> <pre><code>from pyvider.resources import resource, ResourceConfig\n\n@resource\nclass MyResource(ResourceConfig):\n    \"\"\"\n    MyResource is a custom resource that does amazing things.\n    \"\"\"\n    my_attribute: str\n\n    def create(self, ctx):\n        # Create the resource.\n        ctx.state[\"my_attribute\"] = \"my-computed-value\"\n\n    def read(self, ctx):\n        # Read the resource.\n        pass\n\n    def update(self, ctx):\n        # Update the resource.\n        ctx.state[\"my_attribute\"] = \"my-new-computed-value\"\n\n    def delete(self, ctx):\n        # Delete the resource.\n        pass\n</code></pre> <p>In this example, we set the value of the <code>my_attribute</code> attribute in the <code>create</code> and <code>update</code> methods. ```</p>"},{"location":"schema/overview/","title":"\ud83d\udcdc Schema","text":"<p>This chapter provides a detailed overview of the <code>pyvider</code> schema.</p>"},{"location":"schema/overview/#overview","title":"\ud83c\udfdb\ufe0f Overview","text":"<p>The <code>pyvider</code> schema is used to define the structure and constraints of your provider's resources, data sources, and functions. The schema is defined using a combination of Python types and <code>pyvider</code>'s own schema types.</p> <p>The schema is hierarchical, with a root schema that contains a set of attributes and blocks. Attributes are used to define the simple values in your schema, while blocks are used to define the nested objects. ```</p>"},{"location":"schema/schema-by-example/","title":"\ud83d\uddfa\ufe0f Schema by Example","text":"<p>This guide will walk you through the process of building a complex schema from the ground up.</p>"},{"location":"schema/schema-by-example/#the-goal","title":"\ud83d\ude80 The Goal","text":"<p>Our goal is to create a schema for a resource that represents a web server. The web server will have the following attributes:</p> <ul> <li><code>name</code>: The name of the web server.</li> <li><code>port</code>: The port that the web server listens on.</li> <li><code>enabled</code>: A boolean indicating whether the web server is enabled.</li> <li><code>aliases</code>: A list of aliases for the web server.</li> <li><code>config</code>: A map of configuration options for the web server.</li> </ul>"},{"location":"schema/schema-by-example/#the-blueprint-the-schema","title":"\ud83c\udfd7\ufe0f The Blueprint: The Schema","text":"<p>Here is the <code>pyvider</code> schema for our web server resource:</p> <pre><code>from pyvider.schema import Attribute, Block\n\nschema = Block(\n    name=\"my_web_server\",\n    attributes=[\n        Attribute(\n            name=\"name\",\n            type=\"string\",\n            required=True,\n            description=\"The name of the web server.\",\n        ),\n        Attribute(\n            name=\"port\",\n            type=\"number\",\n            optional=True,\n            default=80,\n            description=\"The port that the web server listens on.\",\n        ),\n        Attribute(\n            name=\"enabled\",\n            type=\"bool\",\n            optional=True,\n            default=True,\n            description=\"A boolean indicating whether the web server is enabled.\",\n        ),\n        Attribute(\n            name=\"aliases\",\n            type=\"list\",\n            optional=True,\n            description=\"A list of aliases for the web server.\",\n        ),\n        Attribute(\n            name=\"config\",\n            type=\"map\",\n            optional=True,\n            description=\"A map of configuration options for the web server.\",\n        ),\n    ],\n)\n</code></pre>"},{"location":"schema/schema-by-example/#terraform-configuration-hcl","title":"Terraform Configuration (HCL)","text":"<p>Here is an example of how you would use our web server resource in a Terraform configuration:</p> <p><pre><code>resource \"my-provider_my-web-server\" \"example\" {\n  name    = \"my-web-server\"\n  port    = 8080\n  enabled = true\n  aliases = [\"www.example.com\", \"example.com\"]\n  config = {\n    \"timeout\" = \"30s\"\n    \"max_connections\" = \"1024\"\n  }\n}\n</code></pre> ```</p>"},{"location":"schema/sensitive-data/","title":"\ud83e\udd2b Sensitive Data","text":"<p>This guide explains how to handle sensitive data in your <code>pyvider</code> schema.</p>"},{"location":"schema/sensitive-data/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Sensitive data is data that should not be displayed in the Terraform UI or logs. Sensitive data is defined using the <code>sensitive</code> argument in the <code>Attribute</code> class.</p>"},{"location":"schema/sensitive-data/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to define a sensitive attribute:</p> <pre><code>from pyvider.schema import Attribute\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    sensitive=True,\n    description=\"This is my sensitive attribute.\",\n)\n</code></pre> <p>In this example, we define a sensitive attribute named <code>my_attribute</code> with a type of <code>string</code>.</p>"},{"location":"schema/sensitive-data/#behavior-in-terraform","title":"\ud83e\udde0 Behavior in Terraform","text":"<p>When an attribute is marked as sensitive, its value will be replaced with <code>(sensitive)</code> in the Terraform UI and logs. ```</p>"},{"location":"schema/types/","title":"\ud83d\udd20 Types","text":"<p>This guide provides a reference for the <code>pyvider</code> schema types.</p>"},{"location":"schema/types/#overview","title":"\ud83d\udcc4 Overview","text":"<p><code>pyvider</code> provides a set of built-in types that you can use to define the attributes in your schema. These types correspond to the primitive types in Terraform.</p>"},{"location":"schema/types/#available-types","title":"\ud83d\ude80 Available Types","text":"<p>The following types are available:</p> <ul> <li><code>string</code>: A string of text.</li> <li><code>number</code>: A number.</li> <li><code>bool</code>: A boolean.</li> <li><code>list</code>: A list of values.</li> <li><code>map</code>: A map of key-value pairs.</li> <li><code>set</code>: A set of unique values.</li> <li><code>object</code>: a structured object with a nested schema</li> <li><code>tuple</code>: a structured list with a nested schema</li> <li><code>any</code>: any of the above types ```</li> </ul>"},{"location":"schema/validators/","title":"\u2705 Validators","text":"<p>This guide provides a reference for the <code>pyvider</code> schema validators.</p>"},{"location":"schema/validators/#overview","title":"\ud83d\udcc4 Overview","text":"<p>Validators are used to enforce constraints on the values in your schema. Validators are defined using the <code>Validator</code> class.</p>"},{"location":"schema/validators/#basic-example","title":"\ud83d\ude80 Basic Example","text":"<p>Here is a basic example of how to define a validator:</p> <pre><code>from pyvider.schema import Attribute, Validator\n\nmy_attribute = Attribute(\n    name=\"my_attribute\",\n    type=\"string\",\n    required=True,\n    description=\"This is my attribute.\",\n    validators=[\n        Validator(\n            expression=\"len(value) &gt; 0\",\n            message=\"my_attribute must not be empty.\",\n        )\n    ],\n)\n</code></pre> <p>In this example, we define a validator that checks that the length of the <code>my_attribute</code> value is greater than 0.</p>"},{"location":"schema/validators/#arguments","title":"\u2699\ufe0f Arguments","text":"<p>The <code>Validator</code> class accepts the following arguments:</p> <ul> <li><code>expression</code>: A Python expression that returns <code>True</code> if the value is valid, and <code>False</code> otherwise. The <code>value</code> variable is available in the expression, and contains the value of the attribute or block.</li> <li><code>message</code>: The error message to display if the validation fails. ```</li> </ul>"},{"location":"tutorials/first-provider/","title":"Building Your First Provider","text":"<p>Note: This documentation is currently under development.</p> <p>This guide will walk you through creating your first Terraform provider using PyVider.</p>"},{"location":"tutorials/first-provider/#coming-soon","title":"Coming Soon","text":"<ul> <li>Step-by-step provider creation</li> <li>Resource and data source implementation</li> <li>Testing and validation</li> <li>Publishing your provider</li> </ul> <p>For now, please refer to the API Reference and Creating Providers guide.</p>"},{"location":"tutorials/file-content/01-introduction/","title":"Chapter 10: The Scribe - The <code>file_content</code> Resource","text":"<p>Welcome, scribe, to the scriptorium. In this chapter, we'll learn how to use the <code>file_content</code> resource to write our own stories and bring them to life.</p>"},{"location":"tutorials/file-content/01-introduction/#the-task","title":"\ud83d\uddfa\ufe0f The Task","text":"<p>Our task is to create a resource that can manage the content of a file on the local filesystem. This will allow us to create, read, update, and delete files with Terraform.</p>"},{"location":"tutorials/file-content/01-introduction/#what-youll-need","title":"\ud83c\udf92 What You'll Need","text":"<p>Before we begin, you'll need to have a good understanding of the following concepts:</p> <ul> <li>The <code>pyvider</code> architecture</li> <li>The <code>pyvider</code> schema</li> <li>The <code>pyvider</code> developer guide</li> </ul> <p>If you're not familiar with these concepts, I recommend that you review the previous chapters before proceeding.</p> <p>Ready? Let's begin! ```</p>"},{"location":"tutorials/file-content/02-the-file-content-resource/","title":"\u270d\ufe0f The <code>file_content</code> Resource","text":"<p>Now, let's create the <code>file_content</code> resource.</p>"},{"location":"tutorials/file-content/02-the-file-content-resource/#the-schema","title":"\ud83d\udcc4 The Schema","text":"<p>First, let's define the schema for our resource. Create a new file called <code>resources/file_content.py</code> and add the following code:</p> <pre><code>from pyvider.resources import resource, ResourceConfig\n\n@resource\nclass FileContent(ResourceConfig):\n    \"\"\"\n    Manages the content of a file on the local filesystem.\n    \"\"\"\n    path: str\n    content: str\n</code></pre> <p>In this schema, we define a resource named <code>FileContent</code> with two string attributes: <code>path</code> and <code>content</code>.</p>"},{"location":"tutorials/file-content/02-the-file-content-resource/#the-logic","title":"\ud83e\udde0 The Logic","text":"<p>Now, let's implement the logic for our resource.</p> <pre><code>from pathlib import Path\nfrom pyvider.resources import resource, ResourceConfig\n\n@resource\nclass FileContent(ResourceConfig):\n    \"\"\"\n    Manages the content of a file on the local filesystem.\n    \"\"\"\n    path: str\n    content: str\n\n    def create(self, ctx):\n        Path(self.path).write_text(self.content)\n        ctx.state[\"id\"] = self.path\n\n    def read(self, ctx):\n        try:\n            content = Path(self.path).read_text()\n            ctx.state[\"content\"] = content\n        except FileNotFoundError:\n            ctx.state = {}\n\n    def update(self, ctx):\n        Path(self.path).write_text(self.content)\n\n    def delete(self, ctx):\n        Path(self.path).unlink()\n</code></pre> <p>And with that, we've created our <code>file_content</code> resource! In the next section, we'll learn how to test our new component. ```</p>"},{"location":"tutorials/file-content/03-testing-the-file-content-resource/","title":"\ud83e\uddea Testing the <code>file_content</code> Resource","text":"<p>Now that we've created our <code>file_content</code> resource, it's time to test it.</p>"},{"location":"tutorials/file-content/03-testing-the-file-content-resource/#the-test-plan","title":"\ud83d\udcc4 The Test Plan","text":"<p>We'll create a new test file called <code>tests/test_file_content.py</code> and add a test for the <code>file_content</code> resource.</p>"},{"location":"tutorials/file-content/03-testing-the-file-content-resource/#the-file_content-resource-test","title":"\ud83d\ude80 The <code>file_content</code> Resource Test","text":"<p>Here is the test for the <code>file_content</code> resource:</p> <pre><code>from my_provider.resources import FileContent\n\ndef test_file_content_resource(tmp_path):\n    file_path = tmp_path / \"hello.txt\"\n\n    # Create the resource\n    resource = FileContent(\n        path=str(file_path),\n        content=\"Hello, Pyvider!\",\n    )\n    resource.create(None)\n    assert file_path.read_text() == \"Hello, Pyvider!\"\n\n    # Read the resource\n    resource.read(None)\n    assert resource.content == \"Hello, Pyvider!\"\n\n    # Update the resource\n    resource.content = \"Hello, World!\"\n    resource.update(None)\n    assert file_path.read_text() == \"Hello, World!\"\n\n    # Delete the resource\n    resource.delete(None)\n    assert not file_path.exists()\n</code></pre>"},{"location":"tutorials/file-content/03-testing-the-file-content-resource/#running-the-tests","title":"\ud83c\udfc3\u200d\u2640\ufe0f Running the Tests","text":"<p>Now, let's run the tests:</p> <pre><code>pytest\n</code></pre> <p>And with that, we've tested our new component!</p>"},{"location":"tutorials/file-content/03-testing-the-file-content-resource/#the-scribes-masterpiece","title":"\ud83c\udf89 The Scribe's Masterpiece","text":"<p>Congratulations, scribe! You've successfully created a <code>pyvider</code> resource that can manage the content of a file on the local filesystem.</p> <p>Now, go forth and write your own stories! ```</p>"},{"location":"tutorials/http-api/01-introduction/","title":"Chapter 9: The Oracle - The <code>http_api</code> Data Source","text":"<p>Welcome, seeker of knowledge, to the oracle's chamber. In this chapter, we'll learn how to consult the oracle and retrieve information from the web using the <code>http_api</code> data source.</p>"},{"location":"tutorials/http-api/01-introduction/#the-quest","title":"\ud83d\uddfa\ufe0f The Quest","text":"<p>Our quest is to create a data source that can make an HTTP GET request to a given URL and return the response body. This will allow us to fetch data from any API on the web and use it in our Terraform configurations.</p>"},{"location":"tutorials/http-api/01-introduction/#what-youll-need","title":"\ud83c\udf92 What You'll Need","text":"<p>Before we begin, you'll need to have a good understanding of the following concepts:</p> <ul> <li>The <code>pyvider</code> architecture</li> <li>The <code>pyvider</code> schema</li> <li>The <code>pyvider</code> developer guide</li> </ul> <p>If you're not familiar with these concepts, I recommend that you review the previous chapters before proceeding.</p> <p>Ready? Let's begin! ```</p>"},{"location":"tutorials/http-api/02-the-http-api-data-source/","title":"\ud83d\udd2e The <code>http_api</code> Data Source","text":"<p>Now, let's create the <code>http_api</code> data source.</p>"},{"location":"tutorials/http-api/02-the-http-api-data-source/#the-schema","title":"\ud83d\udcc4 The Schema","text":"<p>First, let's define the schema for our data source. Create a new file called <code>data_sources/http_api.py</code> and add the following code:</p> <pre><code>from pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass HttpApi(DataSourceConfig):\n    \"\"\"\n    Makes an HTTP GET request to a given URL and returns the response body.\n    \"\"\"\n    url: str\n</code></pre> <p>In this schema, we define a data source named <code>HttpApi</code> with a single string attribute: <code>url</code>.</p>"},{"location":"tutorials/http-api/02-the-http-api-data-source/#the-logic","title":"\ud83e\udde0 The Logic","text":"<p>Now, let's implement the logic for our data source. We'll use the <code>httpx</code> library to make the HTTP GET request.</p> <p>First, we need to add <code>httpx</code> to our <code>pyproject.toml</code> file:</p> <pre><code># pyproject.toml\n\n[project]\n# ...\ndependencies = [\n    # ...\n    \"httpx\",\n]\n</code></pre> <p>Now, let's update our <code>data_sources/http_api.py</code> file with the implementation of the <code>HttpApi</code> data source:</p> <pre><code>import httpx\nfrom pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass HttpApi(DataSourceConfig):\n    \"\"\"\n    Makes an HTTP GET request to a given URL and returns the response body.\n    \"\"\"\n    url: str\n\n    def read(self, ctx):\n        response = httpx.get(self.url)\n        response.raise_for_status()\n        return {\n            \"id\": self.url,\n            \"body\": response.text,\n        }\n</code></pre> <p>And with that, we've created our <code>http_api</code> data source! In the next section, we'll learn how to test our new component. ```</p>"},{"location":"tutorials/http-api/03-testing-the-http-api-data-source/","title":"\ud83e\uddea Testing the <code>http_api</code> Data Source","text":"<p>Now that we've created our <code>http_api</code> data source, it's time to test it.</p>"},{"location":"tutorials/http-api/03-testing-the-http-api-data-source/#the-test-plan","title":"\ud83d\udcc4 The Test Plan","text":"<p>We'll create a new test file called <code>tests/test_http_api.py</code> and add a test for the <code>http_api</code> data source.</p>"},{"location":"tutorials/http-api/03-testing-the-http-api-data-source/#the-http_api-data-source-test","title":"\ud83d\ude80 The <code>http_api</code> Data Source Test","text":"<p>Here is the test for the <code>http_api</code> data source:</p> <pre><code>from my_provider.data_sources import HttpApi\n\ndef test_http_api_data_source(httpx_mock):\n    httpx_mock.add_response(\n        method=\"GET\",\n        url=\"https://example.com\",\n        text=\"Hello, Pyvider!\",\n    )\n\n    data_source = HttpApi(\n        url=\"https://example.com\",\n    )\n    result = data_source.read(None)\n    assert result[\"body\"] == \"Hello, Pyvider!\"\n</code></pre>"},{"location":"tutorials/http-api/03-testing-the-http-api-data-source/#running-the-tests","title":"\ud83c\udfc3\u200d\u2640\ufe0f Running the Tests","text":"<p>Now, let's run the tests:</p> <pre><code>pytest\n</code></pre> <p>And with that, we've tested our new component!</p>"},{"location":"tutorials/http-api/03-testing-the-http-api-data-source/#the-oracles-wisdom","title":"\ud83c\udf89 The Oracle's Wisdom","text":"<p>Congratulations, seeker of knowledge! You've successfully created a <code>pyvider</code> data source that can consult the oracle and retrieve information from the web.</p> <p>Now, go forth and use your newfound power to build amazing things! ```</p>"},{"location":"tutorials/jq-components/01-introduction/","title":"Chapter 8: The Grand Tour - Creating the <code>jq</code> Components","text":"<p>Welcome, artisan, to the grand tour. In this chapter, we'll embark on a journey to create a real-world <code>pyvider</code> component from scratch. We'll build a <code>jq</code> function and a <code>jq</code> data source that can be used to query JSON data.</p>"},{"location":"tutorials/jq-components/01-introduction/#the-destination","title":"\ud83d\uddfa\ufe0f The Destination","text":"<p>Our destination is a fully functional <code>jq</code> component that can be used to query JSON data in our Terraform configurations. We'll create a <code>jq</code> function that can be used to apply a <code>jq</code> filter to a JSON string, and a <code>jq</code> data source that can be used to read a JSON file and apply a <code>jq</code> filter to it.</p>"},{"location":"tutorials/jq-components/01-introduction/#what-youll-need","title":"\ud83c\udf92 What You'll Need","text":"<p>Before we begin, you'll need to have a good understanding of the following concepts:</p> <ul> <li>The <code>pyvider</code> architecture</li> <li>The <code>pyvider</code> schema</li> <li>The <code>pyvider</code> developer guide</li> </ul> <p>If you're not familiar with these concepts, I recommend that you review the previous chapters before proceeding.</p> <p>Ready? Let's begin! ```</p>"},{"location":"tutorials/jq-components/02-the-jq-function/","title":"\ud83d\udee0\ufe0f The <code>jq</code> Function","text":"<p>The first component we'll create is the <code>jq</code> function. This function will take a JSON string and a <code>jq</code> filter as input, and return the result of applying the filter to the JSON string.</p>"},{"location":"tutorials/jq-components/02-the-jq-function/#the-schema","title":"\ud83d\udcc4 The Schema","text":"<p>First, let's define the schema for our function. Create a new file called <code>functions/jq.py</code> and add the following code:</p> <pre><code>from pyvider.functions import function\n\n@function\ndef jq(json: str, filter: str) -&gt; str:\n    \"\"\"\n    Applies a jq filter to a JSON string.\n    \"\"\"\n    # We'll implement this later.\n    pass\n</code></pre> <p>In this schema, we define a function named <code>jq</code> with two string arguments: <code>json</code> and <code>filter</code>. The function returns a string.</p>"},{"location":"tutorials/jq-components/02-the-jq-function/#the-logic","title":"\ud83e\udde0 The Logic","text":"<p>Now, let's implement the logic for our function. We'll use the <code>pyjq</code> library to apply the <code>jq</code> filter to the JSON string.</p> <p>First, we need to add <code>pyjq</code> to our <code>pyproject.toml</code> file:</p> <pre><code># pyproject.toml\n\n[project]\n# ...\ndependencies = [\n    # ...\n    \"pyjq\",\n]\n</code></pre> <p>Now, let's update our <code>functions/jq.py</code> file with the implementation of the <code>jq</code> function:</p> <pre><code>import pyjq\nfrom pyvider.functions import function\n\n@function\ndef jq(json_string: str, filter: str) -&gt; str:\n    \"\"\"\n    Applies a jq filter to a JSON string.\n    \"\"\"\n    return pyjq.one(filter, text=json_string)\n</code></pre> <p>And with that, we've created our <code>jq</code> function! In the next section, we'll create the <code>jq</code> data source. ```</p>"},{"location":"tutorials/jq-components/03-the-jq-data-source/","title":"\ud83d\udcca The <code>jq</code> Data Source","text":"<p>Now that we have our <code>jq</code> function, let's create the <code>jq</code> data source. This data source will read a JSON file and apply a <code>jq</code> filter to it.</p>"},{"location":"tutorials/jq-components/03-the-jq-data-source/#the-schema","title":"\ud83d\udcc4 The Schema","text":"<p>First, let's define the schema for our data source. Create a new file called <code>data_sources/jq.py</code> and add the following code:</p> <pre><code>from pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass Jq(DataSourceConfig):\n    \"\"\"\n    Reads a JSON file and applies a jq filter to it.\n    \"\"\"\n    path: str\n    filter: str\n</code></pre> <p>In this schema, we define a data source named <code>Jq</code> with two string attributes: <code>path</code> and <code>filter</code>.</p>"},{"location":"tutorials/jq-components/03-the-jq-data-source/#the-logic","title":"\ud83e\udde0 The Logic","text":"<p>Now, let's implement the logic for our data source. We'll use the <code>pyjq</code> library to apply the <code>jq</code> filter to the JSON file.</p> <pre><code>import json\nimport pyjq\nfrom pyvider.data_sources import data_source, DataSourceConfig\n\n@data_source\nclass Jq(DataSourceConfig):\n    \"\"\"\n    Reads a JSON file and applies a jq filter to it.\n    \"\"\"\n    path: str\n    filter: str\n\n    def read(self, ctx):\n        with open(self.path, \"r\") as f:\n            data = json.load(f)\n\n        result = pyjq.one(self.filter, data)\n\n        return {\n            \"id\": self.path,\n            \"result\": result,\n        }\n</code></pre> <p>And with that, we've created our <code>jq</code> data source! In the next section, we'll learn how to test our new components. ```</p>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/","title":"\ud83e\uddea Testing the <code>jq</code> Components","text":"<p>Now that we've created our <code>jq</code> components, it's time to test them.</p>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/#the-test-plan","title":"\ud83d\udcc4 The Test Plan","text":"<p>We'll create a new test file called <code>tests/test_jq.py</code> and add the following tests:</p> <ul> <li>A test for the <code>jq</code> function.</li> <li>A test for the <code>jq</code> data source.</li> </ul>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/#the-jq-function-test","title":"\ud83d\ude80 The <code>jq</code> Function Test","text":"<p>Here is the test for the <code>jq</code> function:</p> <pre><code>from my_provider.functions import jq\n\ndef test_jq_function():\n    json_string = '{\"foo\": \"bar\"}'\n    filter = \".foo\"\n    result = jq(json_string, filter)\n    assert result == \"bar\"\n</code></pre>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/#the-jq-data-source-test","title":"\ud83d\udcca The <code>jq</code> Data Source Test","text":"<p>Here is the test for the <code>jq</code> data source:</p> <pre><code>from my_provider.data_sources import Jq\n\ndef test_jq_data_source(tmp_path):\n    json_file = tmp_path / \"test.json\"\n    json_file.write_text('{\"foo\": \"bar\"}')\n\n    data_source = Jq(\n        path=str(json_file),\n        filter=\".foo\",\n    )\n    result = data_source.read(None)\n    assert result[\"result\"] == \"bar\"\n</code></pre>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/#running-the-tests","title":"\ud83c\udfc3\u200d\u2640\ufe0f Running the Tests","text":"<p>Now, let's run the tests:</p> <pre><code>pytest\n</code></pre> <p>And with that, we've tested our new components!</p>"},{"location":"tutorials/jq-components/04-testing-the-jq-components/#the-grand-finale","title":"\ud83c\udf89 The Grand Finale","text":"<p>Congratulations, artisan! You've successfully created a real-world <code>pyvider</code> component from scratch. You've learned how to create a function and a data source, and how to test them.</p> <p>Now, go forth and create your own amazing <code>pyvider</code> components! The world is your oyster. ```</p>"}]}